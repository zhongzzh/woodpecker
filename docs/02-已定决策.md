# 02 已定决策

> 只增不删。每条记录：决策、理由、日期。推翻旧决策时新增一条并标注"取代 D#"。

| # | 决策 | 理由 | 日期 |
| --- | --- | --- | --- |
| D1 | 输入由人工从邮件/MR 中提取后交给工具，**不做邮件自动解析** | 邮件解析投入大、脆弱；人工提取每期只花几分钟，先保精准 | 2026-07-13 |
| D2 | Git 仓库用用户本人账号凭证访问（clone + checkout），现阶段**不依赖 GitLab API token** | 现有账号已够用，降低前置条件 | 2026-07-13 |
| D3 | 交付物是一份**总分析报告**：性能判定 + 单测覆盖缺口清单 + 补充测试代码；**代码只写进报告，不改仓库** | 先建立"人审报告"的工作方式，落库与否由人决定 | 2026-07-13 |
| D4 | 对开发提交的代码、文档、测试**零改动**（纯只读分析） | 测试职责边界；避免污染开发分支 | 2026-07-13 |
| D5 | 性能判定标准暂不细化，报告中预留"标准"字段，标准原文由用户后续提供 | 标准已存在但尚未录入，不阻塞框架讨论 | 2026-07-13 |
| D6 | 现有 function-test-coverage-baseline skill **仅作方法论参考，不直接挂接** | 新流程要求更精准、更贴合这四个库的实际约定与可用性 | 2026-07-13 |
| D7 | **先把方案讨论清楚再实现**；实现形态（skill / agent / workflow / 脚本）暂不选型 | 用户明确要求：没想明白不动手 | 2026-07-13 |
| D8 | **任务卡大幅精简**：人工只提供「任务名（如"新增 xxx 函数"）+ 代码仓库/MR 链接 + 文档链接」三样；分支名、md 文档内容、单元测试代码、性能报告全部由工具沿链接自行定位（分支/单测/性能报告都在 MR 里，文档因是新建文档需单独给链接）。取代 03-A 的重字段草案；载体（文件还是对话）仍未定 | 用户反馈原字段表"输入太多"；能推导的不让人填 | 2026-07-13 |
| D9 | **MATLAB 保留为补测 baseline**：补写用例时没有标准答案，无法凭空写 expected，需要用 MATLAB 的计算结果与 Julia（Syslab）结果对照 | 用户原话："你没有正确答案，你怎么写 test"；注意是"有时候有用"，非每个用例都强制 | 2026-07-13 |
| D10 | 补充的测试代码**现阶段不写入任何文件、不做任何改动**，等流程稳定之后再考虑 | 用户明确："先不要写，不要改动，等稳定之后再说"；与 D3/D4 一致并进一步收紧 | 2026-07-13 |
| D11 | 任务类型实际为**两类**；首期**只实现"新增函数"这一类**，另一类之后再扩展 | 先把一条链路走通，避免摊子铺太大 | 2026-07-13 |
| D12 | 性能报告真实样例定位在 **TyStatisticsCore.jl MR !472** 的 note；获取方式为 **playwright MCP 带浏览器登录态访问**（匿名访问被 302 到登录页，WebFetch/curl 不可行） | 用户提供链接并指定用 playwright MCP 查看；2026-07-13 实测匿名不可达 | 2026-07-13 |
| D13 | **性能判定标准**（落实 D5）：设参考时间 T（基准时间或 MATLAB 运行时间），检查耗时比值 x——**T>1s 时 x>1.2 不通过/衰退；0.1s≤T≤1s 时 x>1.25 不通过/衰退；T<0.1s 时 x>1.5 不通过/衰退**。新增函数任务：看报告**分支版本详细数据**中每个用法的 `execute_time_first_diff`（首次）与 `execute_time_average_diff`（二次）两个比值 | 用户提供标准原文；T 越小阈值越宽（短耗时受计时噪声影响大）；细节确认见 03-F | 2026-07-14 |
| D14 | 新增函数任务的性能报告**取同 MR 中最新一条** | 用户明确；同一 MR 会随开发迭代发多轮报告 | 2026-07-14 |
| D15 | **首次与二次执行时间都必须达标**，且首次与二次用**同一套 D13 阈值**，对首次（含 JIT）没有任何特殊豁免；实际报告中大部分函数的首次也是通过的（开发侧会用 precompile 等手段压首次耗时） | 用户明确纠正："首次和二次都要达标……首次也要按照这个标准要求"；此前"JIT 导致首次普遍超标"是对单个性能优化样例的错误外推 | 2026-07-14 |
| D16 | **性能判定执行细节定稿**：① 每项检查的 T 取对应项的 MATLAB 耗时（首次检查用 m_first、二次检查用 m_average；性能优化任务换成基准版 Julia 耗时）；② 任一用法的任一项超标 → 该函数性能测试整体不通过，报告逐用法列明细（T/阈值/x/结论）；③ x 恰好等于阈值算通过；④ 机器人 perf_status 仅作旁证，与 D13 演算不一致时在报告中标注 | 用户对完整讲解确认"可以的，性能这里大致就是这样"（2026-07-14） | 2026-07-14 |
| D17 | **取数流程定稿**（详见 06 文档）：克隆位置固定 `C:\Users\TR\Desktop\doc\<库名>.jl`、已存在则不重复克隆 → 从 MR 页面读源分支 → fetch/checkout/pull → 单测按"报告路径指引 / test 目录约定 / 全仓搜索"三级定位 | 用户口述流程，已用 partialcorri 实走验证 | 2026-07-14 |
| D21 | **要做 UI 软件**给不懂命令行的小白用。方案详见 07 文档。四项选型（2026-07-15 用户拍板）：①**先只本人本机用**（分发/多人后置到 P4）；②UI 形态=**本地网页**（Python 后端 + 浏览器 + 双击启动脚本）；③AI 通道=**anthropic SDK 直连 Messages API**（本机已有 `ANTHROPIC_AUTH_TOKEN` + `ANTHROPIC_BASE_URL` 中转代理，SDK 自动读取；模型需在代码显式指定，待测代理支持哪些）；④读 MR 页面=**继续用 playwright**（不申请 GitLab token） | 用户明确要做 UI 且逐项选定 | 2026-07-15 |
| D22 | **架构=单次 LLM 调用 + 确定性工作流**（非多轮 agent）：全管线只有"覆盖分析"一步需要 AI，其余（取数/定位/性能判定/汇总）全是确定性脚本。只读边界（D18）在代码层强制——repo 模块不实现 push/commit 函数 | 管线拆解后 AI 仅一处；简单形态更稳、可测、易维护 | 2026-07-15 |
| D18 | **权限边界：严格只读**。工具只允许 clone/fetch/checkout/pull 到本地 + 读文件；**禁止 push、禁止 commit、禁止修改/删除仓库中任何代码、文档、测试**。切分支遇到用户本地未提交改动时用 `git stash` 暂存（不丢弃、不覆盖），事后可 `git stash pop` 还原 | 用户强调"你没有任何修改、提交的权限……只有拉取到本地然后阅读的权限"；强化 D4/D10 | 2026-07-14 |
| D19 | **覆盖检查由 AI 完成**：把函数文档 md + 对应单元测试代码喂给 AI，让它产出分析报告（覆盖缺口 + 补充测试用例建议）；工具负责取数与转交材料、承接报告并交给用户。报告只呈现，不落库（沿用 D3/D10/D18） | 用户明确"需要加入 ai……把 md 文档和单元测试代码给它,让它输出报告" | 2026-07-14 |
| D20 | **覆盖分析规则以用户提示词 v1 为基线**（原文归档 `docs/prompts/覆盖分析提示词-v1.md`，允许后续优化出 v2）：参数×结构形式×数据类型细粒度核对、文档缺陷审计、四档覆盖判定、补全代码仿原风格/完整可运行/少引入新代码；报告按提示词六段结构输出 | 用户提供提示词并授权"进一步优化格式内容"；首份报告已按此产出（tasks/polydiv/） | 2026-07-15 |
| D23 | **增加第二种任务类型“函数性能优化”**。示例：`TyDifferentialEquation：ode89 函数性能优化`；本轮先形成方案文档，不修改实现代码 | 用户明确要求在现有“新增函数”之外增加性能优化任务，并要求先梳理、记录 | 2026-07-17 |
| D24 | 性能优化任务的人工输入为**任务名 + 代码 MR 链接**，不要求文档 MR；既有函数文档从本地 `C:\Users\TR\Desktop\doc\syslab-docs-2.0` 自动定位 | 性能优化针对既有函数，文档已在本地文档仓库，无需重复提供链接 | 2026-07-17 |
| D25 | 性能优化只取代码 MR 中**报告时间最新**的一条性能报告；参照系为**分支版 Julia / 基准版 Julia**。首次 T 取基准表 `j_execute_time_first`，二次 T 取基准表 `j_execute_time_average` | 用户明确性能优化只看最新报告，且目标是判断分支相对基准是否衰退 | 2026-07-17 |
| D26 | 性能优化摘要中的“变快 y%”换算 `x=1-y/100`，“变慢 y%”换算 `x=1+y/100`；再按 D13 三档阈值判定。x 等于阈值未衰退，任一首次/二次项超阈值则该用法【发生衰退】 | 用户给出数据处理公式与输出要求；边界和整体聚合沿用 D15/D16 | 2026-07-17 |
| D27 | 详细表的 `execute_time_first_diff` / `execute_time_average_diff` 是 Julia/MATLAB 比值，**不能作为性能优化的分支/基准 x**；性能优化需解析摘要百分比，并以基准/分支表中的 Julia 时间交叉校验或补算 | 已用 MR !472 样例数值核实字段语义；避免复用现有新增函数算法时误判 | 2026-07-17 |
| D28 | 性能优化报告逐用法明确输出【未衰退（性能提升/持平）】或【发生衰退】；摘要缺项时从最新报告的两张详细表补算，数据缺失/无法配对时标为【无法判定】，不得静默当作通过 | 用户要求每个用法明确结论；其 ode89 摘要片段未展示全部 3 个用法的首次/二次项，需要补齐机制 | 2026-07-17 |
| D29 | **源码迁移采用自动自举启动**：GitHub clone 后双击 `启动.bat` 自动创建 `.venv`、安装 `requirements.txt`、下载 Chromium，并在缺少 `.pw-state.json` 时引导个人 GitLab 登录；被测仓库缓存默认改为当前用户 `%LOCALAPPDATA%\woodpecker\repos`，取代 D17/D24 中绑定 `C:\Users\TR` 的固定路径（仍可用 `WOODPECKER_CLONE_ROOT` 覆盖） | `.venv`、浏览器和登录态都不应提交；固定用户名路径在其他电脑必然失效。自动重建既保留源码仓库轻量，也让 clone 后的首次使用只需双击 | 2026-07-18 |
