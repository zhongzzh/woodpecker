# 03 待讨论问题

> 讨论的主战场。每项有结论后，沉淀到 `02-已定决策.md`，并在此标记"已定（→D#）"。

## A. 输入契约：一个任务到底给工具什么？【字段已定 →D8，载体待定】

字段已拍板（D8），任务卡只剩三样，其余工具自己找：

| 字段 | 示例 | 说明 |
| --- | --- | --- |
| 任务名 | 新增 surfaceArea 函数 | 含函数名与任务类型 |
| 代码仓库/MR 链接 | …/merge_requests/330 | 分支名、单测代码、性能报告（note）都从这里推导 |
| 文档链接 | 仓库内路径或另一 MR 链接 | 新增函数的文档是新建的，工具无法凭空猜，必须给 |

工具侧的推导链（用户描述的流程）：
文档链接 → 找到 md 文档内容；MR 链接 → 查源分支 → clone/checkout →
在该分支上找单元测试代码；性能报告就在 MR 的 note 里。

**仍待拍板：**

1. 任务卡载体：Markdown 模板文件？还是对话里直接口述？
   （初步倾向：`tasks/<期号>/<函数名>/task.md`，人填模板、工具读文件——可追溯、可重跑。）
2. 从 MR 链接查源分支需要登录态 → 依赖 playwright MCP（见 F 项）；
   MCP 不可用时的降级方案：人工把分支名补进任务卡。

## B. 报告结构【最优先】

草案见 `04-分析报告模板草案.md`。**待拍板：**

1. 一个任务一份报告、期末拼一份汇总？还是直接一份大报告？
2. 补充测试代码要求到什么程度：能直接复制进测试文件运行的级别？
3. 报告格式：Markdown 够不够？（给谁看，决定了要不要 HTML/Word。）

## C. 实现形态：skill / subagent / workflow / 纯脚本【想清楚再选】

比较维度：

| 维度 | 关键问题 |
| --- | --- |
| 交互性 | 哪些步骤必须人确认？（例：缺口清单先人审，通过后再生成用例？） |
| 上下文量 | 一个函数的文档+测试+相关源码有多大？单个对话装不装得下 |
| 并行度 | 一期约 5 个任务，要不要并行跑 |
| 可复现 | 换台机器/换个实习生，能不能照着跑出同样的东西 |
| 失败恢复 | 中途断了，能不能只重跑一个任务而不从头来 |

**初步倾向（未定，仅供讨论）：**

- 确定性步骤（clone/checkout、定位文件、收集材料）→ 脚本，稳定可靠；
- 分析步骤（读文档、找覆盖缺口、写用例、判性能）→ 模型做；
- 载体选型（skill 还是 agent 还是 workflow）等 A、B 定型后再决定——
  只要"输入任务卡、输出报告"这个接口稳定，换载体成本很低，不必现在锁死。

## D. 四个库的目录/命名约定【TyStatisticsCore 已实测 → 06 文档】

TyStatisticsCore.jl 实测结论（2026-07-14，详见 06 文档）：

- 单测：`test/<大类>/<小类>/<函数名>.jl`，一函数一文件，数据在同名 `_data_N.jl` /
  `_ai_data.jl`；runtests.jl 用 TyAPI 的 `@test_package` 自动发现；
- benchmark：`benchmark/<大类'>/<小类>/<函数名>/PerformanceTest/PerformanceTestN/`，
  `.jl` 与 `.m` 成对；benchmark 与 test 的大类命名**不一致**，不能互推；
- testset 注释里附 MATLAB 原代码；仓库里已有 `_ai_data.jl` + `AI_assert_matlab_equal`
  的 AI 辅助测试先例，补测用例可沿用该模式。

**待补：** 其余几个库（TyMathCore、TyComputationalGeometry、TyGraphTheory、TyOptimCore）
是否同构——本地已有克隆，跑第一个真实任务前顺手抽查即可。
函数文档路径规则**已实测**（→ 06 文档第 5 步）：独立仓库 syslab-docs-2.0（目标分支 develop），
`syslabHelpSourceCode/projects/<Ty项目>/Doc/.../<函数名>.md`，用 MR diff 中新增的 md 定位最稳；
md 固定结构：语法/说明/示例/输入参数/输出参数/另请参阅。

## E. 要不要在本机真的跑？【方向已明，细节待定】

已明确（→D9）：MATLAB 作为补测 baseline **有用**——补写用例时没有标准答案，
需要 MATLAB 的计算结果来对照 Julia（Syslab）的输出，否则 expected 无从写起。
即方向上至少到"档位 3"（静态 + baseline），但"有时候有用"，不是每个用例都强制跑。

三个档位（保留作参考）：

1. **纯静态**：只读文档+测试代码做覆盖分析，补充用例只做静态检验（不运行）。
2. **静态 + 回归**：额外在本机跑一遍开发已有的测试。
3. **静态 + 回归 + baseline**：补充用例的 expected 经 MATLAB baseline 验证后回填。

**仍待确认：** 用户日常怎么跑这些库的测试（命令、registry、Syslab 版本）；
哪些函数 MATLAB 有对应物、哪些没有（没有对应物时 expected 从哪来）。

## F. 性能报告样例与标准【已定稿 →D12~D16，本项关闭】

- 样例已抓取归档：MR !472 最新报告 note_385041，分析见 `05-性能报告样例分析.md`，
  原文见 `samples/MR472-性能报告-note385041.json`。（2026-07-14，playwright MCP）
- 报告由机器人 mtest2 自动发布、同 MR 多轮取最新；标准与执行细节全部沉淀在 D13~D16。
- 用户 2026-07-14 对完整讲解确认通过（"性能这里大致就是这样"）。
- 注意：TyStatisticsCore 不在 01 文档原先的四库清单里，说明涉及库不止四个。
- 唯一遗留（非阻塞）：待拿到第一个新增函数 MR 时，顺手验证其报告与样例同构、
  执行细节演算结果与用户预期一致。

## G. 各任务类型的测试范围定义【新增函数、性能优化已定 →D19/D20/D23~D28】

已定：新增函数的覆盖分析**以文档为准**（提示词 v1 的立场：文档 vs 测试比对，参数×形式×类型
细粒度核对），同时附带文档自身缺陷审计；补充用例的 expected 优先复用原文件已验证数值或
数学推导，语义未定义/无法推导的场景挂起待 MATLAB baseline 确认（与 D9 一致），不凭空断言。

性能优化任务已明确：输入为任务名 + 代码 MR，既有文档从本地文档仓库定位；性能参照系为
分支版 Julia / 基准版 Julia，完整规格见 `08-性能优化任务方案.md`。

| 类型 | 尚待确认的问题 |
| --- | --- |
| 性能优化 | 功能侧当前先复用静态“文档 vs 单测”分析；未来是否增加真实 Syslab 回归执行 |
| 算法优化 | 是否完全并入性能优化任务，还是另设精度/结果一致性规则 |

## H. 节奏：逐任务确认 vs 批量

一期约 5 个任务：逐个人工确认推进，还是一键全跑完出汇总？
（初步倾向：逐个起步，流程跑顺后再考虑批量——待确认。）
