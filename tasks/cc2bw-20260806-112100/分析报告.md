# cc2bw 提测分析报告

> 任务：cc2bw（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_format\test_cc2bw.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/RegionandImageProperties/Find,Select,andLabelObjectsinBinaryImages/cc2bw.md`
> 单测：`test/im_format/test_cc2bw.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-06 11:23 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| CC | 四字段连通分量结构体 | TyImage_CC，字段为 Connectivity、ImageSize、NumObjects、PixelIdxList | 描述二值图像中的连通分量及其像素索引 |
| ObjectsToKeep | 单个对象索引、对象索引向量、逻辑向量 | 正整数、正整数向量、逻辑向量 | 指定需要转换为前景的连通分量；整数索引长度不超过 NumObjects，逻辑向量长度等于 NumObjects |

### 文档自身缺陷审计

- 语法示例使用 `cc2bw(CC,ObjectsToKeep=objectsToKeep)`，但 Julia 实现将 `ObjectsToKeep` 定义为关键字参数，不能按文档所示的普通位置参数调用。
- `CC` 的 `PixelIdxList` 被描述为“1×NumObjects 矩阵”，但 Julia 代码实际按可索引的对象集合使用，更准确的描述应明确为长度为 `NumObjects` 的向量或数组，其元素为像素线性索引向量。
- 文档未明确 `Connectivity`、`ImageSize`、`NumObjects` 和 `PixelIdxList` 的 Julia 数据类型，也未明确 `ImageSize` 支持的维度形式。
- `ObjectsToKeep` 的文档约束只允许正整数，但实现签名允许 `Real` 及 `AbstractVecOrMat{<:Real}`，因此实现还接受浮点数、矩阵等文档未声明形式；实现也未显式校验正性、整数性和索引范围。
- 文档要求逻辑向量长度必须等于 `CC.NumObjects`，而实现对较短逻辑向量进行补零，对较长逻辑向量未显式拒绝，行为与文档约束不完全一致。
- 示例中的 `bbox = []` 依赖后续 `vcat` 推导二维结构，未明确其数据结构；这属于示例稳健性问题，但不影响 `cc2bw` 参数定义本身。

### 参数数据类型与结构覆盖

#### CC

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| TyImage_CC 四字段结构体 | 由 `bwconncomp` 构造并传入 `cc2bw`，结果断言类型和尺寸 | `cc2bw.jl` 的 `src`、`BW = cc2bw(CC)`、`BW = cc2bw(CC;ObjectsToKeep=objectsToKeep)` 测试块 | ☑ |

说明：已有测试明确构造 `CC`、调用函数并断言输出类型和尺寸，但没有逐一断言四个字段的存在及字段间结构关系，属于结构验证较弱的覆盖。

#### ObjectsToKeep

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 正整数 | 显式赋值 `objectsToKeep = 1` 并以关键字传入 | `cc2bw.jl` 的 `BW = cc2bw(CC;ObjectsToKeep=objectsToKeep)` 测试块 | ☑ |
| 正整数向量 | `idx[2:10]` 为向量；另有 `[1 2]` 二维矩阵形式 | `cc2bw.jl` 的 `src` 测试块及 `BW = cc2bw(CC;ObjectsToKeep=objectsToKeep)` 测试块 | ☑ |
| 逻辑向量 | 显式使用 `[true false]` 和 `rand(Bool, 50)` | `cc2bw.jl` 的 `BW = cc2bw(CC;ObjectsToKeep=objectsToKeep)` 测试块 | ☑ |

说明：这些用例均有赋值、调用和结果类型或尺寸断言，但没有断言实际保留的像素集合是否准确；逻辑向量长度与 `CC.NumObjects` 的对应关系也没有显式断言，因此属于弱证据下的覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| CC 连通分量结构体 | 通过 `bwconncomp` 构造并传入 | ☑ | 未逐字段验证 |
| 正整数对象索引 | 选择单个连通分量 | ☑ | 结果仅断言类型和尺寸 |
| 正整数索引向量 | 选择多个连通分量 | ☑ | 同时出现向量和二维矩阵形式 |
| 逻辑对象选择向量 | 按布尔选择连通分量 | ☑ | 未验证长度约束及实际选择结果 |

### 覆盖缺口与优先级

#### 高优先级

- **实际像素选择语义**：现有测试主要断言输出为 `Bool` 且尺寸正确，没有证明 `ObjectsToKeep` 指定的对象被保留、其他对象被排除。建议使用包含两个可分离连通分量的小型二值矩阵，断言选择第一个对象时输出前景像素数和像素位置与 `CC.PixelIdxList[1]` 一致。
- **CC 四字段结构**：现有测试没有逐项验证 `Connectivity`、`ImageSize`、`NumObjects`、`PixelIdxList`。建议增加字段存在性、`NumObjects == length(PixelIdxList)` 及输出尺寸与 `ImageSize` 的关系断言。

#### 中优先级

- **逻辑向量边界长度**：已有逻辑向量调用，但未证明长度等于 `CC.NumObjects`，也未覆盖长度不足或超长时的文档约束行为。建议分别补充合法长度和非法长度的异常或拒绝行为断言。
- **索引边界行为**：未覆盖零、负数、超过 `NumObjects` 的索引。由于文档要求正整数且索引必须对应对象，建议明确预期异常后补测。
- **关键字参数语法一致性**：文档普通语法与实现关键字参数定义不一致，应统一文档或实现，并增加与最终接口一致的调用示例。

#### 低优先级

- **索引容器形式**：文档声明正整数向量，但现有 `[1 2]` 是二维矩阵；建议将文档示例改为一维向量，并单独决定是否公开支持矩阵。
- **空对象集合和空连通分量**：文档未说明 `NumObjects == 0` 或空索引集合的行为，建议补充约束和对应测试。
- **字段具体类型和图像维度**：文档未说明字段类型及一维、二维或更高维图像的支持范围，建议补充类型和维度契约。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| CC | 四字段连通分量结构体 | 四字段连通分量结构体 | 无 | 完全覆盖 |
| ObjectsToKeep | 正整数、正整数向量、逻辑向量 | 正整数、正整数向量、逻辑向量 | 无 | 完全覆盖 |

总计：完全覆盖参数数为 2；至少有测试证据参数数为 2。上述结论仅按文档要求项是否存在构造、调用和结果断言证据判定，不代表实际像素语义、边界异常和字段级断言已经达到强覆盖。

### 缺失测试代码补全

```julia
@testset "cc2bw 补充覆盖" begin
    input = Bool[
        true  false false false
        false false false true
        false false false false
        false false false false
    ]
    CC = bwconncomp(input)

    @test CC isa TyImage_CC
    @test all(name in propertynames(CC) for name in (:Connectivity, :ImageSize, :NumObjects, :PixelIdxList))
    @test CC.ImageSize == size(input)
    @test CC.NumObjects == length(CC.PixelIdxList)

    objectsToKeep = Int[1]
    BW = cc2bw(CC; ObjectsToKeep=objectsToKeep)
    selected_pixels = CC.PixelIdxList[1]
    unselected_pixels = setdiff(1:length(input), selected_pixels)

    @test eltype(BW) == Bool
    @test size(BW) == CC.ImageSize
    @test count(BW) == length(selected_pixels)
    @test all(BW[selected_pixels])
    @test all(.!BW[unselected_pixels])
end
```

静态检验说明：该代码块只有一个新的、独立的顶层 `@testset`，内部变量均在自身作用域内构造，不引用原有测试块的局部变量；使用的 `TyImage_CC`、`bwconncomp` 和 `cc2bw` 均来自测试文件已有导入或被测接口。代码应整体追加到文件末尾，可原样追加到文件末尾；未声称已运行新增测试。

仍需 baseline 校准项：新增用例只使用结构关系和像素集合性质断言，不需要额外的精确 baseline。现有测试中的 `count(BW) == 842` 仍属于依赖 `blobs.png` 固定内容的 fixture baseline，应在测试数据变更时重新校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
cc2bw，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_cc2bw 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_format\test_cc2bw.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\cc2bw.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\RegionandImageProperties\Find,Select,andLabelObjectsinBinaryImages\cc2bw.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
cc2bw，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：cc2bw
遗留缺陷：无
```
