# cmunique 提测分析报告

> 任务：cmunique函数（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_format\test_cmunique.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Graphics/Images/ModifyImageColors/cmunique.md`
> 单测：`test/im_format/test_cmunique.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-07-30 10:20 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| X | m×n 整数矩阵 | Float64、UInt8、UInt16 | 具有重复颜色的索引图像 |
| map | c₁×3 矩阵 | Float64 | 与 X 关联的颜色图，值在 [0,1] |
| RGB | m×n×3 数组 | Float64、UInt8、UInt16 | 真彩色 RGB 图像，值非负 |
| I | m×n 数值矩阵 | Float64、UInt8、UInt16 | 灰度图像 |

### 文档自身缺陷审计

*   **内部类型泄露**：`cmunique.jl` 源码的 `where` 子句包含 `Int64`，但文档输入参数表中未列出 `Int64`。如果该类型不面向用户，应在文档中显式排除；如果支持，则文档遗漏了 `Int64`。
*   **map 参数约束不完整**：文档仅说明 map 是 c₁×3 矩阵且值在 `[0, 1]`，未声明 map 的行数 `c₁` 必须与 X 中的最大索引值兼容（由 `_check_index_image` 校验），也未明确 map 必须是 `Float64` 类型。
*   **输出 Y 的类型描述模糊**：“如果 newmap 的长度小于或等于 256，则输出图像可以属于 uint8 类”使用了“可以”这一不确定表述，与源码逻辑 `<= 256` 则固定返回 `UInt8` 的确定性行为不符。
*   **示例代码存在笔误**：文档示例中 `X = Float64.(magic(4))`，`magic` 返回整数矩阵，转换为 `Float64` 符合索引图像定义，但示例后续 `Y, newmap = cmunique(X, map)` 调用中，`X` 为 `Float64` 类型，其元素作为索引时按源码逻辑应为从 1 开始的正整数。`magic(4)` 的值范围为 1 到 16，且 map 通过 `[gray(8);gray(8)]` 构造大小为 16×3，满足此约束。此点仅为提醒，非文档错误。

### 参数数据类型与结构覆盖

#### X

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | m×n 矩阵，索引图像 | test_cmunique.jl / “输入参数 X 类型为 Float64 ，newmap 长度大于256” | ☑ |
| UInt8 | m×n 矩阵，索引图像 | test_cmunique.jl / “输入参数 X 类型为 UInt8” | ☑ |
| UInt16 | m×n 矩阵，索引图像 | test_cmunique.jl / “输入参数 X 类型为 UInt16 ,newmap 长度大于256，大数据测试” | ☑ |

说明：所有已覆盖项均有类型和形状断言，属于强证据。

#### map

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | c₁×3 矩阵，与 X 联用 | test_cmunique.jl / “输入参数 X 类型为 Float64 ，newmap 长度大于256” | ☑ |

说明：强证据。单点输入（仅 Float64）已完成。文档对 map 仅声明 `Float64` 一种数据类型，无其他待覆盖形式。

#### RGB

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | m×n×3 数组 | test_cmunique.jl / “输入参数 RGB 类型为 Float64” (第一个) | ☑ |
| UInt8 | m×n×3 数组 | test_cmunique.jl / “输入参数 RGB 类型为 Float64” (第二个，实际测试 UInt8) | ☑ |
| UInt16 | m×n×3 数组 | test_cmunique.jl / “输入参数 RGB 类型为 UInt16” | ☑ |

说明：第二个测试集名称写为“RGB 类型为 Float64”，实际构造的是 UInt8 数据，但断言中明确验证了 `typeof(Y)` 等，可作为 UInt8 类型的有效证据。所有已覆盖项均为强证据。

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | m×n 矩阵，灰度图 | test_cmunique.jl / “输入参数 I 类型为 Float64” | ☑ |
| UInt16 | m×n 矩阵，灰度图 | test_cmunique.jl / “输入参数 I 类型为 UInt16” | ☑ |
| UInt8 | m×n 矩阵，灰度图 | — | 未覆盖 |

说明：Float64 和 UInt16 项具备形状和类型断言，为强证据。UInt8 完全缺失测试。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| m×n 整数矩阵 | 索引图像 X | ☑ | Float64/UInt8/UInt16 均已测试 |
| c×3 矩阵 | 颜色图 map | ☑ | 仅 Float64，符合文档 |
| m×n×3 数组 | RGB 图像 | ☑ | Float64/UInt8/UInt16 均已测试 |
| m×n 数值矩阵 | 灰度图像 I | 未覆盖 | 缺少 UInt8 类型的测试 |

### 覆盖缺口与优先级

*   **高优先级**
    *   **缺失类型：I 参数 — UInt8**
        *   **现有证据不足**：`test_cmunique.jl` 中“3_Y,newmap = cmunique(I)”测试块仅覆盖了 Float64 和 UInt16 输入，没有 UInt8 灰度图的任何测试。
        *   **建议补测方向**：构造一个 UInt8 类型的随机灰度矩阵，调用 `cmunique(I)`，断言输出 Y 的类型、形状以及 newmap 的形状和类型。特别地，当唯一灰度级数 ≤ 256 时，应断言 Y 为 `Matrix{UInt8}`。

*   **中优先级**
    *   **未测试场景：RGB 参数在唯一颜色数大于 256 时的输出类型**
        *   **现有证据不足**：现有 RGB 测试用例的唯一颜色数均小于 256，返回的 Y 都是 `UInt8`。文档和源码指出 newmap 长度大于 256 时，Y 应为 `Float64` 类，但此路径在 RGB 输入时未验证。
        *   **建议补测方向**：构造一个 m×n×3 的 Float64 数组，使其包含超过 256 种唯一颜色，调用 `cmunique`，断言 Y 的类型为 `Matrix{Float64}`。

*   **低优先级**
    *   **文档与实现不一致：Int64 类型支持**
        *   **现有证据不足**：文档未声明 Int64 为有效输入类型，但函数签名 `where {T<:Union{UInt8,UInt16,Float64,Int64}}` 表明内部允许该类型。测试中未覆盖任何 Int64 输入。
        *   **建议**：要么补测 Int64 输入，要么从代码中移除该类型支持以与文档对齐。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| X | 3 (Float64, UInt8, UInt16) | 3 | 完全覆盖 |
| map | 1 (Float64) | 1 | 完全覆盖 |
| RGB | 3 (Float64, UInt8, UInt16) | 3 | 完全覆盖 |
| I | 2 (Float64, UInt16) | 3 | 部分覆盖 |

总计：完全覆盖参数数 3 个，至少有证据参数数 4 个。

### 缺失测试代码补全

```julia
@testset "3_Y,newmap = cmunique(I)" begin
    # ... (原有测试)

    @testset "输入参数 I 类型为 UInt8" begin
        I = rand(UInt8, 100, 120)
        Y, newmap = cmunique(I)
        @test size(Y) == (100, 120)
        # UInt8 图像最多 256 个灰度级，因此 newmap 长度必定 ≤ 256
        @test typeof(Y) == Matrix{UInt8}
        @test size(newmap, 2) == 3
        @test typeof(newmap) == Matrix{Float64}
    end
end

@testset "2_Y,newmap = cmunique(RGB)" begin
    # ... (原有测试)

    @testset "RGB 包含超过 256 种唯一颜色" begin
        # 生成 260 种不同颜色的 RGB 图像
        r = repeat(0:259, 1, 100)
        g = zeros(1, 26000)
        b = zeros(1, 26000)
        RGB = cat(reshape(r, 260, 100), reshape(g, 260, 100), reshape(b, 260, 100); dims=3)
        RGB = Float64.(RGB)
        Y, newmap = cmunique(RGB)
        @test size(Y) == (260, 100)
        # newmap 长度 > 256，Y 应为 Float64
        @test typeof(Y) == Matrix{Float64}
        @test size(newmap) == (260, 3)
        @test typeof(newmap) == Matrix{Float64}
    end
end
```

**静态检验说明**

*   新增的 UInt8 测试块直接插入原“3_Y,newmap = cmunique(I)”测试集中，风格与原测试一致。
*   新增的“RGB 包含超过 256 种唯一颜色”测试块直接插入原“2_Y,newmap = cmunique(RGB)”测试集中，复用了原有的断言模式，并明确验证了 `newmap` 长度大于 256 时输出 Y 的类型为 `Float64`。
*   未引入任何新依赖或修改原有测试逻辑。

**仍需 baseline 校准项**

*   新增的 UInt8 灰度图测试和 RGB 大色图测试，均未与独立真值（baseline）进行数值比对。当前断言仅覆盖了类型、形状和色图列数等可验证性质。建议在有标准参考实现或已知结果的数据集上进行数值校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
cmunique函数，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_cmunique 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_format\test_cmunique.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\cmunique.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Graphics\Images\ModifyImageColors\cmunique.md

TyImageProcessing：
```
