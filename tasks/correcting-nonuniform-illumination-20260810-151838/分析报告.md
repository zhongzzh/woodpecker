# correcting-nonuniform-illumination 文档代码一致性报告

> 任务：correcting-nonuniform-illumination（本地材料：C:\Users\TR\Desktop\doc\05_SegmentRiceGrains.jl）
> 任务类型：文档代码一致性检查
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Example\TyImageProcessing\GetStarted\correcting-nonuniform-illumination.md`
> 代码文件：`05_SegmentRiceGrains.jl`
> benchmark：不适用
> HTML 预览：未执行
> 分析规则：本地文本候选初筛＋AI 语义等价复审 ｜ 生成：2026-08-10 15:19 ｜ 模型：deepseek-v4-pro

## 一、文档与代码一致性检查

### 文档代码语义一致性复审（AI）

- Markdown 文档：`correcting-nonuniform-illumination.md`
- 代码文件：`05_SegmentRiceGrains.jl`
- 结论：**发现 2 处明显代码缺失**
- 已复审文本候选：6 条
- 判定为等价实现：1 条

> 本结论按功能目的和数据流判断，不要求文字、变量名、资源路径写法或辅助展示语句完全相同。

**复审摘要：** 文档中两个中间图像显示步骤（imshow(I2) 和 imshow(I3)）在代码文件中缺失，其余差异为语义等价或非必须的替代说明。

#### 明显缺少的代码

##### 1. 步骤 3：从原始图像中减去背景（文档第 47-48 行）

**缺失原因：** 代码文件缺少该中间结果显示步骤，没有等价替代

**文档预期功能：** 显示背景减除后的图像 I2，观察背景均匀化效果

**代码侧证据：** 代码中执行了 I2 = imsubtract(I, background) 但未调用 imshow(I2)，后续直接显示二值图像 bw

文档中的缺失代码：
```
imshow(I2)
```

##### 2. 步骤 4：提高图像对比度（文档第 64-65 行）

**缺失原因：** 代码文件缺少该中间结果显示步骤，没有等价替代

**文档预期功能：** 显示对比度增强后的图像 I3，观察米粒与背景分离效果

**代码侧证据：** 代码中执行了 I3 = imadjust(I2) 但未调用 imshow(I3)，后续直接显示二值图像 bw

文档中的缺失代码：
```
imshow(I3)
```

#### 已确认的等价写法

##### 1. 步骤 1：读取并显示图像

- 文档行号：14-18
- 代码文件行号：9
- 等价理由：路径构造方式不同但读取同一图像资源，语义等价

文档写法：
```
I = imread("rice.png");
```
代码文件中的等价写法：
```
I = imread(joinpath(path, "rice.png"));
```

#### 需要人工留意但不属于缺失
- 步骤3中文档提供了 imtophat 替代方法，代码采用 imsubtract 实现，属于可选路径未实现，非缺失
- 步骤7中文档输出 labeled 的类型和大小（println...typeof/size），代码未包含这两条输出语句，属于辅助展示信息，不影响核心功能
