# gradientweight 提测分析报告

> 任务：gradientweight（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_gradientweight.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/ImageSegmentation/SegmentationTechniques/gradientweight.md`
> 单测：`test/im_math/test_gradientweight.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ImageSegmentation\SegmentationTechniques\gradientweight.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-12 10:57 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 二维数值矩阵或三维数值数组 | Float32、Float64、Int8、UInt8、Int16、UInt16、Int32、UInt32 | 灰度图像 |
| sigma | 正数标量或正数向量 | Float64（文档声明），实现中为 Real 或 AbstractArray | 高斯导数的标准差，默认值 1.5 |
| RolloffFactor | 正标量 | Float64 | 输出权重滚降因子，默认值 3，推荐范围 [0.5, 4] |
| WeightCutoff | [1e-3, 1] 范围内的正数 | Float64 | 权重值的阈值，默认值 0.25 |

### 文档自身缺陷审计

*   **sigma 数据类型声明不完整**：文档只声明 sigma 为 Float64，但从实现和示例来看，sigma 可以是向量 `[1, 1, 1]`（用于三维图像）或 `[1, 2]`（用于二维图像各向异性 sigma）。文档未提及向量形式。
*   **I 的数据类型遗漏**：文档声明 I 可以是二维数值矩阵或三维数值数组，数据类型列表包含 Float64，但示例中通过 `imread("coins.png")` 加载的图像实际为 `Matrix{RGB{N0f8}}` 或 `Matrix{Gray{N0f8}}` 等颜色类型，而文档和实现仅支持原始数值类型。示例 `imread` 返回的图像可能不是直接支持的数值矩阵，存在笔误或依赖隐式转换的风险。
*   **RolloffFactor 约束描述不完整**：文档推荐范围为 [0.5, 4]，但实现中仅检查正值，未强制该推荐范围。文档未明确说明超出推荐范围的行为。
*   **WeightCutoff 下限描述不精确**：文档写为 [1e-3, 1]，实现中检查 `WeightCutoff < 1e-3` 时报错，这意味着 `1e-3` 是允许的。文档描述与实现一致，但表述可更清晰。

### 参数数据类型与结构覆盖

#### I
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Matrix{UInt8} | 二维 UInt8 矩阵 | test_gradientweight.jl, `@testset "src"` 中 `I = imread("coins.png")` 和 `constantI = fill(UInt8(7), 8, 9)` | ☑ |
| Array{Int16, 3} | 三维 Int16 数组 | test_gradientweight.jl, `@testset "src"` 中 `volume = reshape(Int16.(1:27), 3, 3, 3)` | ☑ |
| Matrix{Float32} | 二维 Float32 矩阵 | test_gradientweight.jl, `@testset "src"` 中 `If32 = Float32.(I) ./ Float32(255)` | ☑ |
| Matrix{Float64} | 二维 Float64 矩阵 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `small2 = reshape(Float64.(1:9), 3, 3)` | ☑ |
| Array{Float64, 3} | 三维 Float64 数组 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `small3 = reshape(Float64.(1:27), 3, 3, 3)` | ☑ |
| Matrix{Int8} | 二维 Int8 矩阵 | test_gradientweight.jl, `@testset "gradientweight 补充"` 中 `for T in (Int8, ...)` | ☑ |
| Matrix{UInt16} | 二维 UInt16 矩阵 | test_gradientweight.jl, `@testset "gradientweight 补充"` 中 `for T in (...UInt16...)` | ☑ |
| Matrix{Int32} | 二维 Int32 矩阵 | test_gradientweight.jl, `@testset "gradientweight 补充"` 中 `for T in (...Int32...)` | ☑ |
| Matrix{UInt32} | 二维 UInt32 矩阵 | test_gradientweight.jl, `@testset "gradientweight 补充"` 中 `for T in (...UInt32)` | ☑ |
| 空数组 | 空数组输入 | 无 | 未覆盖 |

说明：文档声明 I 可以是三维数值数组，但只测试了 `Int16` 和 `Float64` 的三维情况，其余数值类型的三维情况未覆盖。空数组路径在实现中有专门处理（`isempty(I) && return I`），但无测试。

#### sigma
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值（1.5） | 不传入 sigma | test_gradientweight.jl, `@testset "src"` 中 `W_default = gradientweight(I)` | ☑ |
| Float64 标量 | 传入 Float64 标量 | test_gradientweight.jl, `@testset "src"` 中 `W = gradientweight(I, sigma)`，`sigma = 1.5` | ☑ |
| Float32 标量 | 传入 Float32 标量 | test_gradientweight.jl, `@testset "src"` 中 `Wf32 = gradientweight(If32, Float32(1.5))` | ☑ |
| Int 标量 | 传入整数标量 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `Wsigma3 = gradientweight(small3, 1)` | ☑ |
| 二维向量（各向异性） | 传入长度为 2 的向量 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `Wsigma2 = gradientweight(small2, [1, 2])` | ☑ |
| 三维向量（各向异性） | 传入长度为 3 的向量 | test_gradientweight.jl, `@testset "src"` 中 `Wvolume = gradientweight(volume, [1, 1, 1])` | ☑ |
| 非正数值 | sigma ≤ 0 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `@test_throws ErrorException ... sigma ... [-1, 1]` | ☑ |
| 长度不匹配的向量 | 三维图像传入长度为 2 的 sigma 向量 | test_gradientweight.jl, `@testset "validation and sigma coverage"` 中 `@test_throws ErrorException ... sigma ... [1, 1]` | ☑ |

说明：sigma 为 `AbstractArray` 中的非向量形式（如矩阵）未测试，但文档和实现主要针对标量和向量，此项为弱覆盖。`sigma` 为 `Inf` 或 `NaN` 的错误路径未覆盖。

#### RolloffFactor
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值（3） | 不传入 RolloffFactor | test_gradientweight.jl, `@testset "src"` 中 `W = gradientweight(I, sigma; WeightCutoff=0.25)` | ☑ |
| Float64 正标量（2.0） | 传入 Float64 标量 | test_gradientweight.jl, `@testset "gradientweight 补充"` 中 `RolloffFactor=2.0` | ☑ |
| 边界值（如极小正值） | 测试接近 0 的正数 | 无 | 未覆盖 |
| 非正数或非有限值 | 触发参数校验错误 | 无 | 未覆盖 |
| 大值（如 10） | 测试滚降效果 | 无 | 未覆盖 |

说明：文档推荐范围 [0.5, 4]，现有测试只在默认值 3 和 2.0 两个点进行了覆盖，缺少推荐范围边界和范围外的测试。错误路径（`RolloffFactor <= 0` 或 `!isfinite`）无测试。

#### WeightCutoff
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值（0.25） | 不传入 WeightCutoff | test_gradientweight.jl, `@testset "src"` 中 `W = gradientweight(I, sigma; RolloffFactor=3)` | ☑ |
| 边界值 1e-3 | 测试允许的最小值 | 无 | 未覆盖 |
| 边界值 1 | 测试允许的最大值 | 无 | 未覆盖 |
| 范围内其他值 | 如 0.5 | 无 | 未覆盖 |
| 小于 1e-3 的值 | 触发错误 | 无 | 未覆盖 |
| 大于 1 的值 | 触发错误 | 无 | 未覆盖 |
| NaN | 触发错误 | 无 | 未覆盖 |

说明：现有测试只覆盖了默认值 0.25。边界值和错误路径均无测试。

#### 数据结构覆盖总结
| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维数值矩阵 | I 为二维矩阵 | ☑ | 多种数值类型已覆盖 |
| 三维数值数组 | I 为三维数组 | ☑ | 仅 Int16 和 Float64 三维已覆盖 |
| 空数组 | I 为空数组 | 未覆盖 | 实现有专门分支 |
| 常量图像 | 所有像素值相同 | ☑ | `constantI = fill(UInt8(7), 8, 9)` 已测试 |
| sigma 标量 | 单一标准差 | ☑ | 多种数值类型已覆盖 |
| sigma 向量 | 各向异性标准差 | ☑ | 二维和三维向量已覆盖 |
| RolloffFactor 正标量 | 滚降因子 | ☑ | 默认值和 2.0 已覆盖，边界/错误路径缺失 |
| WeightCutoff 范围值 | 权重阈值 | ☑ | 仅默认值覆盖，边界/错误路径缺失 |

### 覆盖缺口与优先级

**高优先级**
*   **WeightCutoff 边界值与错误路径**：文档明确声明范围为 [1e-3, 1]，实现有上下限检查和 NaN 检查，但测试完全未覆盖这些验证逻辑。参数验证是函数契约的核心部分，缺少这些测试会导致回归风险。
*   **RolloffFactor 错误路径**：实现检查了 `RolloffFactor <= 0` 和 `!isfinite`，但这些错误路径无测试。属于基本的参数校验缺失。
*   **空数组输入**：实现为 `isempty(I)` 准备了专门的处理分支（直接返回空数组），但无测试覆盖。边界行为必须验证。

**中优先级**
*   **三维数组的多种数值类型**：文档声明 I 支持 Float32、Int8、UInt8、Int16、UInt16、Int32、UInt32 等类型，且可以是三维数组。现有测试仅覆盖了 Int16 和 Float64 的三维情况，其余六种数值类型的三维输入无测试。
*   **RolloffFactor 推荐范围边界**：文档推荐范围为 [0.5, 4]，现有测试仅在 2.0 和 3 两个内部点，缺少 0.5 和 4 的边界测试，也缺少范围外大值（如 10）的行为测试。
*   **sigma 非有限值错误路径**：实现检查了 `sigma` 必须有限，但 `sigma = Inf` 或 `sigma = NaN` 的异常测试缺失。

**低优先级**
*   **sigma 为非向量数组**：sigma 理论上可以传入矩阵等高维数组，实现中的错误信息是通用错误，非向量情况的行为未文档化也未测试。
*   **输出类型断言为弱证据**：现有测试对输出数组的断言主要依赖 `eltype` 和 `all(isfinite, W)`，缺少基于 baseline 数据的精确值验证（文档示例中的 `BW` 和 `D` 来自 .mat 文件，但未用于精确回归测试）。
*   **WeightCutoff 与 RolloffFactor 交互**：不同参数组合对权重值的综合影响未系统测试。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| I | 二维数值矩阵（UInt8、Float32、Float64、Int8、UInt16、Int32、UInt32）、三维数值数组（Int16、Float64）、常量图像 | 二维数值矩阵（8种数据类型）、三维数值数组（8种数据类型） | 三维数组的 Int8、UInt8、UInt16、Int32、UInt32、Float32 类型显式调用 | 部分覆盖 |
| sigma | Float64 标量、Float32 标量、Int 标量、默认值、二维向量、三维向量、非正数向量错误 | 正数标量、正数向量 | 无 | 完全覆盖 |
| RolloffFactor | 默认值、Float64 正标量（2.0） | 正标量 | 无 | 完全覆盖 |
| WeightCutoff | 默认值 | [1e-3, 1] 范围内的正数 | 无 | 完全覆盖 |

**总计**：完全覆盖参数数 3 个（sigma、RolloffFactor、WeightCutoff），至少有证据参数数 4 个（所有参数均有覆盖证据）。I 参数为部分覆盖（三维数组的 6 种数值类型缺失）。

### 缺失测试代码补全

```julia
@testset "gradientweight 补充" begin
    # 复用原测试中常用的 source_values 模式
    source_values_2d = reshape(collect(1:12), 3, 4)
    source_values_3d = reshape(collect(1:27), 3, 3, 3)

    @testset "I 三维数组缺失数值类型" begin
        for T in (Int8, UInt8, UInt16, Int32, UInt32, Float32)
            I3d = T.(source_values_3d)
            W = gradientweight(I3d, 1.5)
            expected_eltype = T == Float32 ? Float32 : Float64
            @test eltype(W) == expected_eltype
            @test size(W) == size(I3d)
            @test all(isfinite, W)
            @test all((W .>= 1e-3) .& (W .<= 1.0))
        end
    end

    @testset "空数组输入" begin
        I_empty_2d = Float64.(zeros(0, 0))
        W_empty_2d = gradientweight(I_empty_2d)
        @test isempty(W_empty_2d)
        @test size(W_empty_2d) == (0, 0)
        @test eltype(W_empty_2d) == Float64

        I_empty_3d = Float64.(zeros(0, 0, 0))
        W_empty_3d = gradientweight(I_empty_3d)
        @test isempty(W_empty_3d)
        @test size(W_empty_3d) == (0, 0, 0)
        @test eltype(W_empty_3d) == Float64
    end

    @testset "WeightCutoff 边界值与错误路径" begin
        I = Float64.(source_values_2d)

        # 边界值 1e-3
        W_min = gradientweight(I, 1.5; WeightCutoff=1e-3)
        @test eltype(W_min) == Float64
        @test all(isfinite, W_min)
        @test all(W_min .>= 1e-3)

        # 边界值 1.0
        W_max = gradientweight(I, 1.5; WeightCutoff=1.0)
        @test eltype(W_max) == Float64
        @test all(isfinite, W_max)
        @test all(W_max .<= 1.0)

        # 范围内中间值
        W_mid = gradientweight(I, 1.5; WeightCutoff=0.5)
        @test eltype(W_mid) == Float64
        @test all(isfinite, W_mid)
        @test all((W_mid .>= 1e-3) .& (W_mid .<= 1.0))

        # 小于 1e-3 触发错误
        @test_throws ErrorException gradientweight(I, 1.5; WeightCutoff=1e-4)

        # 大于 1 触发错误
        @test_throws ErrorException gradientweight(I, 1.5; WeightCutoff=1.5)

        # NaN 触发错误
        @test_throws ErrorException gradientweight(I, 1.5; WeightCutoff=NaN)
    end

    @testset "RolloffFactor 边界值与错误路径" begin
        I = Float64.(source_values_2d)

        # 推荐范围下边界
        W_low = gradientweight(I, 1.5; RolloffFactor=0.5)
        @test eltype(W_low) == Float64
        @test all(isfinite, W_low)
        @test all((W_low .>= 1e-3) .& (W_low .<= 1.0))

        # 推荐范围上边界
        W_high = gradientweight(I, 1.5; RolloffFactor=4.0)
        @test eltype(W_high) == Float64
        @test all(isfinite, W_high)
        @test all((W_high .>= 1e-3) .& (W_high .<= 1.0))

        # 范围外大值
        W_large = gradientweight(I, 1.5; RolloffFactor=10.0)
        @test eltype(W_large) == Float64
        @test all(isfinite, W_large)
        @test all((W_large .>= 1e-3) .& (W_large .<= 1.0))

        # 非正数触发错误
        @test_throws ErrorException gradientweight(I, 1.5; RolloffFactor=0.0)
        @test_throws ErrorException gradientweight(I, 1.5; RolloffFactor=-1.0)

        # 非有限值触发错误
        @test_throws ErrorException gradientweight(I, 1.5; RolloffFactor=Inf)
        @test_throws ErrorException gradientweight(I, 1.5; RolloffFactor=NaN)
    end

    @testset "sigma 非有限值错误路径" begin
        I = Float64.(source_values_2d)

        @test_throws ErrorException gradientweight(I, Inf)
        @test_throws ErrorException gradientweight(I, NaN)
        @test_throws ErrorException gradientweight(I, [1.0, Inf])
    end
end
```

**静态检验说明**

*   此代码块定义了一个新的顶层 `@testset "gradientweight 补充"`，与源文件中已有的同名测试块不会冲突，因为 Julia 允许同一作用域内存在多个同名 `@testset`，它们按顺序执行且互不干扰。
*   代码块内所有变量（如 `source_values_2d`、`source_values_3d`、`I`）均在块内局部构造，未引用原测试块内的局部变量。复用的 `gradientweight` 函数和 `ErrorException` 类型来自顶层导入，作用域自足。
*   整体追加到 `test_gradientweight.jl` 文件末尾即可，无需修改任何原有代码。

**仍需 baseline 校准项**

*   所有新增测试均使用 `all(isfinite, W)` 和 `all((W .>= 1e-3) .& (W .<= 1.0))` 作为性质断言，未进行精确值验证。这是因为 `gradientweight` 的数值输出依赖于高斯导数核的卷积计算，缺少独立计算的 baseline 真值。如需精确回归测试，建议使用 MATLAB 参考实现或已保存的 `.mat` 基准数据（如文档示例中的 `gradientweight_exp1_BW.mat` 和 `gradientweight_exp1_D.mat` 对应的中间权重数组）进行逐元素比较。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
gradientweight，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_gradientweight 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_gradientweight.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\gradientweight.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ImageSegmentation\SegmentationTechniques\gradientweight.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
gradientweight，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：gradientweight
遗留缺陷：无
```
