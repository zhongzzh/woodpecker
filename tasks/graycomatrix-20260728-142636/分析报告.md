# graycomatrix 提测分析报告

> 任务：graycomatrix 函数（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_graycomatrix.jl）
> 任务类型：本地材料分析
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\TextureAnalysis\graycomatrix.md`
> 单测：`test/im_math/test_graycomatrix.jl`（伴随数据 1 个）
> benchmark：未定位到
> 分析规则：提示词 v1（本地文档/代码覆盖分析）＋用户粘贴性能报告分析 ｜ 生成：2026-07-28 14:28 ｜ 模型：grok-4.5

## 一、单元测试覆盖分析（AI）

### 📌 文档定义的输入参数

| 参数 | 允许的形式 | 允许的数据类型 | 简要说明 |
|------|------------|----------------|----------|
| `I` | 二维数值矩阵 \| 二维逻辑矩阵 | `Float64` \| `UInt8` \| `UInt16` \| `Int16` \| `Bool` | 输入灰度/逻辑图像（位置参数，可写） |
| `GrayLimits` | 二元向量 `[low high]` \| `[]` | 未单独罗列元素类型（与数值范围相关） | 将输入缩放到灰度级所用的范围 |
| `NumLevels` | 正整数（标量） | 正整数 | 灰度级数，决定 GLCM 尺寸 |
| `Offset` | 默认 `[0 1]` \| 由整数组成的 `p×2` 矩阵 | 整数 | 感兴趣像素与邻点的行/列偏移 |
| `Symmetric` | `false`（默认） \| `true` | `Bool` | 是否按无对称方式计数像素对 |

说明：`glcm`、`SI` 为输出（只读），不纳入可写输入参数覆盖分析。

---

### 🔍 文档自身缺陷审计报告

1. **`I` 的数据类型列表不完整/不一致**：文档写明 `Float64 | UInt8 | UInt16 | Int16 | Bool`，但示例与常见用法大量出现 `Int64` 字面量矩阵（如 `[1 1 5 ...]`）；实现签名为 `AbstractMatrix{<:Real}`，实际可接受类型宽于文档列表，文档未体现。
2. **示例笔误**：第三个示例中 `imshow（I）` 使用了中文全角括号 `（）`，应为 `imshow(I)`。
3. **`GrayLimits` 类型说明偏弱**：形式写了「二元向量 | `[]`」，未像 `I`/`Symmetric` 那样给出明确元素数据类型；正文又用 “double / int16 的默认范围” 举例，与 Julia 侧 `Float64`/`Int16` 表述混用，易产生歧义。
4. **`NumLevels` 约束前后需对照实现**：文档称“正整数”，实现还允许并校验 `nl < 0` 报错，且对 `Bool` 图像强制 `NumLevels==2`；文档在 `NumLevels` 条目本身未写清二值图约束（约束写在算法/解析逻辑中，条目不完整）。

---

### ✅ 测试覆盖情况整体总览

| 参数 | 文档要求的形式/类型 | 测试代码原文摘录（关键行复制） | 覆盖程度 |
|------|---------------------|----------------------------------|----------|
| `I` | 二维数值矩阵 \| 二维逻辑矩阵；`Float64` \| `UInt8` \| `UInt16` \| `Int16` \| `Bool` | `I = imread("circuit.tif")` | 部分覆盖 |
| | | `I = [1.0 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]` | |
| | | `I = imread("cell.tif")` | |
| | | `I = imread("circuit.tif")` | |
| `GrayLimits` | 二元向量 `[low high]` \| `[]` | `glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])` | 部分覆盖 |
| `NumLevels` | 正整数 | `glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])` | 完全覆盖 |
| `Offset` | 默认 `[0 1]` \| `p×2` 整数矩阵 | `glcm, SI = graycomatrix(I; Offset=[2 0])` | 完全覆盖 |
| | | `offsets = [0 1; -1 1; -1 0; -1 -1]` | |
| | | `glcm, SI = graycomatrix(I; Offset=offsets)` | |
| | | `glcm, SI = graycomatrix(I; Offset=[2 0], Symmetric=true)` | |
| `Symmetric` | `false` \| `true`（`Bool`） | `glcm, SI = graycomatrix(I; Offset=[2 0], Symmetric=true)` | 完全覆盖 |

补充判定说明：

- `I`：仅明确覆盖了 `Float64` 矩阵，以及 `imread` 得到的图像（类型未在测试中断言；通常为 `UInt8`，但不能视为对 `UInt8`/`UInt16`/`Int16`/`Bool` 的显式覆盖）；**二维逻辑矩阵未出现**。
- `GrayLimits`：只测了 `[]`（`Float64[]`），**未测非空二元向量** `[low high]`。
- `Symmetric`：显式测了 `true`；`false` 为未传参时的默认路径，按“非默认值已设置”达到完全覆盖。
- `Offset`：单行与 `p×2` 多偏移均有显式赋值，形式覆盖完整。

---

### ❌ 缺失覆盖的细颗粒度差异清单

1. **完全未覆盖的参数**  
   - 无（五个可写输入均在测试中出现或作为默认路径被行使）。

2. **结构形式缺失**  
   - `I`：缺失 **二维逻辑矩阵**（`Bool` 矩阵）形式。  
   - `GrayLimits`：缺失非空 **二元向量** `[low high]` 形式（仅有 `[]`）。

3. **数据类型缺失**  
   - `I`：缺失显式的 `UInt8`、`UInt16`、`Int16`、`Bool` 赋值与断言（仅有 `Float64` 字面量矩阵 + 未断言类型的 `imread` 结果）。  
   - `GrayLimits`：非空二元向量未用任何数值类型做过显式设置（如 `Float64`/`Int` 的 `[low high]`）。

---

### 📋 差异化度量总结

| 覆盖程度 | 参数个数 | 参数名 |
|----------|----------|--------|
| 完全覆盖 | 3 | `NumLevels`, `Offset`, `Symmetric` |
| 部分覆盖 | 2 | `I`, `GrayLimits` |
| 未覆盖 | 0 | — |

- 整体覆盖率（按参数计）：**完全覆盖 3/5 = 60%**；若将部分覆盖按 0.5 计，则约 **80%**。  
- 主要风险集中在 **`I` 的多类型/逻辑图** 与 **`GrayLimits` 非空二元向量** 两条主路径，与文档公开支持面差距明显。

---

### 🛠️ 缺失测试代码自动补全（核心模块）

```julia
using TyImageProcessing, Test

@testset "graycomatrix" begin
    @testset "src" begin
        I = imread("circuit.tif")
        glcm, SI = graycomatrix(I; Offset=[2 0])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        I = [1.0 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]
        glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        I = imread("cell.tif")
        offsets = [0 1; -1 1; -1 0; -1 -1]
        glcm, SI = graycomatrix(I; Offset=offsets)
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        I = imread("circuit.tif")
        glcm, SI = graycomatrix(I; Offset=[2 0], Symmetric=true)
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # I: UInt8 二维数值矩阵
        I = UInt8[1 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]
        glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # I: UInt16 二维数值矩阵
        I = UInt16[1 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]
        glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # I: Int16 二维数值矩阵
        I = Int16[1 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]
        glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=Float64[])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # I: Bool 二维逻辑矩阵（NumLevels 固定为 2）
        I = Bool[1 1 0 0; 0 1 1 0; 1 0 1 1]
        glcm, SI = graycomatrix(I)
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}
        glcm, SI = graycomatrix(I; NumLevels=2, Offset=[0 1], Symmetric=false)
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # GrayLimits: 非空二元向量 [low high]
        I = [1.0 1 5 6 8 8; 2 3 5 7 0 2; 0 2 3 5 6 7]
        glcm, SI = graycomatrix(I; NumLevels=9, GrayLimits=[0.0 8.0])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}
        glcm, SI = graycomatrix(I; NumLevels=8, GrayLimits=[1 8], Offset=[2 0])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        # GrayLimits 二元向量 + 多类型 I 组合
        I = UInt8[10 20 30 40; 20 30 40 50; 30 40 50 60]
        glcm, SI = graycomatrix(I; NumLevels=4, GrayLimits=[10 60], Offset=[0 1], Symmetric=true)
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}

        I = Int16[10 20 30 40; 20 30 40 50; 30 40 50 60]
        glcm, SI = graycomatrix(I; NumLevels=4, GrayLimits=[10 60], Offset=[-1 0])
        @test typeof(glcm) <: AbstractArray{Float64} && typeof(SI) <: AbstractArray{Float64}
    end
end
```

## 二、性能测试判定（脚本演算）

### 用户粘贴的性能报告分析

#### 数据完整性
报告共包含 5 个函数/用法的性能数据：`imcontour`、`bwdistgeodesic`、`chromadapt`、`jaccard`、`graycomatrix`。

| 函数 | julia 首次 | julia 二次 | MATLAB 首次 | MATLAB 二次 | 首次比例 | 二次比例 | 完整性说明 |
|------|------------|------------|-------------|-------------|---------|---------|-----------|
| imcontour | 有 | 有 | 有 | 有 | 缺失 | 缺失 | 四项耗时齐全，可由耗时推导比例 |
| bwdistgeodesic | 有 | 有 | 有 | 有 | 有 | 有 | 完整 |
| chromadapt | 有 | 有 | 有 | 有 | 有 | 有 | 完整 |
| jaccard | 有 | 有 | 有 | 有 | 有（与耗时不一致） | 缺失 | 四项耗时齐全；首次比例 0.000556 与两侧首次耗时均 1.5880196 矛盾 |
| graycomatrix | 有 | 有 | 有 | 有 | 有 | 有 | 完整 |

对比口径：报告明确为 syslab/matlab（Julia / MATLAB）比例，基准耗时 T 取 MATLAB 用时，比值 x 取 Julia/MATLAB。

#### 可判定项

**1. imcontour**
- 首次：T = 0.305585 s（0.1 ≤ T ≤ 1），x = 0.593346 / 0.305585 ≈ 1.942 > 1.25 → **不通过**
- 二次：T = 0.062642 s（T < 0.1），x = 3.94E-02 / 0.062642 ≈ 0.629 ≤ 1.5 → **通过**

**2. bwdistgeodesic**
- 首次：T = 0.102425 s（0.1 ≤ T ≤ 1），x = 14.893226 > 1.25 → **不通过**
- 二次：T = 0.001689 s（T < 0.1），x = 0.079337 ≤ 1.5 → **通过**

**3. chromadapt**
- 首次：T = 1.529016 s（T > 1），x = 2.275163 > 1.2 → **不通过**
- 二次：T = 1.331454 s（T > 1），x = 2.1330132 > 1.2 → **不通过**

**4. jaccard**
- 首次：T = 1.5880196 s（T > 1）；按两侧首次耗时计算 x = 1.5880196 / 1.5880196 = 1.0 ≤ 1.2 → **通过**  
  （报告填写的首次比例 0.000556 与耗时字段矛盾，判定以耗时字段为准）
- 二次：T = 0.004363 s（T < 0.1），x = 8.25E-05 / 0.004363 ≈ 0.0189 ≤ 1.5 → **通过**

**5. graycomatrix**
- 首次：T = 0.114132 s（0.1 ≤ T ≤ 1），x = 30.234102 > 1.25 → **不通过**
- 二次：T = 0.006451 s（T < 0.1），x = 0.004371 ≤ 1.5 → **通过**

#### 无法判定项
- 无整项无法判定的函数。  
- 局部问题：`imcontour` 未给出比例字段（已由耗时补算并判定）；`jaccard` 首次比例与耗时矛盾、二次比例缺失（已由耗时补算并判定）。

#### 综合结论
在 Julia/MATLAB 口径下，对报告内全部 5 个函数的首次、二次用时均作了判定：

| 函数 | 首次 | 二次 |
|------|------|------|
| imcontour | 不通过 | 通过 |
| bwdistgeodesic | 不通过 | 通过 |
| chromadapt | 不通过 | 不通过 |
| jaccard | 通过 | 通过 |
| graycomatrix | 不通过 | 通过 |

- 二次用时（更反映稳态性能）仅 `chromadapt` 不通过，其余 4 项二次均通过。  
- 首次用时仅 `jaccard` 通过，其余 4 项首次不通过（常见于含编译/初始化开销）。  
- `jaccard` 报告中的首次比例与原始耗时不一致，建议核对原始录入；当前结论以耗时字段为准。

## 三、总结邮件格式（可直接复制）

```text
graycomatrix 函数，功能验证通过，已分析用户粘贴的性能报告信息，请补充自动化脚本
分析材料来自用户电脑本地文件：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_graycomatrix.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\graycomatrix.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\TextureAnalysis\graycomatrix.md

graycomatrix：
```
