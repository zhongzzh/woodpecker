# illumgray 提测分析报告

> 任务：illumgray（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumgray）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumgray.md`
> 单测：`test/im_math/test_illumgray.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumgray.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-17 09:42 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像，三维且第三维必须为 3 |
| percentile | 1（默认）、数值标量、2 元数值向量 | Float32、Float64、UInt8、UInt16 | 排除底部/顶部百分比；标量范围 [0,50]，向量各元素 [0,100) 且 sum≤100 |
| Mask | m×n 逻辑或数值数组 | Float32、Float64、UInt8、UInt16 | 掩膜，值为 0 的像素被排除；默认全 1 |
| Norm | 1（默认）、正数值标量 | Float32、Float64、UInt8、UInt16 | p 范数指数，影响平均 RGB 值计算 |

### 文档自身缺陷审计

- `percentile` 文档数据类型只列 `Float32、Float64、UInt8、UInt16`，但示例使用 `percentiles = 10`，现有测试也使用 Int64 标量；文档未把 Int64 列为允许类型。
- `percentile` 文档形式只写“2 元数值向量”，但实现签名允许 `AbstractVecOrMat`，现有测试使用 `[0 0]` 实际是 1×2 Matrix，文档未说明矩阵形式。
- `Mask` 文档允许形式包含“逻辑数组”，但数据类型列表未包含 Bool，只列了 `Float32、Float64、UInt8、UInt16`。
- `Norm` 文档数据类型同样遗漏 Int64，而现有测试使用 `Norm=2` 且实现接受任意 `Real`。
- 语法描述 `illumgray(___; Name=Value)` 中的 `___` 是模糊占位符，未明确为 `A` 或 `A, percentile`。
- 示例标题 `percentiles=[$(percentiles) $(percentiles)]` 中 `percentiles` 实际是标量 10，标题却写成向量，易造成误导。
- 文档未明确 `A` 为空、`A` 第三维非 3、掩膜全零等情况下的行为；实现会报错，但输入约束描述不完整。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n×3 三维数组结构 | 4×4×3 数组 | test_illumgray.jl / synthetic usage / `A = cat(...)` | ☑ |
| Float64 元素 | 常量 Float64 数组 | test_illumgray.jl / synthetic usage / `illumgray(A,0)` | ☑ |
| UInt8 元素 | `B = fill(UInt8(128),4,4,3)` | test_illumgray.jl / synthetic usage / `illumgray(B,0)` | ☑ |
| 真实图像输入 | `A_lin = rgb2lin(A)`，元素类型未断言 | test_illumgray.jl / case image / `illuminant = illumgray(A_lin, percentiles)` | ☑ |
| Float32 元素 | 未找到 | 无 | 未覆盖 |
| UInt16 元素 | 未找到 | 无 | 未覆盖 |

说明：真实图像路径只证明 `illumgray` 可对图像处理结果调用，且输出形状和元素类型正确；由于没有对 `A_lin` 做元素类型断言，不能作为 Float32、Float64 等具体类型的覆盖证据。Float32、UInt16 仍缺失。

#### percentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量，合法正数 | 使用 Int64 值 0 和 10 | test_illumgray.jl / case image / `percentiles=10`；synthetic usage / `illumgray(A,0)` | ☑ |
| 默认值 1，默认路径 | 未显式传入 percentile 的调用 | test_illumgray.jl / synthetic usage / `illumgray(A; Mask=mask, Norm=2)` | ☑ |
| 2 元数值向量 | 未找到；现有 `[0 0]` 是 1×2 Matrix，不是 Vector | 无 | 未覆盖 |
| Float32 百分位数 | 未找到合法正常调用 | 无 | 未覆盖 |
| Float64 百分位数 | 仅有 `NaN` 非法路径，无合法正常调用 | test_illumgray.jl / validation / `illumgray(A,NaN)` | 未覆盖 |
| UInt8 百分位数 | 未找到 | 无 | 未覆盖 |
| UInt16 百分位数 | 未找到 | 无 | 未覆盖 |
| 非法百分位数 | NaN、负数、100、`[60 50]` 等异常路径 | test_illumgray.jl / validation | ☑ |

说明：合法标量覆盖只使用了 Int64，而 Int64 未列入文档允许类型；2 元向量结构没有覆盖，现有 `[0 0]` 矩阵证据不能替代。

#### Mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n 逻辑数组 | `mask = trues(4,4)` 并修改前 2×2 为 false | test_illumgray.jl / synthetic usage / `illumgray(A; Mask=mask, Norm=2)` | ☑ |
| 默认全 1 掩膜 | 未显式传入 Mask 的调用 | test_illumgray.jl / synthetic usage / `illumgray(A,0)` 等 | ☑ |
| 数值数组掩膜 | 未找到正常数值掩膜 | 无 | 未覆盖 |
| Float32 掩膜 | 未找到 | 无 | 未覆盖 |
| Float64 掩膜 | 未找到 | 无 | 未覆盖 |
| UInt8 掩膜 | 未找到 | 无 | 未覆盖 |
| UInt16 掩膜 | 未找到 | 无 | 未覆盖 |
| 非法 Mask | 全零、尺寸不匹配 | test_illumgray.jl / validation / `Mask=falses(4,4)`、`Mask=trues(4,5)` | ☑ |

说明：逻辑掩膜为强证据；文档要求的数值掩膜正常路径全部缺失。

#### Norm

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 正数值标量非默认值 | 使用 Int64 值 2 | test_illumgray.jl / synthetic usage / `illumgray(A; Mask=mask, Norm=2)` | ☑ |
| 默认值 1，默认路径 | 未显式传入 Norm 的调用 | test_illumgray.jl / synthetic usage / `illumgray(A,0)` 等 | ☑ |
| Float32 正数 | 未找到 | 无 | 未覆盖 |
| Float64 正数 | 未找到 | 无 | 未覆盖 |
| UInt8 正数 | 未找到 | 无 | 未覆盖 |
| UInt16 正数 | 未找到 | 无 | 未覆盖 |
| 非法 Norm | `Norm=0` | test_illumgray.jl / validation | ☑ |

说明：`Norm=2` 是 Int64 非默认值，但 Int64 未列入文档类型；文档要求的 Float32、Float64、UInt8、UInt16 正数均缺失。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| Float64 三维数组 | A | ☑ | 强证据 |
| UInt8 三维数组 | A | ☑ | 强证据 |
| Float32 三维数组 | A | 未覆盖 | 缺失 |
| UInt16 三维数组 | A | 未覆盖 | 缺失 |
| 真实图像输入 | A_lin 元素类型未断言 | ☑ | 弱证据，不替代具体类型覆盖 |
| 标量 percentile | Int64 0、10 | ☑ | 弱证据，文档类型未含 Int64 |
| 2 元向量 percentile | 未使用 Vector | 未覆盖 | 现有 `[0 0]` 是 1×2 Matrix |
| 逻辑 Mask | BitMatrix | ☑ | 强证据 |
| 数值 Mask | 无正常数值掩膜 | 未覆盖 | 文档四种数值类型均缺 |
| 默认 Mask | 未显式传入 | ☑ | 弱证据 |
| 正 Norm 标量 | Int64 2 | ☑ | 弱证据，文档类型未含 Int64 |
| 默认 Norm | 未显式传入 | ☑ | 弱证据 |
| 非法输入边界 | 异常路径 | ☑ | 覆盖 NaN、负数、100、全零 Mask、尺寸错误、Norm=0 |

### 覆盖缺口与优先级

**高优先级**

- `A` 的 Float32、UInt16 合法三维数组没有任何覆盖。
- `percentile` 的 2 元数值向量结构未覆盖；Float32、Float64、UInt8、UInt16 合法百分位数无正常路径覆盖。
- `Mask` 的数值数组正常路径未覆盖；Float32、Float64、UInt8、UInt16 掩膜均缺失。
- `Norm` 的 Float32、Float64、UInt8、UInt16 正数均未覆盖，只有 Int64 非默认值。

**中优先级**

- 默认参数路径只有弱证据：默认 `percentile=1` 和 `Norm=1` 未在非平凡图像上做显式数值断言。
- 真实图像测试仅断言输出形状和元素类型，未断言光源数值，不能验证算法结果。
- 合法边界值未测：`percentile` 标量 0、50，`percentile` 向量 `[0,0]`，`Norm` 为 3、Inf 等正数。

**低优先级**

- `A` 为空、`A` 非三维、`A` 第三维非 3 等结构异常未测试。
- `percentile` 标量略大于 50 的边界，如 50.1、60，未测试。
- 非默认 Mask 与 percentile 组合路径未测试。
- 数值 Mask 转为 Bool 后的效果未单独验证。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | m×n×3 结构、Float64、UInt8 | m×n×3 结构、Float32、Float64、UInt8、UInt16 | Float32、UInt16 | 部分覆盖 |
| percentile | 数值标量、默认 1 | 默认 1、数值标量、2 元数值向量、Float32、Float64、UInt8、UInt16 | 2 元数值向量、Float32、Float64、UInt8、UInt16 | 部分覆盖 |
| Mask | 逻辑数组、默认全 1 | 逻辑或数值数组、Float32、Float64、UInt8、UInt16 | 数值数组、Float32、Float64、UInt8、UInt16 | 部分覆盖 |
| Norm | 正数标量、默认 1 | 默认 1、正数值标量、Float32、Float64、UInt8、UInt16 | Float32、Float64、UInt8、UInt16 | 部分覆盖 |

总计：完全覆盖参数数 0；至少有证据参数数 4，即 `A`、`percentile`、`Mask`、`Norm` 均有部分覆盖证据。

### 缺失测试代码补全

```julia
@testset "illumgray 补充" begin
    A32 = cat(fill(Float32(0.1), 4, 4),
              fill(Float32(0.2), 4, 4),
              fill(Float32(0.4), 4, 4); dims=3)
    @test isapprox(illumgray(A32, 0), [0.1 0.2 0.4]; atol=1e-6)

    A16 = fill(UInt16(32768), 4, 4, 3)
    @test isapprox(illumgray(A16, 0), fill(32768 / 65535, 1, 3); atol=1e-12)

    A = cat(fill(0.1, 4, 4),
            fill(0.2, 4, 4),
            fill(0.4, 4, 4); dims=3)

    @test illumgray(A, [0.0, 0.0]) ≈ [0.1 0.2 0.4]
    @test illumgray(A, 10.0) ≈ [0.1 0.2 0.4]
    @test illumgray(A, UInt8(10)) ≈ [0.1 0.2 0.4]

    mask_u8 = ones(UInt8, 4, 4)
    mask_u8[1:2, 1:2] .= 0
    @test isapprox(illumgray(A; Mask=mask_u8, Norm=2),
                   [0.1 0.2 0.4] ./ sqrt(12); atol=1e-12)

    mask_f64 = ones(4, 4)
    mask_f64[1:2, 1:2] .= 0
    @test illumgray(A; Mask=mask_f64) ≈ [0.1 0.2 0.4]

    mask = trues(4, 4)
    mask[1:2, 1:2] .= false
    @test isapprox(illumgray(A; Mask=mask, Norm=2.0),
                   illumgray(A; Mask=mask, Norm=2); atol=0)
    @test isapprox(illumgray(A; Mask=mask, Norm=UInt8(2)),
                   illumgray(A; Mask=mask, Norm=2); atol=0)

    @test_throws Exception illumgray(rand(0, 0, 3), 0)
    @test_throws Exception illumgray(rand(4, 4, 2), 0)
end
```

静态检验说明：该代码块只包含新增用例，所有数据在块内重建，未引用原文件任何测试块内部的局部变量；它作为一个独立顶层 `@testset`，整体追加到 `test_illumgray.jl` 文件末尾即可，不插入、替换或改写原文件内容。

仍需 baseline 校准项：真实图像 `foosball.jpg` 经 `rgb2lin` 后的具体光源数值；非平凡图像上 percentile 与 Mask 组合的预期光源值；新增测试仅使用常量图验证类型、结构和可计算性质，不验证复杂图像统计正确性。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumgray，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumgray 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumgray.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumgray.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumgray，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumgray
遗留缺陷：无
```
