# illumgray 提测分析报告

> 任务：illumgray（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumgray.md`
> 单测：`test/im_math/test_illumgray.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumgray.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 09:01 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像；必须为三维且第三维为3 |
| percentile | 1(默认)、数值标量、2元数值向量 | Float32、Float64、UInt8、UInt16 | 排除底部和顶部像素百分比；标量范围[0,50]；向量各元素[0,100)且和≤100 |
| Mask | m×n 逻辑或数值数组 | Float32、Float64、UInt8、UInt16 | 默认全1；掩膜为0的像素不参与估计 |
| Norm | 1(默认)、正数值标量 | Float32、Float64、UInt8、UInt16 | p 范数指数，影响平均 RGB 值计算 |

### 文档自身缺陷审计

- `Mask` 允许形式写为“逻辑或数值数组”，但数据类型列表未列出 `Bool`；文档类型表应补充逻辑数组的 `Bool` 类型。
- `Norm` 实现中显式存在 `isinf(exponent)` 分支，即代码支持 `Inf`，但文档只写“正数值标量”且类型列表未说明 `Inf` 是否允许。
- 示例给出的 `illuminant` 精确输出依赖外部图像文件 `foosball.jpg`，文档未标注该文件版本或内容哈希，复现性偏弱。
- `Norm` 的 p 范数定义写为 `sum(abs(x)^p) ^ (1/p)`，但实现实际还在通道内按选定像素数进行平均，文档对“平均 RGB 值的计算”与 p 范数的关系说明不够完整。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 三维数组 | `cat(fill(0.1,4,4),...)` 构造后传入 `illumgray(A,0)` 并断言 | test_illumgray.jl / `@testset "illumgray"` / synthetic usage | ☑ |
| UInt8 三维数组 | `fill(UInt8(128),4,4,3)` 构造后传入并断言 | test_illumgray.jl / `@testset "illumgray"` / synthetic usage | ☑ |
| Float32 三维数组 | `A32` 使用 Float32 构造并传入、断言 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| UInt16 三维数组 | `A16` 使用 UInt16 构造并传入、断言 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| 空数组或非三维结构 | `rand(0,0,3)`、`rand(4,4,2)` 触发异常 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |

#### percentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1，隐式传入 | 多处调用未显式传 `percentile`，如 `illumgray(A; Mask=mask, Norm=2)` | test_illumgray.jl / synthetic usage、补充块 | ☑ |
| 数值标量 Float64 | `illumgray(A, 10.0)` | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| 数值标量 UInt8 | `illumgray(A, UInt8(10))` | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| 数值标量 Float32 | 未找到构造并传入 `Float32` 标量的测试 | 无 | 未覆盖 |
| 数值标量 UInt16 | 未找到构造并传入 `UInt16` 标量的测试 | 无 | 未覆盖 |
| 2元数值向量 Float64 | `illumgray(A, [0.0, 0.0])` | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| 2元数值向量 Float32 | 未找到 `Float32` 向量测试 | 无 | 未覆盖 |
| 2元数值向量 UInt8 | 未找到 `UInt8` 向量测试 | 无 | 未覆盖 |
| 2元数值向量 UInt16 | 未找到 `UInt16` 向量测试 | 无 | 未覆盖 |
| 异常或边界 | `NaN`、`-1`、`100`、`[60 50]` 均触发异常 | test_illumgray.jl / validation | ☑ |

说明：默认值 1 仅通过未显式传入 `percentile` 的调用触发，且现有断言使用均匀图像，无法证明排除百分位的实际影响，属于弱覆盖。`10`、`0`、`[0 0]`、`[60 50]` 等为 `Int64`，不在文档声明的数据类型列表中，因此不能替代 Float32、UInt16 等文档类型的覆盖。

#### Mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 逻辑数组 Bool | `mask = trues(4,4)` 后传入并断言 | test_illumgray.jl / synthetic usage、补充块 | ☑ |
| UInt8 数值数组 | `mask_u8 = ones(UInt8,4,4)` 后传入并断言 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| Float64 数值数组 | `mask_f64 = ones(4,4)` 后传入并断言 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| Float32 数值数组 | `mask_f32 = ones(Float32,4,4)` 后传入并断言 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| UInt16 数值数组 | 未找到 `UInt16` 掩膜测试 | 无 | 未覆盖 |
| 默认 Mask，隐式全1 | 未显式传 `Mask` 的调用，如 `illumgray(A,0)` | test_illumgray.jl / synthetic usage | ☑ |
| 尺寸不匹配异常 | `Mask=trues(4,5)` 触发异常 | test_illumgray.jl / validation | ☑ |
| 全零 Mask 异常 | `Mask=falses(4,4)` 触发异常 | test_illumgray.jl / validation | ☑ |

说明：默认 `Mask` 仅由未传入 `Mask` 的调用触发，属于弱覆盖。逻辑数组测试已覆盖“逻辑数组”形式，但文档类型表缺失 `Bool`，应修正文档。

#### Norm

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1，隐式传入 | 多处调用未显式传 `Norm`，如 `illumgray(A,0)` | test_illumgray.jl / synthetic usage | ☑ |
| Float64 正标量 | `Norm=2.0` 与 `Norm=2` 比较 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| UInt8 正标量 | `Norm=UInt8(2)` 与 `Norm=2` 比较 | test_illumgray.jl / `@testset "illumgray 补充"` | ☑ |
| Float32 正标量 | 未找到 `Float32` 类型的 `Norm` 测试 | 无 | 未覆盖 |
| UInt16 正标量 | 未找到 `UInt16` 类型的 `Norm` 测试 | 无 | 未覆盖 |
| 非正异常 | `Norm=0` 触发异常 | test_illumgray.jl / validation | ☑ |

说明：`Norm=2` 为 `Int64`，不在文档声明的数据类型列表中，不能替代 Float32、UInt16 文档类型覆盖。默认 `Norm=1` 仅由未显式传 `Norm` 的调用触发，属于弱覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| Float64 三维 RGB 图像 | 主要数值类型 | ☑ | 强证据 |
| UInt8 三维 RGB 图像 | 原始字节类型 | ☑ | 强证据 |
| Float32 三维 RGB 图像 | 单精度浮点类型 | ☑ | 强证据 |
| UInt16 三维 RGB 图像 | 16 位整型类型 | ☑ | 强证据 |
| percentile Float64 标量 | 浮点百分比标量 | ☑ | 强证据 |
| percentile UInt8 标量 | 8 位整型百分比标量 | ☑ | 强证据 |
| percentile Float32 标量 | 单精度百分比标量 | 未覆盖 | 缺测试证据 |
| percentile UInt16 标量 | 16 位整型百分比标量 | 未覆盖 | 缺测试证据 |
| percentile Float64 向量 | 双精度二元向量 | ☑ | 强证据 |
| percentile Float32 向量 | 单精度二元向量 | 未覆盖 | 缺测试证据 |
| percentile UInt8 向量 | 8 位整型二元向量 | 未覆盖 | 缺测试证据 |
| percentile UInt16 向量 | 16 位整型二元向量 | 未覆盖 | 缺测试证据 |
| Mask Bool 逻辑数组 | 二值掩膜 | ☑ | 文档类型表漏 Bool |
| Mask UInt8 数值数组 | 8 位整型掩膜 | ☑ | 强证据 |
| Mask Float64 数值数组 | 双精度浮点掩膜 | ☑ | 强证据 |
| Mask Float32 数值数组 | 单精度浮点掩膜 | ☑ | 强证据 |
| Mask UInt16 数值数组 | 16 位整型掩膜 | 未覆盖 | 缺测试证据 |
| Norm Float64 正标量 | 双精度范数指数 | ☑ | 强证据 |
| Norm UInt8 正标量 | 8 位整型范数指数 | ☑ | 强证据 |
| Norm Float32 正标量 | 单精度范数指数 | 未覆盖 | 缺测试证据 |
| Norm UInt16 正标量 | 16 位整型范数指数 | 未覆盖 | 缺测试证据 |

### 覆盖缺口与优先级

#### 高优先级

- `percentile` 缺失 `Float32` 标量、`UInt16` 标量、`Float32` 向量、`UInt8` 向量、`UInt16` 向量：这些均为文档明确列出的数据类型；现有 `Int64` 标量和向量不能替代文档要求类型。
- `Mask` 缺失 `UInt16` 数值数组：文档明确允许 `UInt16` 掩膜，当前无构造、传入和断言证据。
- `Norm` 缺失 `Float32`、`UInt16` 正标量：文档明确列出这两种数据类型，当前仅有 `Float64` 和 `UInt8` 证据。

#### 中优先级

- 默认参数 `percentile=1`、`Mask` 全1、`Norm=1` 仅有隐式默认路径，未用非均匀数据显式验证默认值行为；建议补强以区分默认与非默认值的实际影响。
- 文档 `Mask` 类型表缺失 `Bool`，但实现和测试均支持逻辑掩膜；应修正文档。
- 实现支持 `Norm=Inf`，文档未说明；应明确该边界行为是允许还是应拒绝。

#### 低优先级

- 百分位边界值 50、向量和等于 100 的边界行为未测试。
- `Mask` 数值数组含 `NaN` 的异常路径未测试。
- `A` 的文档未声明整型等实现会拒绝的类型，当前也没有覆盖这些非法类型的异常断言。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | Float64、UInt8、Float32、UInt16 三维数组 | Float64、UInt8、Float32、UInt16 三维数组 | 无 | 完全覆盖 |
| percentile | 默认1、Float64标量、UInt8标量、Float64向量 | 默认1、Float32标量、Float64标量、UInt8标量、UInt16标量、Float32向量、Float64向量、UInt8向量、UInt16向量 | Float32标量、UInt16标量、Float32向量、UInt8向量、UInt16向量 | 部分覆盖 |
| Mask | Bool逻辑数组、UInt8数值数组、Float64数值数组、Float32数值数组、默认全1 | Bool逻辑数组、UInt8数值数组、Float64数值数组、Float32数值数组、UInt16数值数组、默认全1 | UInt16数值数组 | 部分覆盖 |
| Norm | 默认1、Float64正标量、UInt8正标量 | 默认1、Float32正标量、Float64正标量、UInt8正标量、UInt16正标量 | Float32正标量、UInt16正标量 | 部分覆盖 |

总计：完全覆盖参数数为 1 个；至少有证据参数数为 4 个。缺失项主要集中在 `percentile`、`Mask`、`Norm` 的 `Float32` 和 `UInt16` 文档类型上。

### 缺失测试代码补全

```julia
@testset "illumgray 补充" begin
    A = cat(fill(0.1, 4, 4), fill(0.2, 4, 4), fill(0.4, 4, 4); dims=3)

    @test illumgray(A, Float32(10)) ≈ [0.1 0.2 0.4]
    @test illumgray(A, UInt16(10)) ≈ [0.1 0.2 0.4]

    @test illumgray(A, Float32[10, 10]) ≈ [0.1 0.2 0.4]
    @test illumgray(A, UInt8[10, 10]) ≈ [0.1 0.2 0.4]
    @test illumgray(A, UInt16[10, 10]) ≈ [0.1 0.2 0.4]

    mask_u16 = ones(UInt16, 4, 4)
    mask_u16[1:2, 1:2] .= 0
    @test illumgray(A; Mask=mask_u16) ≈ [0.1 0.2 0.4]

    mask = trues(4, 4)
    @test isapprox(
        illumgray(A; Mask=mask, Norm=Float32(2)),
        [0.1 0.2 0.4] ./ sqrt(16);
        atol=1e-12,
    )
    @test isapprox(
        illumgray(A; Mask=mask, Norm=UInt16(2)),
        [0.1 0.2 0.4] ./ sqrt(16);
        atol=1e-12,
    )
end
```

**静态检验说明：** 该代码块为独立顶层 `@testset`，整体追加到 `test_illumgray.jl` 文件物理末尾；不插入、不替换、不嵌套进任何原有测试块。块内仅重建局部变量 `A`、`mask_u16`、`mask`，不引用原有测试块内部局部变量；可复用原文件顶层的 `using TyImageProcessing, Test`。该新增块与已有同名补充块内容不重复，可原样追加执行。

**仍需 baseline 校准项：** 本补测使用合成均匀图像，只能验证类型接受、形状和可计算的均匀值性质；真实图像 `foosball.jpg` 的光源估计值应使用文档示例给出的 `[0.220627 0.298522 0.521911]` 或独立 baseline 进行精确校准，当前测试未覆盖该真实值断言。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumgray，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumgray 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumgray.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumgray.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumgray，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumgray
遗留缺陷：无
```
