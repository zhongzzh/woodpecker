# illumgray 提测分析报告

> 任务：illumgray（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumgray.md`
> 单测：`test/im_math/test_illumgray.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 15:35 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

我会把两个 `@testset` 文件与实现分开审计：覆盖证据只认测试文件中的显式构造、调用和断言；实现代码只用于核对文档约束是否自洽，不用实现能力反推覆盖。补测部分将仅在确有独立缺口时给出一个可追加的顶层测试块。### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| `A` | `m×n×3` RGB 数值数组 | `Float32`、`Float64`、`UInt8`、`UInt16` | 文档要求三维数组且第三维为 3 |
| `percentile` | 标量、2 元数值向量 | `Float32`、`Float64`、`UInt8`、`UInt16` | 标量范围为 `[0, 50]`；2 元向量各元素小于 `100` 且总和不超过 `100` |
| `Mask` | `m×n` 逻辑或数值数组 | `Bool`、`Float32`、`Float64`、`UInt8`、`UInt16` | 文档正文明确允许逻辑数组；数值类型列表未列出 `Bool` |
| `Norm` | 正数值标量 | `Float32`、`Float64`、`UInt8`、`UInt16` | 默认值为 `1`，文档未将默认值的整数类型列入数据类型列表 |

### 文档自身缺陷审计

- `Mask` 的说明允许逻辑数组，但“数据类型”列表只列出 `Float32`、`Float64`、`UInt8`、`UInt16`，遗漏 `Bool`。
- `percentile` 和 `Norm` 的默认值都写为整数 `1`，但对应的数据类型列表未列出整数类型。应明确默认整数是否属于例外，或将整数类型纳入支持范围。
- `percentile` 的文档形式写为“2 元数值向量”，但实现签名使用 `AbstractVecOrMat`，实际也接受长度为 2 的矩阵，例如 `1×2` 矩阵。文档应明确是否允许矩阵形式。
- `percentile` 标量规则与实现的总和约束共同作用后确实限制到不超过 `50`，但实现主要通过标量复制后检查总和实现该约束。建议在文档中明确标量等价于 `[p, p]`，以避免读者误解边界条件。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `m×n×3` | 通过 `cat(...; dims=3)` 构造三通道三维数组 | `test_illumgray.jl` / `synthetic usage`、`illumgray 补充` / `A32` | ☑ |
| `Float32` | 显式构造 `Float32` 三维 RGB 数组并调用，结果有近似断言 | `test_illumgray.jl` / `illumgray 补充` / `A32` | ☑ |
| `Float64` | `fill(0.1, ...)` 等默认浮点字面量构造 `Float64` 三维数组并调用，结果有断言 | `test_illumgray.jl` / `synthetic usage`、`validation` | ☑ |
| `UInt8` | 显式构造 `fill(UInt8(128), 4, 4, 3)` 并调用，结果有断言 | `test_illumgray.jl` / `synthetic usage` / `B` | ☑ |
| `UInt16` | 显式构造 `fill(UInt16(32768), 4, 4, 3)` 并调用，结果有近似断言 | `test_illumgray.jl` / `illumgray 补充` / `A16` | ☑ |

`imread("foosball.jpg")` 的返回元素类型没有类型断言，因此该图片输入本身不能作为某一具体 `A` 元素类型的证据；但四种文档声明的数据类型均已由合成数组显式覆盖。

#### percentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量 | 显式传入 `0`、`10.0`、`Float32(50)`、`UInt8(10)`、`UInt16(10)`，均有结果或异常断言 | `test_illumgray.jl` / `synthetic usage`、`illumgray 补充` | ☑ |
| 2 元数值向量 | 显式传入 `[0.0, 0.0]`、`Float32[40, 60]`，均有结果断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `Float32` | 在标量和 2 元向量结构中均有显式构造、调用和断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `Float64` | `10.0` 标量和 `[0.0, 0.0]` 向量均显式传入并有断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `UInt8` | 显式传入 `UInt8(10)` 标量并有结果断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `UInt16` | 显式传入 `UInt16(10)` 标量；另有 `UInt16[0, 100]` 异常调用 | `test_illumgray.jl` / `illumgray 补充` | ☑ |

#### Mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `m×n` | 显式构造 `4×4` 掩膜并通过 `Mask=` 传入 | `test_illumgray.jl` / `synthetic usage`、`illumgray 补充` | ☑ |
| `Bool` | 使用 `trues`、`falses` 构造逻辑掩膜，并有结果或异常断言 | `test_illumgray.jl` / `synthetic usage`、`validation`、`illumgray 补充` | ☑ |
| `Float32` | 显式构造 `ones(Float32, 4, 4)` 并调用，结果有近似断言 | `test_illumgray.jl` / `illumgray 补充` / `mask_f32` | ☑ |
| `Float64` | 显式构造 `ones(4, 4)` 并调用，结果有断言 | `test_illumgray.jl` / `illumgray 补充` / `mask_f64` | ☑ |
| `UInt8` | 显式构造 `ones(UInt8, 4, 4)` 并调用，结果有近似断言 | `test_illumgray.jl` / `illumgray 补充` / `mask_u8` | ☑ |
| `UInt16` | 显式构造 `ones(UInt16, 4, 4)` 并调用，结果有近似断言 | `test_illumgray.jl` / `illumgray 补充` / `mask_u16` | ☑ |

#### Norm

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 正数值标量 | 显式传入 `2`、`2.0`、`Float32(2)`、`UInt8(2)`、`UInt16(2)`，均有结果断言 | `test_illumgray.jl` / `synthetic usage`、`illumgray 补充` | ☑ |
| `Float32` | 显式传入 `Norm=Float32(2)` 并有结果断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `Float64` | 显式传入 `Norm=2.0` 并与 `Norm=2` 的结果比较 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `UInt8` | 显式传入 `Norm=UInt8(2)` 并与 `Norm=2` 的结果比较 | `test_illumgray.jl` / `illumgray 补充` | ☑ |
| `UInt16` | 显式传入 `Norm=UInt16(2)` 并有结果断言 | `test_illumgray.jl` / `illumgray 补充` | ☑ |

### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| `A` 的 `m×n×3` | 显式构造四种元素类型的三维三通道数组 | ☑ | 结构和类型均有强证据 |
| `percentile` 标量 | 多种显式标量类型和非默认值均有调用 | ☑ | 包含有效值和越界异常 |
| `percentile` 2 元向量 | 显式构造 `Float64`、`Float32` 两元素向量 | ☑ | 另有 `1×2` 矩阵形式调用，但文档未明确该形式 |
| `Mask` 的 `m×n` | 显式构造 `4×4` 逻辑和数值掩膜 | ☑ | 五种文档声明类型均有证据 |
| `Norm` 正数值标量 | 显式传入多种类型的正数 | ☑ | 非正值异常路径也有测试 |

### 覆盖缺口与优先级

**高优先级**

- 无。文档声明的每个输入参数结构和数据类型均已找到独立测试证据。

**中优先级**

- 无。已有测试覆盖了 `percentile` 的有效标量、有效 2 元向量以及若干边界异常。
- 无。`Mask` 的逻辑、浮点和无符号整数类型均已显式构造并断言结果或异常。

**低优先级**

- 可补充文档默认路径的显式调用与结果断言，例如省略 `percentile`、`Mask` 和 `Norm` 的调用，以确认默认值行为；这不是文档要求项缺口，因此不影响覆盖结论。
- 可补充 `percentile` 的 `1×2` 矩阵是否应被支持的行为测试，但该项属于文档与实现契约澄清，不是当前文档“2 元向量”要求的缺失。
- 可补充 `Norm=Inf`、非整数正数范数和 `percentile` 精确边界值的测试，但这些属于边界行为扩展，不是当前独立结构或数据类型缺口。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| `A` | `m×n×3`、`Float32`、`Float64`、`UInt8`、`UInt16` | `m×n×3`、`Float32`、`Float64`、`UInt8`、`UInt16` | 无 | 完全覆盖 |
| `percentile` | 标量、2 元数值向量、`Float32`、`Float64`、`UInt8`、`UInt16` | 标量、2 元数值向量、`Float32`、`Float64`、`UInt8`、`UInt16` | 无 | 完全覆盖 |
| `Mask` | `m×n`、`Bool`、`Float32`、`Float64`、`UInt8`、`UInt16` | `m×n`、逻辑数组、`Float32`、`Float64`、`UInt8`、`UInt16` | 无 | 完全覆盖 |
| `Norm` | 正数值标量、`Float32`、`Float64`、`UInt8`、`UInt16` | 正数值标量、`Float32`、`Float64`、`UInt8`、`UInt16` | 无 | 完全覆盖 |

总计：完全覆盖参数数 `4`；至少有证据参数数 `4`。由于没有真正缺失的文档要求项，不将弱证据、默认路径或样本规模问题折算为覆盖缺口。

### 缺失测试代码补全

当前没有需要新增的独立补测代码。四个文档输入参数的结构和数据类型均已有明确的构造或赋值、调用及结果或异常断言证据，因此不输出空的测试块，避免追加无实际覆盖价值的用例。

**静态检验说明**

无新增代码需要整体追加到文件末尾。现有 `illumgray 补充` 测试块已经位于测试文件末尾，并且其内部使用的样例变量均在该测试块内重新构造；本报告未声称已经运行这些新增测试。

**仍需 baseline 校准项**

无因覆盖缺口产生的 baseline 校准项。文档中关于默认整数类型、`Mask` 的 `Bool` 类型列举以及 2 元矩阵是否属于允许形式的问题，仍建议在接口文档与实现契约确认后补充对应的规范性断言。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumgray，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumgray 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumgray.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumgray.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumgray.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumgray，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumgray
遗留缺陷：无
```
