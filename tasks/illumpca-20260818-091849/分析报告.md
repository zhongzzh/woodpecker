# illumpca 提测分析报告

> 任务：illumpca（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumpca）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumpca.md`
> 单测：`test/im_math/test_illumpca.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumpca.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 09:23 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | 线性 RGB 图像，三维数组，第三维长度为 3 |
| percentage | 3.5 默认、数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 范围 (0,50]，用于保留最暗和最亮像素比例 |
| mask | m×n 逻辑或数值矩阵 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | 默认全 1；掩膜值为 0 的像素不参与估计 |

### 文档自身缺陷审计

- 文档未声明 A 必须非空；实现 `illumpca_parseInputs` 会对空 A 报错“A must be nonempty”。
- 文档对 `percentage` 和 `Mask` 允许类型列表不完整：实现签名使用 `Real`，实际接受 `Float16`、`BigFloat`、`Int128`、`UInt128` 等超出文档列表的类型；同时实现也未按文档列表做类型限制，存在约束不一致。
- 文档未在“输入参数”中明确写出 A 的“三维且第三维长度为 3”的约束，虽然 `m×n×3` 形式已隐含，但与实现中显式校验相比仍不够完整。
- 示例中的 `illuminant` 数值依赖外部文件 `foosball.jpg`，文档未提供该图像或可复现的合成数据来源，精确输出难以独立复现。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | 构造 `Float64.(1:144)./144` 的 8×6×3 数组并调用 `illumpca(A, 10)`，有精确数值断言 | test_illumpca.jl / percentage and mask | ☑ |
| Float32 | 构造 `zeros(Float32, 4, 4, 3)` 并调用 `illumpca`，结果与 `zeros(1, 3)` 比较 | test_illumpca.jl / percentage and mask | ☑ |
| UInt8 | 未找到直接向 `illumpca` 传入 UInt8 数组的测试代码；`imread` 结果未做类型断言，且后续传入的是 `rgb2lin(A)` | test_illumpca.jl / case image | 未覆盖 |
| UInt16 | 未找到直接向 `illumpca` 传入 UInt16 数组的测试代码 | test_illumpca.jl | 未覆盖 |
| m×n×3 数组结构 | 测试使用 8×6×3、2×2×3、4×4×3 三维数组，第三维均为 3 | test_illumpca.jl / percentage and mask | ☑ |

说明：Float32 覆盖基于全零图像，属于弱证据；Float64 覆盖为强证据。UInt8 和 UInt16 未覆盖。

#### percentage

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 3.5 | `illumpca(A_lin)` 未显式传入 percentage，仅证明默认路径执行 | test_illumpca.jl / case image | ☑ |
| Float32 | 未找到显式传入 Float32 percentage 的有效范围测试 | test_illumpca.jl | 未覆盖 |
| Float64 | 未找到显式传入 Float64 有效范围百分比的测试；仅 `NaN` 无效值调用 | test_illumpca.jl / input validation | 未覆盖 |
| Int8、Int16、Int32 | 未找到显式传入这些类型的测试 | test_illumpca.jl | 未覆盖 |
| Int64 | 显式传入 `10`、`1` 正常路径，`0`、`51` 异常路径 | test_illumpca.jl / percentage and mask、input validation | ☑ |
| UInt8、UInt16、UInt32、UInt64 | 未找到显式传入这些类型的测试 | test_illumpca.jl | 未覆盖 |
| 标量形式 | `10`、`1`、`NaN`、`0`、`51` 均为标量传入 | test_illumpca.jl / percentage and mask、input validation | ☑ |
| 范围 (0,50] 行为 | 越界值 `0`、`51` 及 `NaN` 有异常断言；上限 50 的有效路径未显式测试 | test_illumpca.jl / input validation | ☑ |

说明：默认 3.5 仅覆盖默认路径，属弱证据；Int64 覆盖较强。Float32、Float64 有效值以及除 Int64 外的整数类型均未覆盖。

#### mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Bool 矩阵 | 构造 `trues(8, 6)` 并作为 `Mask` 传入，有精确数值断言 | test_illumpca.jl / percentage and mask | ☑ |
| Float64 矩阵 | 使用 `Float64.(mask)` 构造数值掩膜并传入，有数值断言 | test_illumpca.jl / percentage and mask | ☑ |
| Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 未找到这些类型的 mask 测试 | test_illumpca.jl | 未覆盖 |
| m×n 二维矩阵结构 | 使用 8×6、4×4、3×4 等二维 mask，含正常和异常尺寸断言 | test_illumpca.jl / percentage and mask、input validation | ☑ |
| 默认全 1 mask | 不传 `Mask` 的调用执行默认路径 | test_illumpca.jl / case image、percentage and mask | ☑ |

说明：Bool 和 Float64 mask 为强覆盖；默认全 1 mask 属弱证据。其他允许数值类型未覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| Float64 RGB 图像 | 正常三维 Float64 数组 | ☑ | 强覆盖 |
| Float32 RGB 图像 | 全零 Float32 三维数组 | ☑ | 弱覆盖 |
| UInt8 RGB 图像 | 无直接调用 | 未覆盖 | 需补测 |
| UInt16 RGB 图像 | 无直接调用 | 未覆盖 | 需补测 |
| percentage Int64 标量 | 显式传入 10、1 | ☑ | 强覆盖 |
| percentage 默认 3.5 | 默认路径 | ☑ | 弱覆盖 |
| percentage 其他允许类型 | Float32、Float64 正常值及多数整数类型 | 未覆盖 | 需补测 |
| mask Bool 二维矩阵 | 显式传入 `trues(8, 6)` | ☑ | 强覆盖 |
| mask Float64 二维矩阵 | 显式传入 `Float64.(mask)` | ☑ | 强覆盖 |
| mask 其他允许数值矩阵 | 除 Bool、Float64 外的文档类型 | 未覆盖 | 需补测 |
| 异常边界 | percentage 0、51、NaN，mask 全零、尺寸不匹配、3D、NaN | ☑ | 已有异常断言 |

### 覆盖缺口与优先级

**高优先级**

- A 的 UInt8、UInt16 输入完全没有直接调用 `illumpca` 的测试，无法验证实际归一化和工作类型分支。
- percentage 允许的 Float32、Float64 正常值，以及 Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 类型均未覆盖，目前只有 Int64 有强证据。
- mask 允许的 Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 矩阵未覆盖，当前仅有 Bool 和 Float64。

**中优先级**

- A 的 Float32 覆盖基于全零图像，缺少非零典型图像测试。
- percentage 上限 50 的有效路径未验证，目前只有 51 越界异常。
- mask 数值矩阵缺少多种整数类型的混合 0/1 测试。

**低优先级**

- 单像素有效 mask 或仅单个像素入选等 PCA 退化路径未覆盖。
- 非方形图像 m≠n 的正常 mask 匹配未验证，目前只有 3×4 异常尺寸。
- 不同工作类型下的数值稳定性与输出范围未系统检查。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | m×n×3 结构、Float32、Float64 | m×n×3 结构、Float32、Float64、UInt8、UInt16 | UInt8、UInt16 | 部分覆盖 |
| percentage | 默认 3.5、标量形式、Int64、部分范围边界 | 默认 3.5、数值标量、范围 (0,50]、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Float32 有效值、Float64 有效值、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64、上限 50 有效路径 | 部分覆盖 |
| mask | m×n 二维结构、默认全 1、Bool、Float64 | m×n 逻辑或数值矩阵、默认全 1、Bool、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：完全覆盖参数数 0；至少有证据参数数 3。所有参数均存在至少一个文档要求项完全没有测试证据，因此结论均为部分覆盖。

### 缺失测试代码补全

```julia
@testset "illumpca 补充" begin
    @testset "A 类型补充" begin
        A_u8 = reshape(UInt8.(1:144), 8, 6, 3)
        illuminant_u8 = illumpca(A_u8)
        @test size(illuminant_u8) == (1, 3)
        @test eltype(illuminant_u8) == Float64
        @test all(isfinite, illuminant_u8)
        @test all(x -> 0 <= x <= 1, illuminant_u8)

        A_u16 = reshape(UInt16.(1:144), 8, 6, 3)
        illuminant_u16 = illumpca(A_u16)
        @test size(illuminant_u16) == (1, 3)
        @test eltype(illuminant_u16) == Float64
        @test all(isfinite, illuminant_u16)
        @test all(x -> 0 <= x <= 1, illuminant_u16)
    end

    @testset "percentage 类型补充" begin
        A = reshape(Float64.(1:144) ./ 144, 8, 6, 3)
        for T in (Float32, Float64, Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64)
            p = T(10)
            illuminant = illumpca(A, p)
            @test size(illuminant) == (1, 3)
            @test eltype(illuminant) == Float64
            @test all(isfinite, illuminant)
        end
        @test size(illumpca(A, Float32(3.5))) == (1, 3)
        @test size(illumpca(A, Float64(3.5))) == (1, 3)
        @test size(illumpca(A, 50)) == (1, 3)
    end

    @testset "mask 类型补充" begin
        A = reshape(Float64.(1:144) ./ 144, 8, 6, 3)
        baseMask = trues(8, 6)
        baseMask[2:4, 3:5] .= false
        for T in (Float32, Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64)
            maskT = T.(baseMask)
            illuminant = illumpca(A, 10; Mask=maskT)
            @test size(illuminant) == (1, 3)
            @test eltype(illuminant) == Float64
            @test all(isfinite, illuminant)
        end
    end
end
```

**静态检验说明**

该代码块可整体追加到 `test_illumpca.jl` 文件末尾。代码块作用域自足，所有变量均在新增顶层 `@testset "illumpca 补充"` 内部定义，不引用原测试块内部局部变量；仅依赖原文件顶层已有的 `using TyImageProcessing, Test`，不插入、替换或改写任何原有测试代码。

**仍需 baseline 校准项**

- A 的 UInt8、UInt16 非零图像新增测试目前仅验证输出形状、类型、有限性和单位范围，未做精确数值回归；后续需使用已知参考实现或保存的预期输出进行 baseline 校准。
- percentage 和 mask 各类型补充测试同样只验证输出形状、类型和有限性，未验证跨类型时数值一致性是否与参考实现完全一致。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumpca，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumpca 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumpca.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumpca.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumpca，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumpca
遗留缺陷：无
```
