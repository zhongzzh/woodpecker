# illumpca 提测分析报告

> 任务：illumpca（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumpca）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumpca.md`
> 单测：`test/im_math/test_illumpca.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 15:44 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 三维数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像，第三维固定为 3 |
| percentage | 数值标量，默认值 3.5 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 有效范围为 (0, 50] |
| mask | m×n 逻辑或数值矩阵，默认全为 1 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | 通过区分大小写的关键字 `Mask` 传入，值为 0 的位置被排除 |

### 文档自身缺陷审计

- 参数标题和正文使用小写 `mask`，但实际 Julia 关键字及语法为区分大小写的 `Mask`，命名不一致，容易导致用户使用 `mask=` 后调用失败。
- 文档没有说明 `A` 必须非空、不得包含 NaN 且必须为有限值，而实现明确检查这些约束。
- 文档没有明确说明掩膜必须非空、不得包含 NaN，并且至少要有一个非零元素；实现会拒绝空掩膜、含 NaN 的掩膜和全零掩膜。
- 文档以 m×n 描述掩膜与图像尺寸关系，但没有直述 `Mask` 的行列数必须与 `A` 的前两维完全相同。
- 文档将输出描述为“三元数值行向量”，示例和实现实际返回尺寸为 1×3 的 `Matrix{Float64}`；对 Julia 用户而言，最好明确它不是一维 `Vector`。
- 文档未列出上述输入约束可能触发的异常类型；实现测试目前也只断言宽泛的 `Exception`。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n×3 三维数组 | 已明确构造多个三维 RGB 数组并断言结果尺寸或数值 | test_illumpca.jl + `percentage and mask` 中 `reshape`、`smallA`，以及 `A 类型补充` | ☑ |
| Float32 | 明确构造 4×4×3 全零数组并断言返回全零结果 | test_illumpca.jl + `percentage and mask` 中 `zeros(Float32, 4, 4, 3)` | ☑ |
| Float64 | 明确构造多个 Float64 RGB 数组并进行精确近似、尺寸和异常断言 | test_illumpca.jl + `percentage and mask`、`input validation` | ☑ |
| UInt8 | 明确构造 8×6×3 UInt8 数组并断言结果尺寸、类型、有限性和范围 | test_illumpca.jl + `A 类型补充` 中 `A_u8` | ☑ |
| UInt16 | 明确构造 8×6×3 UInt16 数组并断言结果尺寸、类型、有限性和范围 | test_illumpca.jl + `A 类型补充` 中 `A_u16` | ☑ |

说明：Float32 的文档类型已有覆盖证据，但现有样本是退化的全零图像，只验证了零输入分支，未验证非均匀 Float32 图像的常规 PCA 路径。`imread` 得到的图像及 `rgb2lin` 结果没有元素类型断言，因此未将图片加载用例作为任何特定 `A` 数据类型的证据。

#### percentage

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 | 多个标量被显式赋值并作为第二个位置参数传入 | test_illumpca.jl + `percentage and mask`、`percentage 类型补充` | ☑ |
| 默认值 3.5 | 未显式传入第二个参数的调用具有结果断言，证明默认路径被执行 | test_illumpca.jl + `case image`、`A 类型补充` | ☑ |
| 有效范围 (0, 50] | 正值 1、3.5、10 和上界 50 已调用；0 和 51 已断言异常 | test_illumpca.jl + `percentage and mask`、`input validation`、`percentage 类型补充` | ☑ |
| Float32 | 显式构造 Float32 标量 10 和 3.5 并断言结果属性 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| Float64 | 显式构造 Float64 标量 10 和 3.5 并断言结果属性 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| Int8 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| Int16 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| Int32 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| Int64 | 整数字面量 10、1、0、51、50 在常规 64 位 Julia 环境中为 Int64，并具有结果或异常断言 | test_illumpca.jl + `percentage and mask`、`input validation`、`percentage 类型补充` | ☑ |
| UInt8 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| UInt16 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| UInt32 | 没有找到该类型的 percentage 构造、调用和结果断言 | 无 | 未覆盖 |
| UInt64 | 显式构造 UInt64 标量 10 并断言结果属性 | test_illumpca.jl + `percentage 类型补充` | ☑ |

#### mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n 二维矩阵 | 显式传入与 A 前两维相同的 8×6 掩膜并断言数值结果 | test_illumpca.jl + `percentage and mask` 中 `mask`、`numericMask` | ☑ |
| 逻辑矩阵形式 | 显式构造 BitMatrix，设置部分 false 后通过 `Mask` 传入并断言基准数值 | test_illumpca.jl + `percentage and mask` 中 `mask` | ☑ |
| 数值矩阵形式 | 将逻辑掩膜转换为 Float64 矩阵并断言结果与逻辑掩膜一致 | test_illumpca.jl + `percentage and mask` 中 `numericMask` | ☑ |
| 默认全为 1 | 多个省略 `Mask` 的调用具有结果断言，证明默认掩膜路径被执行 | test_illumpca.jl + `case image`、`percentage and mask`、`A 类型补充` | ☑ |
| Float32 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| Float64 | 显式构造 Float64 数值掩膜并断言与逻辑掩膜结果一致 | test_illumpca.jl + `percentage and mask` 中 `numericMask` | ☑ |
| Int8 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| Int16 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| Int32 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| Int64 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| UInt8 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| UInt16 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| UInt32 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| UInt64 | 没有找到该类型掩膜的构造、关键字调用和结果断言 | 无 | 未覆盖 |
| Bool | 显式构造部分为 false 的 BitMatrix，并同时测试全 false、尺寸错误及三维逻辑掩膜异常 | test_illumpca.jl + `percentage and mask`、`input validation` | ☑ |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| m×n×3 RGB 数组 | 文档要求的三维 RGB 结构已有多个显式构造用例 | ☑ | 4 种文档类型均有证据 |
| 数值标量位置参数 | percentage 已以默认值和显式标量形式调用 | ☑ | 仍缺 6 种文档声明的数据类型 |
| m×n 逻辑矩阵 | 部分 true 和 false 的 BitMatrix 已作为 `Mask` 传入 | ☑ | 具有数值基准断言 |
| m×n 数值矩阵 | Float64 的 0 和 1 数值矩阵已作为 `Mask` 传入 | ☑ | 仍缺 9 种文档声明的数据类型 |
| 省略 percentage 的默认调用 | 调用结果具有尺寸、类型或数值断言 | ☑ | 只能证明默认路径 |
| 省略 Mask 的默认调用 | 调用结果具有尺寸、类型或数值断言 | ☑ | 只能证明默认全包含路径 |

### 覆盖缺口与优先级

高优先级

- `mask` 缺少 Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64。现有数值掩膜只有 Float64，不能证明其他公开声明类型可通过关键字分派和输入解析。建议将同一非平凡 Bool 掩膜转换为各缺失类型，并与 Bool 掩膜结果进行等价性断言。
- `percentage` 缺少 Int8、Int16、Int32、UInt8、UInt16、UInt32。现有整数证据只有 Int64 和 UInt64，不能证明其余公开声明的整数标量类型。建议使用同一个有效值 10 构造各类型，并与 Float64 基准结果比较。

中优先级

- 无独立结构维度缺失。A 的 m×n×3 形式、percentage 的标量形式，以及 mask 的二维逻辑和数值形式均已有明确证据。

低优先级

- 无新的文档要求项缺失。可选的证据质量增强是增加非均匀 Float32 图像，但 Float32 类型本身已有覆盖，因此不应把它重复统计为缺口。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | m×n×3 三维数组、Float32、Float64、UInt8、UInt16 | m×n×3 三维数组、Float32、Float64、UInt8、UInt16 | 无 | 完全覆盖 |
| percentage | 数值标量、默认值 3.5、范围 (0, 50]、Float32、Float64、Int64、UInt64 | 数值标量、默认值 3.5、范围 (0, 50]、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Int8、Int16、Int32、UInt8、UInt16、UInt32 | 部分覆盖 |
| mask | m×n 二维矩阵、逻辑形式、数值形式、默认全为 1、Float64、Bool | m×n 二维矩阵、逻辑形式、数值形式、默认全为 1、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：3 个文档输入参数中，1 个完全覆盖；3 个参数均至少具有部分测试证据。真正缺失的是 15 个独立的数据类型项，其中 percentage 缺失 6 项，mask 缺失 9 项；没有结构形式缺口。

### 缺失测试代码补全

```julia
@testset "illumpca 补充" begin
    @testset "percentage 缺失整数类型" begin
        A = reshape(Float64.(1:144) ./ 144, 8, 6, 3)
        reference = illumpca(A, Float64(10))

        for T in (Int8, Int16, Int32, UInt8, UInt16, UInt32)
            percentage = T(10)
            illuminant = illumpca(A, percentage)

            @test percentage isa T
            @test size(illuminant) == (1, 3)
            @test eltype(illuminant) == Float64
            @test all(isfinite, illuminant)
            @test illuminant ≈ reference
        end
    end

    @testset "Mask 缺失数值类型" begin
        A = reshape(Float64.(1:144) ./ 144, 8, 6, 3)
        logicalMask = trues(8, 6)
        logicalMask[2:4, 3:5] .= false
        reference = illumpca(A, Float64(10); Mask=logicalMask)

        for T in (Float32, Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64)
            numericMask = T.(logicalMask)
            illuminant = illumpca(A, Float64(10); Mask=numericMask)

            @test eltype(numericMask) == T
            @test size(illuminant) == (1, 3)
            @test eltype(illuminant) == Float64
            @test all(isfinite, illuminant)
            @test illuminant ≈ reference
        end
    end
end
```

静态检验说明：以上代码只包含一个新的、独立的顶层 `@testset "illumpca 补充" begin`。所有样例数组、标量、掩膜和比较基准均在新增测试块内部重新构造，不引用原有测试块的局部变量；它仅依赖测试文件已有的 `TyImageProcessing` 和 `Test` 顶层导入。该代码块应整体追加到单元测试主文件的物理末尾，可原样粘贴；此处仅完成静态审计，未声称已经运行新增测试。

仍需 baseline 校准项：新增用例采用相同数值输入下不同公开数据类型应产生一致结果的差分基准，没有编造新的精确光源系数。若项目要求为这些类型分别保存精确回归值，仍需使用可信参考实现或经确认的数学 baseline 校准后再加入固定系数断言。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumpca，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumpca 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumpca.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumpca.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumpca，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumpca
遗留缺陷：无
```
