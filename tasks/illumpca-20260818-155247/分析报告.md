# illumpca 提测分析报告

> 任务：illumpca（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumpca.md`
> 单测：`test/im_math/test_illumpca.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 15:54 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像，空间维度为 m×n，第三维固定为 3 |
| percentage | 数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 默认值为 3.5，允许范围为 (0, 50] |
| Mask（文档锚点为 mask） | m×n 逻辑或数值矩阵 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | 通过零值排除像素；默认应等价于全 1 掩膜 |

### 文档自身缺陷审计

- 关键字大小写不统一：语法和实现使用 `Mask`，输入参数标题及链接锚点使用 `mask`。Julia 关键字区分大小写，文档应明确实际调用必须写作 `Mask=...`。
- 文档未说明 A 必须非空、不得包含 `NaN` 或无穷值，但实现会拒绝这些输入。
- 文档未说明 Mask 必须非空、必须至少包含一个非零元素且不得包含 `NaN`，实现对此有显式校验。
- 文档没有说明数值 Mask 是否允许负数、无穷值或除 0、1 以外的非零值。实现按“非零即选中”处理，且只显式拒绝 `NaN`。
- 文档把 percentage 和 Mask 的类型列为有限集合，但函数签名接受更广泛的 `Real` 元素类型；除 A 外，解析代码没有按文档类型列表进行白名单校验，文档与实现接受范围可能不一致。
- 文档声明 percentage 的默认值是 3.5，但现有测试只分别执行默认调用和显式 3.5 调用，没有验证二者结果等价。
- 示例通过 `imread` 和 `rgb2lin` 获得 A，但没有断言 `A_lin` 的元素类型，不能由该示例或对应测试推断其覆盖了哪一种文档数据类型。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n×3 数值数组 | 多个测试显式构造三维数组并传入，结果具有形状、类型、数值或性质断言 | test_illumpca.jl + `percentage and mask`、`A 类型补充` | ☑ |
| Float64 | 显式构造 `reshape(Float64.(1:144) ./ 144, 8, 6, 3)`，并进行精确 baseline 近似断言 | test_illumpca.jl + `percentage and mask` | ☑ |
| Float32 | 显式构造 `zeros(Float32, 4, 4, 3)`，调用后断言返回全零矩阵 | test_illumpca.jl + `percentage and mask` | ☑ |
| UInt8 | 显式构造 UInt8 三维数组，断言输出形状、元素类型和取值范围 | test_illumpca.jl + `A 类型补充` | ☑ |
| UInt16 | 显式构造 UInt16 三维数组，断言输出形状、有限性和取值范围 | test_illumpca.jl + `A 类型补充` | ☑ |

说明：Float32 仅由全零图像覆盖，属于退化样本，虽然已具备构造、调用和结果断言并计为覆盖，但对非零 Float32 PCA 路径的证明较弱。`case image` 中加载并线性化的图像没有元素类型断言，因此不用于认定 A 的任何具体数据类型。

#### percentage

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 | 所有显式 percentage 均以标量形式传入并具有结果或异常断言 | test_illumpca.jl + `percentage and mask`、`percentage 类型补充`、`percentage 缺失整数类型` | ☑ |
| Float32 | 显式传入 `Float32(10)` 和 `Float32(3.5)`，断言输出形状及有限性 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| Float64 | 显式传入 `Float64(10)` 和 `Float64(3.5)`，断言输出形状及有限性 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| Int8、Int16、Int32 | 分别构造值为 10 的标量，结果与 Float64 reference 比较 | test_illumpca.jl + `percentage 缺失整数类型` | ☑ |
| Int64 | 直接传入整数文字量 10，并与固定 baseline 比较；在常规 64 位 Julia 环境中该文字量为 Int64 | test_illumpca.jl + `percentage and mask` | ☑ |
| UInt8、UInt16、UInt32 | 分别构造值为 10 的标量，结果与 Float64 reference 比较 | test_illumpca.jl + `percentage 缺失整数类型` | ☑ |
| UInt64 | 显式传入 `UInt64(10)`，断言输出形状及有限性 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| 默认调用路径 | 多个调用省略 percentage，且对结果形状、类型或数值进行断言 | test_illumpca.jl + `case image`、`percentage and mask`、`A 类型补充` | ☑ |
| 默认值等于 3.5 | 默认调用与显式 3.5 调用从未直接比较，不能证明默认值语义 | test_illumpca.jl + 未找到等价性断言 | 未覆盖 |
| 范围内普通值 | 显式测试 1、3.5 和 10 | test_illumpca.jl + `percentage and mask`、`percentage 类型补充` | ☑ |
| 上界 50 | 显式传入 50 并断言返回形状 | test_illumpca.jl + `percentage 类型补充` | ☑ |
| 排除下界 0 | 显式断言 percentage 为 0 时抛出异常 | test_illumpca.jl + `input validation` | ☑ |
| 大于上界 | 显式断言 percentage 为 51 时抛出异常 | test_illumpca.jl + `input validation` | ☑ |
| 负数 | 文档范围排除所有负数，但没有负值调用及异常断言 | test_illumpca.jl + 未找到负 percentage 用例 | 未覆盖 |

说明：UInt64、Float32 和 Float64 的部分用例主要使用形状与有限性断言，没有数学 baseline，但仍满足构造、调用和结果断言要求。默认调用只能证明默认路径被执行，不能证明其具体默认值为 3.5。

#### Mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n 矩阵 | 显式构造与 A 前两维相同的 8×6 掩膜并作为关键字参数传入 | test_illumpca.jl + `percentage and mask`、`Mask 缺失数值类型` | ☑ |
| Bool | 构造含 true 和 false 的 BitMatrix，结果与固定 baseline 或 reference 比较 | test_illumpca.jl + `percentage and mask`、`Mask 缺失数值类型` | ☑ |
| Float64 | 将逻辑掩膜转换为 Float64，结果与逻辑掩膜结果比较 | test_illumpca.jl + `percentage and mask` | ☑ |
| Float32 | 将同一逻辑掩膜转换为 Float32，结果与逻辑 reference 比较 | test_illumpca.jl + `Mask 缺失数值类型` | ☑ |
| Int8、Int16、Int32、Int64 | 分别转换同一逻辑掩膜，结果与逻辑 reference 比较 | test_illumpca.jl + `Mask 缺失数值类型` | ☑ |
| UInt8、UInt16、UInt32、UInt64 | 分别转换同一逻辑掩膜，结果与逻辑 reference 比较 | test_illumpca.jl + `Mask 缺失数值类型` | ☑ |
| 零值排除语义 | 数值掩膜由含 false 的逻辑掩膜转换而来，数值结果与逻辑结果一致 | test_illumpca.jl + `percentage and mask`、`Mask 缺失数值类型` | ☑ |
| 默认 Mask 路径 | 多个调用省略 Mask，并对结果进行断言 | test_illumpca.jl + `case image`、`percentage and mask`、`A 类型补充` | ☑ |
| 默认等价于全 1 掩膜 | 没有将省略 Mask 的结果与显式全 true 或全 1 掩膜结果比较 | test_illumpca.jl + 未找到默认掩膜等价性断言 | 未覆盖 |

说明：全 false 掩膜只用于异常断言，不构成有效 ROI 的正常行为覆盖；但 Bool 类型和逻辑矩阵结构已由含 true、false 的正常掩膜充分覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| m×n×3 RGB 数值数组 | A 的文档要求结构已由多种显式构造数组覆盖 | ☑ | 第三维固定为 3 的正常调用有明确断言 |
| 数值标量 | percentage 的标量结构已覆盖全部文档列举类型 | ☑ | 未发现向量或矩阵被误作 percentage 的正常调用 |
| m×n 逻辑矩阵 | Bool 掩膜含选中与排除区域，并有结果比较 | ☑ | 与 A 前两维一致 |
| m×n 数值矩阵 | 文档列出的全部数值 Mask 类型均已覆盖 | ☑ | 通过与逻辑 reference 比较验证 0、1 语义 |
| 省略可选位置参数 | 默认 percentage 路径已经执行 | ☑ | 尚未证明默认结果等价于显式 3.5 |
| 省略关键字参数 | 默认 Mask 路径已经执行 | ☑ | 尚未证明默认结果等价于显式全 1 掩膜 |
| `Mask=...` 关键字调用 | 显式 Bool 和数值 Mask 均通过该形式调用 | ☑ | 实际关键字首字母大写 |

### 覆盖缺口与优先级

高优先级

- 无。A、percentage 和 Mask 的文档结构及全部列举数据类型均已有构造、调用和结果断言证据。

中优先级

- percentage 的默认值语义未覆盖：现有测试分别执行了省略参数和显式 3.5，但没有比较结果，因而不能证明文档声明的默认值确为 3.5。建议在同一非退化 Float64 图像上比较两种调用结果。
- Mask 的默认全 1 语义未覆盖：现有默认调用只能证明默认路径被执行，不能证明它与显式全 true 掩膜等价。建议比较省略 Mask 与 `Mask=trues(m, n)` 的结果。
- percentage 的负值区间未覆盖：现有 0 用例覆盖排除下界，51 覆盖超出上界，但没有验证严格小于 0 的值被拒绝。建议加入负标量的异常断言。

低优先级

- Float32 A 仅使用全零退化图像。该类型已计为覆盖，不属于差异化度量缺失项；如后续需要提高算法路径强度，可另用非零 Float32 图像进行性质或 baseline 校准测试。
- `imread` 后的 `A_lin` 没有元素类型断言。该文件加载场景仍可证明集成调用有效，但不能作为具体 A 数据类型证据；现有显式数组已覆盖全部文档类型，因此不需要为类型覆盖重复补测。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | m×n×3、Float32、Float64、UInt8、UInt16 | m×n×3、Float32、Float64、UInt8、UInt16 | 无 | 完全覆盖 |
| percentage | 数值标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、默认调用路径、范围内普通值、上界 50、排除下界 0、大于上界 | 数值标量、全部十种列举类型、默认值 3.5、取值范围 (0, 50]、范围外拒绝行为 | 默认调用等价于显式 3.5、负 percentage 拒绝行为 | 部分覆盖 |
| Mask | m×n 矩阵、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool、零值排除、默认调用路径、关键字调用 | m×n 逻辑或数值矩阵、全部十一种列举类型、零值排除、默认全 1 掩膜、`Mask=...` 调用 | 默认调用等价于显式全 1 掩膜 | 部分覆盖 |

总计：3 个输入参数中，完全覆盖参数数为 1；至少具有部分覆盖证据的参数数为 3。所有文档列举的数据类型和结构形式均已有证据，部分覆盖结论仅来自尚未验证的默认值语义和负值边界行为。

### 缺失测试代码补全

```julia
@testset "illumpca 补充" begin
    A = reshape(Float64.(1:144) ./ 144, 8, 6, 3)

    @testset "percentage 默认值与负值边界" begin
        defaultIlluminant = illumpca(A)
        explicitDefaultIlluminant = illumpca(A, Float64(3.5))
        @test defaultIlluminant ≈ explicitDefaultIlluminant
        @test_throws Exception illumpca(A, -1)
    end

    @testset "Mask 默认全选语义" begin
        defaultMaskIlluminant = illumpca(A, Float64(10))
        explicitFullMaskIlluminant = illumpca(
            A, Float64(10); Mask=trues(size(A, 1), size(A, 2))
        )
        @test defaultMaskIlluminant ≈ explicitFullMaskIlluminant
    end
end
```

静态检验说明：以上代码只包含一个新的、独立的顶层 `@testset "illumpca 补充" begin`，其所需 A 和中间结果均在自身作用域内构造；它仅依赖原测试文件已有的 `TyImageProcessing` 与 `Test` 顶层导入，可原样整体追加到文件末尾。此处仅进行了静态审查，未声称已经运行新增测试。

仍需 baseline 校准项：无。新增测试只验证文档直接声明的默认调用等价性和负值异常性质，不引入未经数学真值或既有 baseline 验证的精确光源数值。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumpca，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumpca 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumpca.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumpca.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumpca.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumpca，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumpca
遗留缺陷：无
```
