# illumwhite 提测分析报告

> 任务：illumwhite（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumwhite）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumwhite.md`
> 单测：`test/im_math/test_illumwhite.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumwhite.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 09:07 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像 |
| topPercentile | 默认值 1 或数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 范围 [0, 100)，0 表示返回最大通道值 |
| mask | m×n 逻辑或数值矩阵 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | 值为 0 的像素被排除，默认全 1 |

### 文档自身缺陷审计

- `topPercentile` 文档称设置为 0 时“返回最大的红色、绿色和蓝色值”，但实现按均匀分箱返回 bin 代表值，存在量化近似，文档未说明。
- `mask` 实现会将显式 1×1 全非零掩膜视为默认掩膜并绕过尺寸一致性校验，文档未说明该特殊行为。
- 语法行 `illuminant = illumwhite(___; Mask=mask)` 使用 `___` 占位符，未明确说明前两个输入可省略或组合，格式略不严谨。
- 未发现明显的数据类型列表与主签名之间的直接矛盾。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float32 | m×n×3 Float32 数组正常估计 | 无 | 未覆盖 |
| Float64 | m×n×3 Float64 数组，仅在非法 p、Mask 异常路径中构造传入 | test_illumwhite.jl / @testset "input validation" | ☑ |
| UInt8 | m×n×3 UInt8 数组正常估计 | test_illumwhite.jl / @testset "percentile and mask" | ☑ |
| UInt16 | m×n×3 UInt16 数组正常估计 | 无 | 未覆盖 |
| m×n×3 结构 | 三维数组且第三维为 3 | test_illumwhite.jl / case image、percentile and mask | ☑ |

说明：`Float64` 的 ☑ 仅来自异常路径，未断言正常 `illuminant` 结果，属于弱覆盖；`UInt8` 与 `m×n×3` 结构为强覆盖。

#### topPercentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float32 | 正常 Float32 标量 | 无 | 未覆盖 |
| Float64 | Float64 NaN 异常 | test_illumwhite.jl / @testset "input validation" | ☑ |
| Int8 | 正常 Int8 标量 | 无 | 未覆盖 |
| Int16 | 正常 Int16 标量 | 无 | 未覆盖 |
| Int32 | 正常 Int32 标量 | 无 | 未覆盖 |
| Int64 | 5、1、-1、100 | test_illumwhite.jl / case image、percentile and mask、input validation | ☑ |
| UInt8 | 正常 UInt8 标量 | 无 | 未覆盖 |
| UInt16 | 正常 UInt16 标量 | 无 | 未覆盖 |
| UInt32 | 正常 UInt32 标量 | 无 | 未覆盖 |
| UInt64 | 正常 UInt64 标量 | 无 | 未覆盖 |
| 数值标量结构 | 位置参数为单个标量 | test_illumwhite.jl / case image、percentile and mask | ☑ |
| 范围边界 0 | topPercentile=0 合法行为 | 无 | 未覆盖 |
| 默认值 1 | 不传 topPercentile 的默认路径 | 无 | 未覆盖 |

说明：`Float64` 的 ☑ 仅来自 NaN 异常，未覆盖正常 Float64 标量计算；`Int64` 覆盖正常值和非法值；默认路径与 0 边界均无现有测试。

#### mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float32 | 正常 Float32 数值掩膜 | 无 | 未覆盖 |
| Float64 | 正常 Float64 数值掩膜，含 0 和 1 | test_illumwhite.jl / @testset "percentile and mask" | ☑ |
| Int8 | 正常 Int8 数值掩膜 | 无 | 未覆盖 |
| Int16 | 正常 Int16 数值掩膜 | 无 | 未覆盖 |
| Int32 | 正常 Int32 数值掩膜 | 无 | 未覆盖 |
| Int64 | 正常 Int64 数值掩膜 | 无 | 未覆盖 |
| UInt8 | 正常 UInt8 数值掩膜 | 无 | 未覆盖 |
| UInt16 | 正常 UInt16 数值掩膜 | 无 | 未覆盖 |
| UInt32 | 正常 UInt32 数值掩膜 | 无 | 未覆盖 |
| UInt64 | 正常 UInt64 数值掩膜 | 无 | 未覆盖 |
| Bool | 正常逻辑掩膜 | test_illumwhite.jl / @testset "percentile and mask" mask=trues(4,4) | ☑ |
| m×n 结构 | 与 A 尺寸一致的二维矩阵 | test_illumwhite.jl / @testset "percentile and mask" | ☑ |
| 默认全 1 | 不传 Mask 时使用全部像素 | test_illumwhite.jl / @testset "percentile and mask" illumwhite(A,1) | ☑ |
| 排除 0 | 掩膜值为 0 的像素被排除 | test_illumwhite.jl / @testset "percentile and mask" mask[1,1]=false | ☑ |

说明：`Bool`、`Float64`、`m×n` 结构、默认全 1、排除 0 为强覆盖；其余数值类型均未覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| RGB 三维数组 | m×n×3 输入 A | ☑ | UInt8 强覆盖，Float64 弱覆盖 |
| 数值标量 | topPercentile 输入 | ☑ | Int64 强覆盖，Float64 弱覆盖 |
| 二维逻辑矩阵 | mask Bool | ☑ | 强覆盖 |
| 二维数值矩阵 | mask Float64 | ☑ | 强覆盖 |
| 默认全 1 掩膜形式 | 不传 Mask 的默认路径 | ☑ | 强覆盖 |

### 覆盖缺口与优先级

**高优先级**

- A 的 Float32、UInt16 类型完全缺失；现有仅 UInt8 正常、Float64 仅异常路径。建议补测合成 Float32、UInt16 图像并断言 1×3 Float64 输出及可推导精确值。
- topPercentile 的 Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 类型完全缺失；现有仅 Int64 正常、Float64 仅 NaN。建议循环构造同值不同整数、浮点标量，断言结果一致。
- mask 的 Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 类型完全缺失；现有仅 Bool、Float64。建议循环构造全真数值掩膜，断言与 Bool 掩膜结果一致。

**中优先级**

- topPercentile=0 文档明确边界未覆盖；建议补测 p=0 时返回最大通道值。
- 默认 topPercentile 路径未覆盖；现有调用均显式传入 p。建议补测 `illumwhite(A)`。
- A 的 Float64 正常路径未覆盖，仅在非法 p、Mask 下出现；建议补测 Float64 正常估计。
- mask 的非 1 非零值行为未覆盖；现有数值 mask 仅 0、1。建议补测混合 0、2.0 等值，断言仅排除 0。

**低优先级**

- 文档未说明浮点输入的分箱量化近似，topPercentile=0 返回 bin 代表值而非精确最大像素值；建议文档补充。
- 实现将显式 1×1 全非零 Mask 视为默认掩膜并绕过尺寸校验，文档未说明；建议补测试或文档说明。
- 非法 A 元素类型无测试；建议补测 Int32 A 报错。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | UInt8、Float64、m×n×3 结构 | Float32、Float64、UInt8、UInt16、m×n×3 结构 | Float32、UInt16 | 部分覆盖 |
| topPercentile | Int64、Float64、数值标量结构 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、数值标量、范围 [0,100) 含 0、默认 1 | Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64、topPercentile=0 合法边界、默认参数未显式调用路径 | 部分覆盖 |
| mask | Float64、Bool、m×n 结构、默认全 1、排除 0 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool、m×n 逻辑或数值矩阵、默认全 1、排除 0 | Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：完全覆盖参数数 0；至少有证据参数数 3。三个参数均为部分覆盖，不将部分覆盖折算为百分比。

### 缺失测试代码补全

```julia
@testset "illumwhite 补充" begin
    @testset "A Float32、UInt16、Float64 正常路径" begin
        A32 = zeros(Float32, 4, 4, 3)
        @test illumwhite(A32, 1) ≈ zeros(1, 3)

        A16 = zeros(UInt16, 4, 4, 3)
        @test illumwhite(A16, 1) ≈ zeros(1, 3)

        A64 = ones(Float64, 4, 4, 3)
        @test illumwhite(A64, 1) ≈ ones(1, 3)
    end

    @testset "A 非法元素类型" begin
        @test_throws Exception illumwhite(zeros(Int32, 4, 4, 3))
    end

    @testset "topPercentile 类型与 0 边界" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected = reshape(Float64[200, 210, 220] ./ 255, 1, 3)

        @test illumwhite(A, 0) ≈ expected

        for p in (Int8(1), Int16(1), Int32(1), UInt8(1), UInt16(1), UInt32(1), UInt64(1), Float32(1.0), Float64(1.0))
            @test illumwhite(A, p) ≈ expected
        end

        @test illumwhite(A) ≈ expected
    end

    @testset "Mask 数值类型与非 1 非零值" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected_all = reshape(Float64[200, 210, 220] ./ 255, 1, 3)
        expected_excl = reshape(Float64[10, 20, 40] ./ 255, 1, 3)

        for T in (Float32, Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64)
            M = ones(T, 4, 4)
            @test illumwhite(A; Mask=M) ≈ expected_all
        end

        mixed = fill(2.0, 4, 4)
        mixed[1, 1] = 0.0
        @test illumwhite(A; Mask=mixed) ≈ expected_excl
    end
end
```

**静态检验说明**

该代码块作用域自足，所有辅助数据均在新增顶层 `@testset "illumwhite 补充"` 内部重新构造，不引用原测试块内部的局部变量；仅依赖原文件顶层的 `using TyImageProcessing, Test` 和已定义的 `illumwhite` 函数。该代码块可整体追加到 `test_illumwhite.jl` 文件物理末尾，无需插入或改写任何原有代码。

**仍需 baseline 校准项**

合成数据的精确值基于当前实现的均匀分箱和边界取值推导；若实现的分箱策略、归一化分母或像素计数规则调整，需用官方参考结果校准。真实图像 `foosball.jpg` 的 illuminant 数值仍需与官方示例结果进行 baseline 校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumwhite，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumwhite 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumwhite.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumwhite.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumwhite，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumwhite
遗留缺陷：无
```
