# illumwhite 提测分析报告

> 任务：illumwhite（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumwhite）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumwhite.md`
> 单测：`test/im_math/test_illumwhite.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 14:17 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | Float32、Float64、UInt8、UInt16 | RGB 图像，第三维为 3，非空未显式说明 |
| topPercentile | 1（默认）或数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 范围 [0,100)，0 表示取最大红绿蓝值 |
| Mask | m×n 逻辑或数值矩阵 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64、Bool | 关键字 Mask=mask，排除 0 像素，默认全 1 |

### 文档自身缺陷审计

- A 未明确说明必须为非空数组；实现会对空数组报错。
- A 未明确说明第三维必须为 3，但文档形式已写 m×n×3，矛盾较小。
- topPercentile 只说明“数值标量”，未明确必须为实数值标量；源码签名要求 `Real`，数组传入会报错。
- Mask 未明确说明 `m`、`n` 必须与 A 前两维一致；实现仅对默认 1×1 非零掩膜放宽该约束。
- Mask 未明确说明不允许 NaN；实现会对 NaN 掩膜报错。
- Mask 的默认形式未说明 1×1 非零掩膜会被视为默认全 1 掩膜。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float32 | zeros(Float32,4,4,3) 正常调用并断言 | test_illumwhite.jl：illumwhite 补充 / A Float32、UInt16、Float64 正常路径 | ☑ |
| Float64 | ones(Float64,4,4,3) 正常调用并断言 | test_illumwhite.jl：illumwhite 补充 / A Float32、UInt16、Float64 正常路径 | ☑ |
| UInt8 | zeros(UInt8,4,4,3) 构造后写入像素并断言 | test_illumwhite.jl：percentile and mask | ☑ |
| UInt16 | zeros(UInt16,4,4,3) 正常调用并断言 | test_illumwhite.jl：illumwhite 补充 / A Float32、UInt16、Float64 正常路径 | ☑ |
| m×n×3 三维结构 | 4×4×3 数组多次构造、调用并断言 | test_illumwhite.jl：percentile and mask；illumwhite 补充 | ☑ |

说明：`case image` 中通过 `imread` 得到的 A 未断言元素类型，因此该用例不作为 A 具体数据类型覆盖证据，仅作为真实图像调用路径佐证。

#### topPercentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Int64 标量 | 显式 5、1、0 及默认 1 调用并断言 | test_illumwhite.jl：case image；percentile and mask；illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| Float64 标量 | Float64(1.0) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| Float32 标量 | Float32(1.0) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| Int8 标量 | Int8(1) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| Int16 标量 | Int16(1) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| Int32 标量 | Int32(1) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| UInt8 标量 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt16 标量 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt32 标量 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt64 标量 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| [0,100) 范围与异常 | 0 边界、NaN、-1、100 异常断言 | test_illumwhite.jl：illumwhite 补充 / topPercentile 类型与 0 边界；input validation | ☑ |
| 数值标量结构 | 非标量向量或矩阵传入异常未测 | 无 | 未覆盖 |

说明：`illumwhite(A)` 默认调用仅证明默认路径可执行，不能替代非默认值和全部允许类型的覆盖；当前已有非默认值 5、1、0 的显式证据。

#### Mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Bool 矩阵 | trues(4,4) 且排除一个像素后断言 | test_illumwhite.jl：percentile and mask | ☑ |
| Float64 矩阵 | numericMask 与 fill(2.0) 非 1 非零掩膜调用并断言 | test_illumwhite.jl：percentile and mask；illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| Float32 矩阵 | ones(Float32,4,4) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| Int8 矩阵 | ones(Int8,4,4) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| Int16 矩阵 | ones(Int16,4,4) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| Int32 矩阵 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| Int64 矩阵 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt8 矩阵 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt16 矩阵 | 文档允许，无显式调用与结果断言 | 无 | 未覆盖 |
| UInt32 矩阵 | ones(UInt32,4,4) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| UInt64 矩阵 | ones(UInt64,4,4) 调用并断言 | test_illumwhite.jl：illumwhite 补充 / Mask 数值类型与非 1 非零值 | ☑ |
| m×n 矩阵结构 | 4×4 掩膜与 A 同尺寸 | test_illumwhite.jl：percentile and mask；illumwhite 补充 | ☑ |
| 默认全 1 路径 | 不传 Mask 调用并断言 | test_illumwhite.jl：percentile and mask；illumwhite 补充 / topPercentile 类型与 0 边界 | ☑ |
| 0 排除、NaN 与尺寸异常 | 掩膜 0 排除、NaN、尺寸不匹配异常 | test_illumwhite.jl：percentile and mask；input validation | ☑ |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| RGB 三维数组 m×n×3 | 四种允许元素类型均正常调用并断言 | ☑ | imread 用例未断言元素类型，不计入具体类型 |
| 数值标量 percentile | 部分类型、0 边界、默认路径、范围异常已覆盖 | ☑ | 缺 UInt8、UInt16、UInt32、UInt64 和非标量异常 |
| m×n 逻辑或数值掩膜 | 多种类型、默认路径、0 排除、异常已覆盖 | ☑ | 缺 Int32、Int64、UInt8、UInt16 类型 |

### 覆盖缺口与优先级

**高优先级**

- `topPercentile::UInt8/UInt16/UInt32/UInt64`：文档允许这些无符号整数类型，但现有补充循环只覆盖 Int8、Int16、Int32、Float32、Float64，缺少无符号整型显式调用及结果断言。建议用已知 UInt8 图像和预期光源向量逐一测试。
- `Mask::Int32/Int64/UInt8/UInt16`：文档允许这些数值矩阵类型，但现有补充循环只覆盖 Float32、Int8、Int16、UInt32、UInt64，缺少四种类型。建议用 `ones(T,4,4)` 作掩膜并断言。

**中优先级**

- `topPercentile` 非标量非法输入未测：文档称“数值标量”，源码对数组会 MethodError。建议补 `@test_throws Exception illumwhite(A, [1,2])`。
- `Mask` 空矩阵非法输入未测：源码要求非空，文档未说明。建议补 `Mask=zeros(0,0)` 或 `falses(0,0)` 异常断言。

**低优先级**

- A 空数组、非三通道、非法元素类型异常未测：文档只声明允许类型和 m×n×3，未明确非法输入异常；如需可补异常用例。
- 显式 `Mask=trues(1,1)` 特殊默认掩膜路径未测：源码将该形式视为默认全 1，文档未说明；建议补测或补充文档说明。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | Float32、Float64、UInt8、UInt16、m×n×3 结构 | Float32、Float64、UInt8、UInt16、m×n×3 结构 | 无 | 完全覆盖 |
| topPercentile | Int64、Float64、Float32、Int8、Int16、Int32 标量、默认 1 路径、0 边界、NaN 与 -1 和 100 异常 | 数值标量、[0,100)、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | UInt8、UInt16、UInt32、UInt64 显式标量调用；非标量异常行为 | 部分覆盖 |
| Mask | Bool、Float64、Float32、Int8、Int16、UInt32、UInt64 矩阵、m×n 结构、默认路径、0 排除、NaN 与尺寸异常 | Bool、Float64、Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 矩阵、m×n 结构、默认路径、0 排除 | Int32、Int64、UInt8、UInt16 矩阵显式调用 | 部分覆盖 |

总计：完全覆盖参数数 1 个；至少有证据参数数 3 个。

### 缺失测试代码补全

```julia
@testset "illumwhite 覆盖补全" begin
    @testset "topPercentile 无符号整型类型" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected = reshape(Float64[200, 210, 220] ./ 255, 1, 3)

        for p in (UInt8(1), UInt16(1), UInt32(1), UInt64(1))
            @test illumwhite(A, p) ≈ expected
        end
    end

    @testset "topPercentile 非标量异常" begin
        A = zeros(UInt8, 4, 4, 3)
        @test_throws Exception illumwhite(A, [1, 2])
        @test_throws Exception illumwhite(A, zeros(1, 1))
    end

    @testset "Mask 补充类型与空掩膜异常" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected_all = reshape(Float64[200, 210, 220] ./ 255, 1, 3)

        for T in (Int32, Int64, UInt8, UInt16)
            M = ones(T, 4, 4)
            @test illumwhite(A; Mask=M) ≈ expected_all
        end

        @test_throws Exception illumwhite(A; Mask=zeros(Int32, 0, 0))
        @test_throws Exception illumwhite(A; Mask=falses(0, 0))
    end
end
```

静态检验说明：以上新增代码块只包含一个独立顶层 `@testset "illumwhite 覆盖补全"`，作用域自足，不引用原测试块内部局部变量，也不重新打开或嵌套现有测试块；可整体追加到 `test_illumwhite.jl` 文件末尾。

仍需 baseline 校准项：补测中的 UInt8 合成图像期望值基于当前实现的 8-bit 归一化路径和 `topPercentile=1` 的确定性行为；若用于真实图像或浮点边界像素，应与独立 baseline 或参考实现校准。空矩阵和非标量异常仅验证会抛出 `Exception`，未细粒度校准具体错误类型。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumwhite，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumwhite 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumwhite.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumwhite.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumwhite，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumwhite
遗留缺陷：无
```
