# illumwhite 提测分析报告

> 任务：illumwhite（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_illumwhite）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/AutomaticWhiteBalance/illumwhite.md`
> 单测：`test/im_math/test_illumwhite.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-18 15:41 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | m×n×3 数值数组 | `Float32`、`Float64`、`UInt8`、`UInt16` | 非空 RGB 图像，第三维长度为 3 |
| topPercentile | 数值标量，默认值为 1，取值范围为 `[0, 100)` | `Float32`、`Float64`、`Int8`、`Int16`、`Int32`、`Int64`、`UInt8`、`UInt16`、`UInt32`、`UInt64` | 位置参数；设为 0 时返回各通道最大值对应的估计 |
| mask | m×n 逻辑或数值矩阵，通过关键字 `Mask` 传入 | `Float32`、`Float64`、`Int8`、`Int16`、`Int32`、`Int64`、`UInt8`、`UInt16`、`UInt32`、`UInt64`、`Bool` | 0 值像素被排除，非零值像素被纳入；默认全部像素均纳入 |

### 文档自身缺陷审计

- 示例先通过 `A_lin = rgb2lin(A)` 生成线性 RGB 图像，但随后调用的是 `illumwhite(A, topPercentile)`，而不是 `illumwhite(A_lin, topPercentile)`；之后又把估计结果用于线性图像 `A_lin`。文档应说明这是有意在 sRGB 图像上估计，还是示例变量使用错误。
- 文档没有说明浮点图像 `A` 的有效数值范围。实现按近似 `[0, 1]` 的归一化范围分箱，对大于 1、负值、`NaN` 和无穷值存在特殊处理，但这些行为未形成公开约束。
- 文档没有完整说明 `A` 必须非空，也没有明确列出非三维数组或第三维不为 3 时的异常行为。
- 文档没有说明 `mask` 必须非空、不得包含 `NaN`，且至少必须有一个非零元素；实现对这些情况均有显式异常处理。
- 文档只列出了固定的 `topPercentile` 类型，但函数签名接受任意 `Real`，实现也没有按文档类型列表进行白名单校验。因此 `Bool`、`Float16`、部分大整数或有理数等可能被实现接受，文档与实现的类型边界不一致。
- 文档只列出了固定的 `mask` 元素类型，但函数签名接受任意元素为 `Real` 的数组，解析过程同样没有按文档类型列表限制，因此实现的实际接受范围可能宽于文档。
- `mask` 在语法中以小写名称展示，但 Julia 调用关键字实际为大小写敏感的 `Mask`。说明正文和参数标题应明确区分文档概念名与实际关键字名。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n×3 数值数组 | 多个 `4×4×3` 数组被明确构造、传入并断言结果 | `test_illumwhite.jl` + `percentile and mask`、`A Float32、UInt16、Float64 正常路径` | ☑ |
| `Float32` | 构造 `zeros(Float32, 4, 4, 3)`，调用后断言三通道结果为 0 | `test_illumwhite.jl` + `A Float32、UInt16、Float64 正常路径` | ☑ |
| `Float64` | 构造 `ones(Float64, 4, 4, 3)`，调用后断言三通道结果为 1 | `test_illumwhite.jl` + `A Float32、UInt16、Float64 正常路径` | ☑ |
| `UInt8` | 构造具有通道背景值和单个亮点的数组，并断言精确归一化结果 | `test_illumwhite.jl` + `percentile and mask` | ☑ |
| `UInt16` | 构造 `zeros(UInt16, 4, 4, 3)`，调用后断言三通道结果为 0 | `test_illumwhite.jl` + `A Float32、UInt16、Float64 正常路径` | ☑ |

说明：`Float32`、`Float64` 和 `UInt16` 的正常路径只使用了全零或全一的均匀图像，能够证明类型、结构、调用和基本结果，但对这些类型的分箱及亮点选择行为属于较弱证据。`imread("foosball.jpg")` 没有元素类型断言，因此不能据此补充任何 `A` 类型证据。

#### topPercentile

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 | 多个整数和浮点标量被明确传入，并有结果或异常断言 | `test_illumwhite.jl` + `percentile and mask`、`topPercentile 类型与 0 边界`、`input validation` | ☑ |
| 默认值 1 | `illumwhite(A)` 未显式传入百分比并断言结果，证明默认路径执行 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| 特殊值 0 | 显式传入 0，并断言返回各通道亮点对应结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| 合法正值 | 显式传入 1 和 5，并断言输出 | `test_illumwhite.jl` + `case image`、`percentile and mask` | ☑ |
| `Float32` | 显式传入 `Float32(1.0)` 并断言结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| `Float64` | 显式传入 `Float64(1.0)` 并断言结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| `Int8` | 显式传入 `Int8(1)` 并断言结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| `Int16` | 显式传入 `Int16(1)` 并断言结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| `Int32` | 显式传入 `Int32(1)` 并断言结果 | `test_illumwhite.jl` + `topPercentile 类型与 0 边界` | ☑ |
| `Int64` | 整数字面量 1、5、0、-1 和 100 在常规 64 位 Julia 环境中为 `Int64`，均有结果或异常断言 | `test_illumwhite.jl` + `case image`、`percentile and mask`、`input validation`、`topPercentile 类型与 0 边界` | ☑ |
| `UInt8` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt16` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt32` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt64` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |

说明：默认调用只能证明默认值 1 的路径，不能替代其他整数类型的显式覆盖。`NaN`、-1 和 100 已有异常断言，可作为范围校验的附加证据，但不替代缺失的无符号数据类型。

#### mask

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n 二维矩阵 | 构造多个 `4×4` 逻辑或数值矩阵，通过 `Mask` 传入并断言结果 | `test_illumwhite.jl` + `percentile and mask`、`Mask 数值类型与非 1 非零值` | ☑ |
| 逻辑矩阵 | 构造 `BitMatrix`，排除亮点后断言剩余像素结果 | `test_illumwhite.jl` + `percentile and mask` | ☑ |
| 数值矩阵 | 构造多个数值矩阵并通过关键字传入 | `test_illumwhite.jl` + `percentile and mask`、`Mask 数值类型与非 1 非零值` | ☑ |
| 默认全包含路径 | 省略 `Mask` 的调用具有结果断言 | `test_illumwhite.jl` + `percentile and mask`、`topPercentile 类型与 0 边界` | ☑ |
| 0 值排除语义 | 将亮点位置设为 0，并断言亮点被排除 | `test_illumwhite.jl` + `percentile and mask`、`Mask 数值类型与非 1 非零值` | ☑ |
| 非 1 的非零值纳入语义 | 使用值为 2.0 的掩膜并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `Float32` | 构造 `ones(Float32, 4, 4)` 并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `Float64` | 构造 `Float64.(mask)` 和含 0、2 的矩阵并断言结果 | `test_illumwhite.jl` + `percentile and mask`、`Mask 数值类型与非 1 非零值` | ☑ |
| `Int8` | 构造 `ones(Int8, 4, 4)` 并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `Int16` | 构造 `ones(Int16, 4, 4)` 并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `Int32` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `Int64` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt8` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt16` | 未找到显式构造、传入和结果断言 | 无 | 未覆盖 |
| `UInt32` | 构造 `ones(UInt32, 4, 4)` 并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `UInt64` | 构造 `ones(UInt64, 4, 4)` 并断言结果 | `test_illumwhite.jl` + `Mask 数值类型与非 1 非零值` | ☑ |
| `Bool` | 构造 `trues(4, 4)`，排除亮点后断言结果 | `test_illumwhite.jl` + `percentile and mask` | ☑ |

说明：`Float32`、`Int8`、`Int16`、`UInt32` 和 `UInt64` 掩膜均为全一矩阵，只能较弱地证明这些元素类型可被传入并产生结果；0 值排除和非 1 非零值纳入的语义由 `Bool`、`Float64` 掩膜提供了更强证据。按照独立维度规则，不要求每种已覆盖元素类型重复测试全部掩膜取值语义。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| m×n×3 数值数组 | `A` 的公开结构形式 | ☑ | 多种显式构造的 `4×4×3` 数组具有结果断言 |
| 数值标量 | `topPercentile` 的公开结构形式 | ☑ | 位置参数、默认调用和边界值均有证据 |
| m×n 逻辑矩阵 | `Mask` 的逻辑结构形式 | ☑ | 包含排除亮点的结果断言 |
| m×n 数值矩阵 | `Mask` 的数值结构形式 | ☑ | 多种元素类型和非 1 非零值已有证据 |
| 省略可选位置参数 | 使用默认 `topPercentile=1` | ☑ | `illumwhite(A)` 有结果断言 |
| 省略可选关键字参数 | 使用默认全包含掩膜 | ☑ | 多个无 `Mask` 调用有结果断言 |
| 显式关键字调用 | 使用 `Mask=mask` | ☑ | 逻辑和数值掩膜均有结果断言 |

### 覆盖缺口与优先级

**高优先级**

- `topPercentile` 缺少文档声明的 `UInt8`、`UInt16`、`UInt32`、`UInt64`。现有整数测试只覆盖有符号整数，默认值和整数字面量不能证明这些无符号类型可正常传入。建议在现有非均匀 `UInt8` 图像上分别显式构造值为 1 的四种无符号标量，并复用已有亮点结果断言。

**中优先级**

- `mask` 缺少文档声明的 `Int32`、`Int64`、`UInt8`、`UInt16`。现有其他数值掩膜不能替代这些元素类型。建议为每种缺失类型构造 `4×4` 掩膜，将亮点位置置零，并断言亮点被排除后的结果；一个循环即可补齐类型维度，不需要与不同 `A` 类型组合测试。

**低优先级**

- 没有其他完全缺失的文档结构或类型要求。现有 `Float32`、`Float64`、`UInt16` 图像主要是均匀输入，部分掩膜类型也只测试全一矩阵，语义证据较弱；后续可增加非均匀样本提升缺陷检出能力，但这些证据质量问题不构成新的文档要求缺失项。
- `imread("foosball.jpg")` 的元素类型未断言，不能作为输入类型覆盖证据。若希望把伴随图片纳入类型审计，应先断言加载结果的 `eltype`，但这不影响当前由显式数组完成的四种 `A` 类型覆盖。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | m×n×3 数值数组、`Float32`、`Float64`、`UInt8`、`UInt16` | m×n×3 数值数组、`Float32`、`Float64`、`UInt8`、`UInt16` | 无 | 完全覆盖 |
| topPercentile | 数值标量、默认值 1、特殊值 0、合法正值、`Float32`、`Float64`、`Int8`、`Int16`、`Int32`、`Int64` | 数值标量、默认值 1、特殊值 0、合法正值、`Float32`、`Float64`、`Int8`、`Int16`、`Int32`、`Int64`、`UInt8`、`UInt16`、`UInt32`、`UInt64` | `UInt8`、`UInt16`、`UInt32`、`UInt64` | 部分覆盖 |
| mask | m×n 二维矩阵、逻辑形式、数值形式、默认全包含、0 值排除、非零值纳入、`Float32`、`Float64`、`Int8`、`Int16`、`UInt32`、`UInt64`、`Bool` | m×n 二维矩阵、逻辑形式、数值形式、默认全包含、0 值排除、非零值纳入、`Float32`、`Float64`、`Int8`、`Int16`、`Int32`、`Int64`、`UInt8`、`UInt16`、`UInt32`、`UInt64`、`Bool` | `Int32`、`Int64`、`UInt8`、`UInt16` | 部分覆盖 |

总计：3 个公开输入参数中，1 个参数完全覆盖；3 个参数均至少具有部分覆盖证据。缺失项集中在 `topPercentile` 的 4 种无符号标量类型和 `mask` 的 4 种数值矩阵元素类型。

### 缺失测试代码补全

```julia
@testset "illumwhite 补充" begin
    @testset "topPercentile 无符号整数类型" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected = reshape(Float64[200, 210, 220] ./ 255, 1, 3)

        for p in (UInt8(1), UInt16(1), UInt32(1), UInt64(1))
            result = illumwhite(A, p)
            @test size(result) == (1, 3)
            @test eltype(result) == Float64
            @test result ≈ expected
        end
    end

    @testset "Mask 缺失数值类型" begin
        A = zeros(UInt8, 4, 4, 3)
        A[:, :, 1] .= 10
        A[:, :, 2] .= 20
        A[:, :, 3] .= 40
        A[1, 1, :] .= UInt8[200, 210, 220]
        expected = reshape(Float64[10, 20, 40] ./ 255, 1, 3)

        for T in (Int32, Int64, UInt8, UInt16)
            mask = ones(T, 4, 4)
            mask[1, 1] = zero(T)
            result = illumwhite(A; Mask=mask)
            @test size(result) == (1, 3)
            @test eltype(result) == Float64
            @test result ≈ expected
        end
    end
end
```

静态检验说明：以上代码只包含一个新的、独立的顶层 `@testset "illumwhite 补充" begin`，全部测试数据和期望值均在该测试块内重新构造，不引用原有测试块的局部变量。它复用测试文件已有的 `TyImageProcessing` 和 `Test` 顶层导入，可不经编辑整体追加到文件末尾；此处仅完成静态审查，未声称已经运行新增测试。

仍需 baseline 校准项：无。新增断言复用了原测试已经建立的两组人工图像结果：值为 1 的百分比应选中单个亮点，掩膜排除亮点后应返回各通道背景值。仍建议在目标 Julia 环境中实际运行完整测试文件，以确认所有无符号标量转换和掩膜元素类型均与当前实现兼容。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
illumwhite，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_illumwhite 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_illumwhite.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\illumwhite.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\AutomaticWhiteBalance\illumwhite.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
illumwhite，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：illumwhite
遗留缺陷：无
```
