# imbilatfilt 提测分析报告

> 任务：imbilatfilt（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_imbilatfilt.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageFilteringandEnhancement/ImageFiltering/Edge-PreservingFiltering/imbilatfilt.md`
> 单测：`test/im_filter/test_imbilatfilt.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-17 09:19 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 二维灰度图像 | UInt8、UInt16、Int16、Float32、Float64 | 大小为 m×n |
| I | 二维彩色图像 | UInt8、UInt16、Int16、Float32、Float64 | 大小为 m×n×3 |
| degreeOfSmoothing | 正数 | 未明确 Julia 类型，接受正实数 | 默认值取决于 I 的数据类型 |
| spatialSigma | 1（默认）、正数 | 未明确 Julia 类型，接受正实数 | 空间高斯平滑核标准差 |
| neighborhoodSize | 奇值正整数 | Int64 | 默认 2×ceil(2×spatialSigma)+1 |

### 文档自身缺陷审计

- `neighborhoodSize` 的语法说明写“使用关键字参数来更改双边筛选器的行为”，但语法和实现均为第四个位置参数，前后矛盾。
- `I` 文档声明数据类型为 UInt8、UInt16、Int16、Float32、Float64，但实现签名还接受 Int8、UInt32、Int32；原测试已覆盖这些类型，文档类型列表不完整。
- `degreeOfSmoothing` 仅说明默认值取决于图像数据类型，未给出具体公式或各数据类型默认值，约束描述不完整。
- `neighborhoodSize` 文档限为 Int64，但实现接受整数等价 Real，原测试中传入 `3.0` 未被拒绝，文档未说明该行为。

### 参数数据类型与结构覆盖

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 二维灰度 UInt8 | 常量、脉冲、边缘、大图 | test_imbilatfilt.jl / public overloads and grayscale behavior、threaded large-input branches | ☑ |
| 二维灰度 UInt16 | fixtures、separated、large_u16 | test_imbilatfilt.jl / element types and implementation paths、threaded large-input branches | ☑ |
| 二维灰度 Int16 | fixtures 中 Int16 灰度 | test_imbilatfilt.jl / element types and implementation paths | ☑ |
| 二维灰度 Float32 | fixtures、large_f32 | test_imbilatfilt.jl / element types and implementation paths、threaded large-input branches | ☑ |
| 二维灰度 Float64 | fixtures、corner | test_imbilatfilt.jl / element types and implementation paths、replicate padding and helpers | ☑ |
| 二维彩色 UInt8 | rgb_u8、large_rgb | test_imbilatfilt.jl / RGB filtering、threaded large-input branches | ☑ |
| 二维彩色 UInt16 | 未找到构造、调用、结果断言证据 | 无 | 未覆盖 |
| 二维彩色 Int16 | rgb_constant | test_imbilatfilt.jl / RGB filtering | ☑ |
| 二维彩色 Float32 | rgb_f32 | test_imbilatfilt.jl / RGB filtering | ☑ |
| 二维彩色 Float64 | 未找到构造、调用、结果断言证据 | 无 | 未覆盖 |

说明：二维彩色 Int16 覆盖来自常量图恒等测试，属于弱证据；二维彩色 UInt16、Float64 为完全缺失项。

#### degreeOfSmoothing

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 显式正实数 | 传入 1、25、0.5、0.6、10_000、100_000 等 | test_imbilatfilt.jl / public overloads and grayscale behavior、RGB filtering | ☑ |
| 默认值且 I 为 UInt8 | imbilatfilt(constant_u8) | test_imbilatfilt.jl / public overloads and grayscale behavior | ☑ |
| 默认值且 I 为 UInt16、Int16、Float32、Float64 | 未找到对应默认调用 | 无 | 未覆盖 |

说明：默认路径仅有 UInt8 证据，其他文档图像类型的默认 degreeOfSmoothing 未覆盖。

#### spatialSigma

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1 | imbilatfilt(constant_u8, 25) 等省略 spatialSigma 的调用 | test_imbilatfilt.jl / public overloads and grayscale behavior | ☑ |
| 显式正实数 | 0.6、0.8、1、2 等 | test_imbilatfilt.jl / public overloads and grayscale behavior、RGB filtering | ☑ |

#### neighborhoodSize

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Int64 奇正整数 | 1、3、5 显式传入 | test_imbilatfilt.jl / public overloads and grayscale behavior、validation | ☑ |
| 默认推导值 | 通过省略 neighborhoodSize 调用 | test_imbilatfilt.jl / public overloads and grayscale behavior | ☑ |
| 非法邻域值 | 0、-1、Inf、NaN、3.5、4、9、空图等 | test_imbilatfilt.jl / validation | ☑ |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维灰度矩阵 | UInt8、UInt16、Int16、Float32、Float64 | ☑ | 覆盖较强 |
| 二维彩色数组 m×n×3 | UInt8、Int16、Float32 | ☑ | Int16 为常量恒等弱证据 |
| 二维彩色数组 m×n×3 | UInt16、Float64 | 未覆盖 | 补测缺口 |
| 标量 Real 参数 degreeOfSmoothing | 显式正数和 UInt8 默认路径 | ☑ | 非 UInt8 默认路径未覆盖 |
| 标量 Real 参数 spatialSigma | 默认 1 和显式正数 | ☑ | 覆盖充分 |
| 标量 Int64 参数 neighborhoodSize | 奇正整数、默认推导、非法边界 | ☑ | 覆盖充分 |

### 覆盖缺口与优先级

高优先级：

- `I` 二维彩色 UInt16 和 Float64：文档声明类型，但测试中没有任何彩色 UInt16、Float64 构造、调用和断言；建议补测 7×8×3 彩色数组，并至少断言输出尺寸、元素类型和数值范围或有限性。
- `degreeOfSmoothing` 默认值对非 UInt8 文档类型：现有默认调用只有 `imbilatfilt(constant_u8)`，未验证 UInt16、Int16、Float32、Float64 的默认路径；建议补测这些类型的无第二参数调用。

中优先级：

- 二维彩色 Int16 覆盖来自常量图恒等测试，属于单点弱证据；建议补充非恒定彩色 Int16 场景。
- 现有常量灰度测试较多，单点或恒等结果证据偏弱；建议补充非恒定且可验证输出性质的样本。
- `neighborhoodSize` 的 Int64 覆盖主要依赖整数字面量，可补充显式 `Int64` 变量传参，但当前不属于明显缺口。

低优先级：

- `spatialSigma` 默认值和显式正数均有覆盖，缺口不明显。
- `I` 二维灰度类型的五类文档数据类型均有覆盖，缺口不明显。
- 非法参数边界测试已覆盖主要异常路径。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| I | 二维灰度 UInt8、二维灰度 UInt16、二维灰度 Int16、二维灰度 Float32、二维灰度 Float64、二维彩色 UInt8、二维彩色 Int16、二维彩色 Float32 | 二维灰度 UInt8、二维灰度 UInt16、二维灰度 Int16、二维灰度 Float32、二维灰度 Float64、二维彩色 UInt8、二维彩色 UInt16、二维彩色 Int16、二维彩色 Float32、二维彩色 Float64 | 二维彩色 UInt16、二维彩色 Float64 | 部分覆盖 |
| degreeOfSmoothing | 显式正实数、UInt8 默认路径 | 正数、UInt8 默认路径、非 UInt8 默认路径 | 非 UInt8 默认路径（UInt16、Int16、Float32、Float64） | 部分覆盖 |
| spatialSigma | 默认 1、显式正实数 | 默认 1、显式正实数 | 无 | 完全覆盖 |
| neighborhoodSize | Int64 奇正整数、默认推导、非法边界 | Int64 奇正整数、默认推导 | 无 | 完全覆盖 |

总计：完全覆盖参数数 2 个，即 `spatialSigma`、`neighborhoodSize`；至少有证据参数数 4 个，所有输入参数均至少存在部分证据。缺失项集中在 `I` 的彩色 UInt16、Float64，以及 `degreeOfSmoothing` 的非 UInt8 默认路径。

### 缺失测试代码补全

```julia
@testset "imbilatfilt 补充" begin
    @testset "文档声明但原测试未覆盖的二维彩色类型" begin
        rgb_u16 = zeros(UInt16, 7, 8, 3)
        rgb_u16[4, 4, :] .= UInt16[1000, 2000, 3000]
        rgb_u16[4, 3, :] .= UInt16[200, 400, 600]
        filtered_u16 = imbilatfilt(rgb_u16, 1_000_000, 1, 5)
        @test size(filtered_u16) == size(rgb_u16)
        @test eltype(filtered_u16) === UInt16
        @test all(x -> 0 <= x <= typemax(UInt16), filtered_u16)

        rgb_f64 = zeros(Float64, 7, 8, 3)
        rgb_f64[4, 4, :] .= [0.9, 0.8, 0.7]
        rgb_f64[4, 3, :] .= [0.1, 0.2, 0.3]
        filtered_f64 = imbilatfilt(rgb_f64, 0.5, 0.8, 3)
        @test size(filtered_f64) == size(rgb_f64)
        @test eltype(filtered_f64) === Float64
        @test all(isfinite, filtered_f64)
    end

    @testset "非 UInt8 文档类型的默认 degreeOfSmoothing 路径" begin
        gray_u16 = reshape(UInt16.(1:56), 7, 8)
        gray_i16 = Int16.(gray_u16 .- 30)
        gray_f32 = Float32.(gray_u16 ./ 56)
        gray_f64 = Float64.(gray_u16 ./ 56)
        for input in (gray_u16, gray_i16, gray_f32, gray_f64)
            result = imbilatfilt(input)
            @test size(result) == size(input)
            @test eltype(result) === eltype(input)
        end
    end
end
```

静态检验说明：该代码块只包含一个独立的顶层 `@testset`，全部数据在块内重新构造，未引用原测试块内部局部变量，可整体追加到 `test_imbilatfilt.jl` 文件的物理末尾；未插入、替换、删除或改写原文件中的任何代码。

仍需 baseline 校准项：新增测试仅验证输出类型、尺寸、有限性和整数范围，未给出精确滤波值；二维彩色 UInt16、Float64 以及非 UInt8 默认 degreeOfSmoothing 路径的真实输出仍需与 MATLAB 或已知真值基线对照校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
imbilatfilt，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_imbilatfilt 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_imbilatfilt.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\imbilatfilt.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ImageFiltering\Edge-PreservingFiltering\imbilatfilt.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
imbilatfilt，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：imbilatfilt
遗留缺陷：无
```
