# imbinarize 提测分析报告

> 任务：imbinarize（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_thresh\test_imbinarize.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/ImageTypeConversion/ConverttoBinaryImageUsingThresholding/imbinarize.md`
> 单测：`test/im_thresh/test_imbinarize.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-17 09:30 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 二维灰度图像、三维灰度图像体 | UInt8、UInt16、Int16、Float32、Float64 | 文档要求浮点类型像素值在 [0, 1] 范围内 |
| method | "global"（默认）、"adaptive" | UInt8、UInt16、Int16、Float32、Float64（应为字符串） | 文档数据类型误写为数值类型 |
| T | 数值标量、数值数组 | Real | 数值数组时文档未明确尺寸约束 |

### 文档自身缺陷审计

- method 参数的数据类型误写为 `UInt8、UInt16、Int16、Float32、Float64`，与方法实际使用的字符串值矛盾，应修正为字符串类型。
- 文档声明 I 可以为三维灰度图像体，但当前实现仅支持 `AbstractMatrix`，即二维矩阵，未提供三维体方法。
- 文档要求 Float32、Float64 图像像素值在 [0, 1] 范围内，但实现未强制校验；原测试还接受 `-1、0、1、2、NaN、Inf` 等越界或特殊值，文档约束与实现行为不一致。
- T 为数值数组时未在文档中说明必须与 I 尺寸一致，仅代码中通过 `DimensionMismatch` 异常体现。

### 参数数据类型与结构覆盖

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 二维矩阵 | UInt8 二维灰度图像 | test_imbinarize.jl、scalar thresholds use normalized image ranges、imbinarize(UInt8[0 127; 128 255], 0.5) | ☑ |
| UInt16 二维矩阵 | UInt16 二维灰度图像 | test_imbinarize.jl、scalar thresholds use normalized image ranges、imbinarize(UInt16[0 32767; 32768 65535], 0.5) | ☑ |
| Int16 二维矩阵 | Int16 二维灰度图像 | test_imbinarize.jl、scalar thresholds use normalized image ranges、imbinarize(Int16[-32768 -1; 0 32767], 0.5) | ☑ |
| Float32 二维矩阵 | Float32 二维灰度图像 | test_imbinarize.jl、scalar thresholds use normalized image ranges、imbinarize(Float32[0 0.5; 0.5001 1], 0.5) | ☑ |
| Float64 二维矩阵 | Float64 二维灰度图像 | test_imbinarize.jl、scalar thresholds use normalized image ranges、imbinarize(Float64[-1 0; 1 2], 1) | ☑ |
| 三维灰度图像体 | 文档声明支持三维体 | 无 | 未覆盖 |

说明：二维矩阵各数据类型均有构造、调用和结果断言，属强覆盖。三维体无任何测试证据，且当前实现不支持三维数组。

#### method

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 字符串 "global" 路径 | 默认、显式 "GLOBAL"、前缀 "g"、空字符串默认路径 | test_imbinarize.jl、global Otsu methods and supported classes、imbinarize(image, "GLOBAL")、method="g"、method="" | ☑ |
| 字符串 "adaptive" 路径 | 显式 "ADAPTIVE"、前缀 "a" | test_imbinarize.jl、adaptive method and validation、imbinarize(image, "ADAPTIVE"; Sensitivity=0.5)、method="a" | ☑ |

说明：method 文档数据类型误写为数值类型，已作为缺陷审计；本表按方法实际字符串语义核对。

#### T

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 | Float64 标量阈值 0.5、1 | test_imbinarize.jl、scalar thresholds use normalized image ranges | ☑ |
| 数值数组 | Float64 矩阵阈值，包含尺寸不匹配异常 | test_imbinarize.jl、per-pixel thresholds | ☑ |

说明：弱证据——原测试仅覆盖 Float64 类型的阈值标量和数组，未显式覆盖 Int、Float32 等其他 `Real` 子类型；补测代码已补充 Int 标量、Float32 标量和 Float32 数组。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维灰度图像矩阵 | I 的二维矩阵形式 | ☑ | UInt8、UInt16、Int16、Float32、Float64 均有测试 |
| 三维灰度图像体 | I 的三维体形式 | 未覆盖 | 文档声明但实现仅 AbstractMatrix |
| 数值标量 | T 的标量形式 | ☑ | 原测试仅 Float64，补测补 Int、Float32 |
| 数值数组 | T 的数组形式 | ☑ | Float64 矩阵，含尺寸不匹配异常 |

### 覆盖缺口与优先级

高优先级：

- 三维灰度图像体完全缺失：文档声明支持三维体，但实现没有 `AbstractArray{T,3}` 方法，测试也没有三维数组；建议扩展实现或修订文档后补测三维结果。
- method 数据类型文档错误：将 method 标注为数值类型，与字符串语义矛盾；应修正为字符串类型说明。
- Float32、Float64 像素范围约束未执行：文档要求 [0,1]，但实现接受越界、NaN、Inf；建议明确实现行为或增加校验，并补测范围异常。

中优先级：

- T 数值数组尺寸约束未写入文档：已有 `DimensionMismatch` 测试，但文档应补充“数组需与 I 同尺寸”。
- T 的 Real 类型覆盖样本单一：原测试仅使用 Float64 阈值；补测代码已补 Int、Float32，但仍建议补充更多 Real 类型。
- method 完整小写 "global"、"adaptive" 的直接断言较少：原测试多使用前缀或大小写变体；补测代码已补完整小写形式。

低优先级：

- 三维体之外的更多数组形状覆盖有限：原测试包含 2×2、2×3 和非方阵，但不是所有类型、所有形状。
- 空图像仅覆盖 UInt8；其他类型的空矩阵边界可补但不紧急。
- 自适应方法仅用常数图像断言，缺少一般图像的 baseline 校准。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| I | UInt8 二维矩阵、UInt16 二维矩阵、Int16 二维矩阵、Float32 二维矩阵、Float64 二维矩阵 | 二维灰度图像、三维灰度图像体、UInt8、UInt16、Int16、Float32、Float64 | 三维灰度图像体 | 部分覆盖 |
| method | "global" 路径、"adaptive" 路径 | "global"、"adaptive" | 无 | 完全覆盖 |
| T | 数值标量、数值数组 | 数值标量、数值数组、Real | 无 | 完全覆盖 |

注：method 文档数据类型误写已按实际字符串语义修正，不计入缺失项。

总计：完全覆盖参数数 2；至少有一项覆盖证据参数数 3。

### 缺失测试代码补全

```julia
@testset "imbinarize 补充" begin
    @testset "Real 阈值类型补充" begin
        @test imbinarize(UInt8[0 127; 128 255], 0) == Bool[0 1; 1 1]
        @test imbinarize(UInt8[0 127; 128 255], 1) == Bool[0 0; 0 0]
        @test imbinarize(UInt8[0 127; 128 255], Float32(0.5)) == Bool[0 0; 1 1]

        thresholds32 = Float32[0.0 0.5; 0.75 1.0]
        @test imbinarize(UInt8[1 128; 192 255], thresholds32) == Bool[1 1; 1 0]
    end

    @testset "method 完整小写形式" begin
        image = UInt8[0 0; 255 255]
        @test imbinarize(image, "global") == Bool[0 0; 1 1]
        @test imbinarize(fill(UInt8(100), 3, 3); method="adaptive", Sensitivity=0.5) ==
            falses(3, 3)
    end

    @testset "三维体当前限制" begin
        @test_throws MethodError imbinarize(zeros(UInt8, 2, 2, 2))
    end
end
```

静态检验说明：该代码块作用域自足，未引用原测试块内部局部变量，所需数据均在新增块内重新构造；整块可原样追加到 `test_imbinarize.jl` 文件末尾，不需要插入或改写任何现有测试。新增代码未实际运行验证。

仍需 baseline 校准项：

- 三维体异常测试仅为当前实现限制的锁定；若后续实现支持三维体，应替换为三维结果断言并补充文档覆盖。
- 自适应方法补测沿用已有常数图像预期，不属于一般图像 baseline；一般图像的自适应阈值结果仍需算法 baseline 校准。
- Real 阈值类型补充中的 Int 阈值预期基于当前归一化比较实现，若实现语义改变需重新校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
imbinarize，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_imbinarize 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_thresh\test_imbinarize.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\imbinarize.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\ImageTypeConversion\ConverttoBinaryImageUsingThresholding\imbinarize.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
imbinarize，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：imbinarize
遗留缺陷：无
```
