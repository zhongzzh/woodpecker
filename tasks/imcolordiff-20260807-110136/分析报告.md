# imcolordiff 提测分析报告

> 任务：imcolordiff（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_space\test_imcolordiff.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_imcolordiff）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/Import,Export,andConversion/Color/MeasureColorDifferences/imcolordiff.md`
> 单测：`test/im_space/test_imcolordiff.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\MeasureColorDifferences\imcolordiff.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析）＋用户粘贴性能报告分析 ｜ 生成：2026-08-07 11:06 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I1 | 1×3 数值向量、c×3 数值矩阵、m×n×3 数值数组、多维数值数组 | Float32、Float64、UInt8、UInt16 | 第一组颜色数据；第三维为 3 个颜色通道 |
| I2 | 1×3 数值向量、c×3 数值矩阵、m×n×3 数值数组、多维数值数组 | Float32、Float64、UInt8、UInt16 | 第二组颜色数据；第三维为 3 个颜色通道 |
| Standard | `"CIE94"`、`"CIEDE2000"` | AbstractString | CIE 标准，默认值为 `"CIE94"` |
| isInputLab | `false`、`true` | Bool | 指示输入是否已经处于 L\*a\*b\* 色彩空间 |
| kL | 默认值 1、数值标量 | Number | 亮度系数 |
| kC | 默认值 1、数值标量 | Number | 色度补偿系数 |
| kH | 默认值 1、数值标量 | Number | 色调补偿系数 |
| K1 | 默认值 0.045、数值标量 | Number | K1 加权因子 |
| K2 | 默认值 0.015、数值标量 | Number | K2 加权因子 |

### 文档自身缺陷审计

- 文档将 I1、I2 的允许类型限定为 Float32、Float64、UInt8、UInt16，但代码实际还允许 Int64；文档存在类型遗漏。
- 文档对 L\*a\*b\* 输入写明只能是 Float32 或 Float64，与代码允许 Int64 不一致。
- 文档称 K1、K2 仅适用于 CIE94，但示例在 `Standard="CIEDE2000"` 时传入 K1、K2，前后约束不一致；代码也没有针对标准限制这两个参数。
- 文档中的参数锚点为 `isLab`，实际参数名称为 `isInputLab`。
- 语法示例中的 `imcolordiff（I1，I2，Standard=“CIEDE2000”）` 使用了全角括号、逗号和引号，不是可直接执行的 Julia 语法。
- 文档只说明参数是“数值标量”，没有说明代码实际要求 kL、kC、kH、K1、K2 必须大于 0。
- 输出类型说明与实现的快速路径可能不一致：代码对 UInt8、UInt16 输入的快速路径返回 Float64，而文档称非 Float64 输入时输出为 Float32。
- 示例 3 给出了 `7.444409565678752`，但测试没有对该数值进行结果断言，文档示例真值与实现的一致性未被测试验证。

### 参数数据类型与结构覆盖

#### I1

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | 1×1×3 RGB 数组和 1×1×3 L\*a\*b\* 数组 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `rgb64`、`lab64` 的调用 | ☑ |
| Float32 | 1×2×3 RGB 数组 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `rgb32` 的调用 | ☑ |
| UInt8 | 1×3 颜色向量、图像数据 | `test_imcolordiff.jl`，`src` 中 `pureRed`；图像调用中的加载结果类型未断言 | ☑ |
| UInt16 | 文档声明支持的整数类型 | 未找到 UInt16 的 I1 构造、调用和结果断言 | 未覆盖 |
| 1×3 数值向量 | 参考颜色结构 | `test_imcolordiff.jl`，`src` 中 `pureRed` 传入目标函数 | ☑ |
| c×3 数值矩阵 | 多颜色集合结构，需有超过一行的公开函数调用证据 | 仅有内部函数 `imcolordiff_fast_path` 的 `ct1` 调用，不是目标函数公开调用 | 未覆盖 |
| m×n×3 数值数组 | 彩色图像结构 | `test_imcolordiff.jl`，`src` 中图像调用；`validation and fallback coverage` 中 `rgb32` 调用 | ☑ |
| 多维数值数组，例如 m×n×3×p | 批量彩色图像结构 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `lab64_4d` 调用 | ☑ |

说明：`src` 中图像加载结果没有对元素类型作明确断言，因此只能确认这些图像结构参与了调用，不能据此确认其具体元素类型。`pureRed` 是显式构造的 1×3 UInt8 矩阵，可作为 1×3 向量形式的弱结构证据。

#### I2

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float64 | 1×1×3 RGB 数组和 1×1×3 L\*a\*b\* 数组 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `rgb64b`、`lab64b` 的调用 | ☑ |
| Float32 | 1×2×3 RGB 数组 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `rgb32b` 的调用 | ☑ |
| UInt8 | 1×3 颜色向量、图像数据 | `test_imcolordiff.jl`，`src` 中 `darkRed`；图像调用中的加载结果类型未断言 | ☑ |
| UInt16 | 文档声明支持的整数类型 | 未找到 UInt16 的 I2 构造、调用和结果断言 | 未覆盖 |
| 1×3 数值向量 | 参考颜色结构 | `test_imcolordiff.jl`，`src` 中 `darkRed` 传入目标函数 | ☑ |
| c×3 数值矩阵 | 多颜色集合结构，需有超过一行的公开函数调用证据 | 仅有内部函数调用中的颜色表数据，不是目标函数公开调用 | 未覆盖 |
| m×n×3 数值数组 | 彩色图像结构 | `test_imcolordiff.jl`，`src` 中图像调用；`validation and fallback coverage` 中 `rgb32b` 调用 | ☑ |
| 多维数值数组，例如 m×n×3×p | 批量彩色图像结构 | `test_imcolordiff.jl`，`validation and fallback coverage` 中 `lab64_4db` 调用 | ☑ |

说明：图像加载结果没有元素类型断言，不能猜测其为 Float32、Float64 或其他具体类型。`darkRed` 是显式构造的 1×3 UInt8 矩阵，能够证明该结构参与了调用，但不是多行 c×3 颜色表。

#### Standard

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `"CIE94"` | 默认标准路径被执行，但没有显式传入该值 | `test_imcolordiff.jl`，`src` 中未传 `Standard` 的多次调用 | ☑ |
| `"CIEDE2000"` | 显式名称-值调用 | `test_imcolordiff.jl`，`src` 中 pureRed 和 fabric 调用；`validation and fallback coverage` 中 RGB 和 4D 调用 | ☑ |

说明：`"CIE94"` 只有默认路径证据，不能证明显式传入 `"CIE94"` 的名称-值调用路径。

#### isInputLab

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `false` | 默认 RGB 输入路径 | `test_imcolordiff.jl`，`src` 中 RGB 图像、颜色和 fabric 调用；`validation and fallback coverage` 中 RGB 调用 | ☑ |
| `true` | 显式 L\*a\*b\* 输入路径 | `test_imcolordiff.jl`，`src` 中 `imcolordiff(lab, lab2; isInputLab=true)`；`validation and fallback coverage` 中 4D Lab 调用 | ☑ |

#### kL

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1 | 默认亮度系数路径 | `test_imcolordiff.jl`，`src` 中未传 `kL` 的调用 | ☑ |
| 非默认数值标量 | 显式整数标量 2 | `test_imcolordiff.jl`，`src` 中 fabric 调用的 `kL=2`；验证块中的 `kL=0` 异常调用 | ☑ |

说明：文档只公开声明 Number，现有测试显式覆盖了整数标量，但没有覆盖其他 Number 子类型。

#### kC

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1 | 默认色度系数路径 | `test_imcolordiff.jl`，`src` 和验证块中未传 `kC` 的调用 | ☑ |
| 非默认数值标量 | 文档允许的显式数值标量 | 仅有 `kC=0` 异常调用，没有有效非默认值调用 | 未覆盖 |

说明：`kC=0` 只能证明无效边界会触发异常，不能证明有效的非默认数值标量路径。

#### kH

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1 | 默认色调系数路径 | `test_imcolordiff.jl`，`src` 和验证块中未传 `kH` 的调用 | ☑ |
| 非默认数值标量 | 文档允许的显式数值标量 | 仅有 `kH=0` 异常调用，没有有效非默认值调用 | 未覆盖 |

说明：`kH=0` 只覆盖无效值验证，不能证明有效非默认数值标量。

#### K1

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 0.045 | 默认 K1 路径 | `test_imcolordiff.jl`，`src` 和验证块中未传 `K1` 的调用 | ☑ |
| 非默认数值标量 | 显式 Float64 标量 0.048 | `test_imcolordiff.jl`，`src` 中 fabric 调用的 `K1=0.048`；验证块中的 `K1=0` 异常调用 | ☑ |

#### K2

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 0.015 | 默认 K2 路径 | `test_imcolordiff.jl`，`src` 和验证块中未传 `K2` 的调用 | ☑ |
| 非默认数值标量 | 显式 Float64 标量 0.014 | `test_imcolordiff.jl`，`src` 中 fabric 调用的 `K2=0.014`；验证块中的 `K2=0` 异常调用 | ☑ |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 1×3 数值向量 | 单个参考颜色 | ☑ | 通过显式 UInt8 颜色数据覆盖，严格说 Julia 表达式为 1×3 矩阵 |
| c×3 数值矩阵 | 多个颜色组成的颜色表 | 未覆盖 | 只有内部快速路径调用，没有目标函数公开调用 |
| m×n×3 数值数组 | 单张彩色图像 | ☑ | RGB Float32 小数组和图像调用均有证据 |
| m×n×3×p 多维数组 | 批量彩色图像 | ☑ | 4D Float64 Lab 数组有尺寸断言 |
| UInt16 数值数组 | 文档声明的整数元素类型 | 未覆盖 | I1、I2 均未发现 UInt16 公开调用 |
| L\*a\*b\* 数值数组 | `isInputLab=true` 的输入结构 | ☑ | 有 Float64 Lab 3D、4D 调用和结果尺寸断言 |

### 覆盖缺口与优先级

#### 高优先级

- I1 的 UInt16 输入未覆盖。现有测试只构造了 Float64、Float32 和 UInt8；建议构造 UInt16 颜色表或 3D RGB 数组，调用目标函数并断言结果类型或形状。
- I2 的 UInt16 输入未覆盖。原因同上；建议让 I1、I2 都使用 UInt16，避免只验证单侧类型。
- I1 的 c×3 多颜色矩阵未覆盖。现有 `ct1` 只调用内部 `imcolordiff_fast_path`，不能替代公开函数测试；建议通过 `imcolordiff` 直接传入至少两行的 c×3 矩阵，并断言 c×1 结果形状。
- I2 的 c×3 多颜色矩阵未覆盖。建议与 I1 同时使用两行或更多行的颜色表，验证逐行结果。

#### 中优先级

- kC 的有效非默认数值标量未覆盖。已有 `kC=0` 仅证明异常边界；建议使用正的非默认值并断言结果为有限实数。
- kH 的有效非默认数值标量未覆盖。已有 `kH=0` 仅证明异常边界；建议使用正的非默认值并断言结果为有限实数。
- `"CIE94"` 的显式名称-值调用未覆盖。现有证据来自省略 `Standard` 的默认路径；建议显式传入 `Standard="CIE94"` 并断言结果形状或类型。
- 文档声明的输出类型规则需要增加实现基线或类型断言测试，尤其是 UInt8、UInt16 快速路径返回类型与文档描述不一致的情况。

#### 低优先级

- kL、kC、kH、K1、K2 的其他 Number 子类型未覆盖。文档只写 Number，建议在确认 API 预期后补充 Float32 或其他允许的数值标量。
- CIEDE2000 的公开参考真值尚未在测试中断言。可使用文档示例值或标准参考数据，但需要先确认实现公式、输入缩放和数值精度。
- 可增加名称-值参数组合及参数顺序测试，以验证文档所述“顺序不重要”的调用约束；当前测试只覆盖了有限组合。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| I1 | Float32、Float64、UInt8、1×3 数值向量、m×n×3 数值数组、多维数值数组 | Float32、Float64、UInt8、UInt16、1×3 数值向量、c×3 数值矩阵、m×n×3 数值数组、多维数值数组 | UInt16；c×3 数值矩阵 | 部分覆盖 |
| I2 | Float32、Float64、UInt8、1×3 数值向量、m×n×3 数值数组、多维数值数组 | Float32、Float64、UInt8、UInt16、1×3 数值向量、c×3 数值矩阵、m×n×3 数值数组、多维数值数组 | UInt16；c×3 数值矩阵 | 部分覆盖 |
| Standard | `"CIE94"`、`"CIEDE2000"` | `"CIE94"`、`"CIEDE2000"` | 无 | 完全覆盖 |
| isInputLab | `false`、`true` | `false`、`true` | 无 | 完全覆盖 |
| kL | 默认值 1、非默认数值标量 2 | 默认值 1、数值标量 | 无 | 完全覆盖 |
| kC | 默认值 1 | 默认值 1、数值标量 | 有效非默认数值标量 | 部分覆盖 |
| kH | 默认值 1 | 默认值 1、数值标量 | 有效非默认数值标量 | 部分覆盖 |
| K1 | 默认值 0.045、非默认数值标量 0.048 | 默认值 0.045、数值标量 | 无 | 完全覆盖 |
| K2 | 默认值 0.015、非默认数值标量 0.014 | 默认值 0.015、数值标量 | 无 | 完全覆盖 |

总计：完全覆盖参数数为 6；至少有证据参数数为 9。这里不将弱证据、默认路径、单点样本或缺少精确数值真值单独折算为缺失项。

### 缺失测试代码补全

```julia
@testset "imcolordiff 补充覆盖" begin
    uint16_table1 = UInt16[65535 0 0; 0 65535 0]
    uint16_table2 = UInt16[0 0 65535; 65535 65535 0]

    uint16_table_de = imcolordiff(
        uint16_table1,
        uint16_table2;
        Standard="CIE94",
        kC=1.25,
        kH=1.15,
    )
    @test size(uint16_table_de) == (2, 1)
    @test all(isfinite, uint16_table_de)

    uint16_vector1 = UInt16[65535 0 0]
    uint16_vector2 = UInt16[65535 4096 8192]
    uint16_vector_de = imcolordiff(uint16_vector1, uint16_vector2)
    @test uint16_vector_de isa Real
    @test isfinite(uint16_vector_de)

    rgb64_1 = reshape([0.1, 0.2, 0.3], 1, 1, 3)
    rgb64_2 = reshape([0.15, 0.25, 0.35], 1, 1, 3)
    explicit_cie94_de = imcolordiff(rgb64_1, rgb64_2; Standard="CIE94")
    @test explicit_cie94_de isa Real
    @test isfinite(explicit_cie94_de)
end
```

静态检验说明：以上代码只包含一个独立的顶层 `@testset "imcolordiff 补充覆盖"`，其变量均在该测试块内部构造，未引用原有测试块的局部变量；代码块应整体追加到文件末尾，可原样追加到文件末尾。此处仅进行了静态审计，未声称已经运行这些新增测试。

仍需 baseline 校准项：新增测试只断言类型、形状和有限性，不需要编造精确 expected。若要验证 CIE94、UInt16 快速路径或 kC、kH 非默认值的数值正确性，仍需使用标准 CIE 参考数据或已验证的 baseline 进行校准。

## 二、性能测试判定（AI，仅当前函数）

### 用户粘贴的性能报告分析

#### 数据完整性

已找到当前函数 `imcolordiff` 的性能数据。无表头六数值字段完整，首次与二次耗时比复算结果的相对误差均不超过 `1e-4`，校验通过，无缺失字段。

#### 可判定项

| 阶段 | Julia 用时（秒） | MATLAB 2025b 用时 T（秒） | 报告耗时比 x | 复算耗时比 | 阈值 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| 首次 | 0.152984 | 0.725602 | 0.210837346 | 0.2108373460933129 | 1.25 | 通过 |
| 二次 | 0.001273 | 0.054803 | 0.023228655 | 0.023228655365582176 | 1.5 | 通过 |

#### 综合结论

首次通过，二次通过。

**性能结论：性能通过**

## 三、总结邮件格式（可直接复制）

```text
imcolordiff，功能验证通过，性能通过，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_imcolordiff 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_space\test_imcolordiff.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\imcolordiff.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\Color\MeasureColorDifferences\imcolordiff.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
imcolordiff，功能单点验证通过，性能验证通过
测试环境：windows
测试项：imcolordiff
遗留缺陷：无
```
