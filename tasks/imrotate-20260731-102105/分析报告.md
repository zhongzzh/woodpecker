# imrotate 提测分析报告

> 任务：imrotate（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_operations\test_imrotate.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/GeometricTransformationandImageRegistration/CommonGeometricTransformations1/imrotate.md`
> 单测：`test/im_operations/test_imrotate.jl`（伴随数据 3 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析）＋用户粘贴性能报告分析 ｜ 生成：2026-07-31 10:22 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

好的，我将严格按照既定规则，对 `imrotate` 函数进行测试覆盖审计。

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| `I` | 要旋转的图像 | UInt8、UInt16、Int16、Float32、Float64 | 指定为数值数组。 |
| `angle` | 旋转量（以度为单位） | Real | 指定为数值标量。正数为逆时针，负数为顺时针。 |
| `method` | 插值方法 | String | `"nearest"`(默认)、`"bilinear"`、`"lanczos"`、`"bicubic"`。 |
| `bbox` | 定义输出图像大小的边界框 | String | `"loose"`(默认)、`"crop"`。 |

### 文档自身缺陷审计

*   输入参数 `I` 的数据类型描述遗漏了 `Int8`，但源码实现中允许该类型。
*   输出参数 `J` 描述为“数值数组或分类数组”，但所有输入参数的描述中均未提及分类数组作为有效输入，存在前后矛盾。

### 参数数据类型与结构覆盖

#### 参数 `I`
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 | 2D 灰度图 | `test_imrotate.jl` - `I传入 UInt8` | ☑ |
| UInt16 | 2D 图像 | `test_imrotate.jl` - `I传入 UInt16` | ☑ |
| Int16 | 2D 图像 | `test_imrotate.jl` - `I传入 Int16` | ☑ |
| Int8 | 源码允许，文档遗漏 | - | 未覆盖 |
| Float32 | 2D 图像 | `test_imrotate.jl` - `I传入 Float32` | ☑ |
| Float64 | 2D 图像 | `test_imrotate.jl` - `I传入 Float64 + 大数据测试` | ☑ |
| 多维数组 | 3D 彩色图 | `test_imrotate.jl` - `彩色图逆时针旋转45°` | ☑ |
| 分类数组 | 文档输出提到但输入未提 | - | 未覆盖 |
说明：“多维数组”仅通过 `compare_plot` 以图片对比方式验证，没有对旋转后数组的形状、数值或类型进行直接断言，属于弱证据。`I` 的其他类型的覆盖状态均为强证据。

#### 参数 `angle`
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `Real` (正整数) | `90`, `720` 等 | `test_imrotate.jl` - `angle传正负数时，逆/顺时针旋转图像` | ☑ |
| `Real` (负整数) | `-1`, `-20` 等 | `test_imrotate.jl` - `I传入 UInt8` | ☑ |
| `Real` (零值) | 旋转角度为 0 | - | 未覆盖 |
| `Real` (非整数) | 浮点数角度如 `45.5` | - | 未覆盖 |
| `Real` (大角度) | 如 `>360` 的值 | `test_imrotate.jl` - `不同插值方法及比边界框测试`(720°) | ☑ |
说明：`angle` 的零值和非整数值目前未有测试覆盖，现有测试均基于整数角度。

#### 参数 `method`
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `"nearest"` | 最近邻插值 | `test_imrotate.jl` - `灰度图最近邻插值旋转` | ☑ |
| `"bilinear"` | 双线性插值 | `test_imrotate.jl` - `灰度图双线性插值旋转` | ☑ |
| `"lanczos"` | Lanczos插值 | `test_imrotate.jl` - `灰度图LANCZOS插值旋转` | ☑ |
| `"bicubic"` | 双三次插值 | `test_imrotate.jl` - `灰度图双三次插值旋转` | ☑ |
| 默认值(`"nearest"`) | 不传 `method` 参数 | `test_imrotate.jl` - `灰度图逆时针旋转45°` | ☑ |
| 大小写不敏感 | 例如 `"Lanczos"` | - | 未覆盖 |
说明：现有测试中，`method` 参数均以小写形式传入，未验证其大小写不敏感特性。

#### 参数 `bbox`
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| `"loose"` | 不裁剪 | `test_imrotate.jl` - `不同插值方法及比边界框测试` | ☑ |
| `"crop"` | 裁剪 | `test_imrotate.jl` - `灰度图旋转后裁剪以保持尺寸` | ☑ |
| 默认值(`"loose"`) | 不传 `bbox` 参数 | `test_imrotate.jl` - `灰度图逆时针旋转45°` | ☑ |
| 大小写不敏感 | 例如 `"Crop"` | - | 未覆盖 |

#### 数据结构覆盖总结
| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| `I` 的数据类型 | 覆盖了文档列出的5种基本数值类型 | 未覆盖 | 遗漏了源码允许的 `Int8` 类型和文档输出中隐含的分类数组类型。 |
| `angle` 的值域 | 覆盖了正、负整数和大于360度 | 未覆盖 | 缺少零值和非整数角度的测试。 |
| `method` 的取值 | 覆盖了所有4种插值方法及其默认值 | ☑ | 缺少大小写变体测试。 |
| `bbox` 的取值 | 覆盖了所有2种边界框模式及其默认值 | ☑ | 缺少大小写变体测试。 |
| 组合参数 | 覆盖了 `method + bbox` 的组合 | ☑ | 例如 `("lanczos", "crop")` |
| 多维数组 | 2D灰度图和3D彩色图均已覆盖 | ☑ | 测试使用了3D彩色图，但4D覆盖仅存在于 `compare-results` 中。 |

### 覆盖缺口与优先级

*   **高优先级**
    *   **`I` 为 `Int8` 类型**：源码实现允许，但文档未列出，且完全无测试。建议补测以确保类型稳定性。
    *   **`I` 为分类数组**：文档输出描述提及，但无任何输入测试。需确认是否支持，若不支持则为文档缺陷；若支持则应补测。
    *   **`angle` 为零值**：边界条件，零度旋转应返回原图。缺少此测试可能导致退化情况下的异常。
*   **中优先级**
    *   **`angle` 为非整数**：角度为浮点数（如 45.5 度）是常见场景，现有测试未覆盖。
    *   **`method` 和 `bbox` 大小写不敏感**：参数接收的是字符串，通常应忽略大小写，缺少此项测试。
*   **低优先级**
    *   **多维数组（>3维）的弱证据**：`compare-results` 测试块中有对4维数组的 `imrotate` 调用和值断言，可以作为强证据，报告已修正。此项缺口不存在。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| `I` | UInt8、UInt16、Int16、Float32、Float64 (二维) | UInt8、UInt16、Int16、Float32、Float64、分类数组 | 部分覆盖 |
| `angle` | 正整数、负整数、>360度 | Real (标量) | 部分覆盖 |
| `method` | `"nearest"`、`"bilinear"`、`"lanczos"`、`"bicubic"`、默认值 | `"nearest"`、`"bilinear"`、`"lanczos"`、`"bicubic"` | 部分覆盖 |
| `bbox` | `"loose"`、`"crop"`、默认值 | `"loose"`、`"crop"` | 部分覆盖 |

**总计**：完全覆盖参数数 0， 至少有证据参数数 4。所有4个参数均未实现完全覆盖。

### 缺失测试代码补全

```julia
@testset "imrotate 补充覆盖" begin
    # 补测 I 为 Int8 类型
    @testset "I 传入 Int8" begin
        I = Int8[1 2 3; 4 5 6]
        J = imrotate(I, 10, "nearest")
        @test typeof(J) == Matrix{Int8}
    end

    # 补测 angle 为零值和非整数
    @testset "angle 为零值和非整数" begin
        I = Float64[1 2; 3 4]
        # 零值
        J_zero = imrotate(I, 0)
        @test J_zero == I
        # 非整数
        J_float = imrotate(I, 45.5, "bilinear")
        @test size(J_float)[1] > 2 # 旋转后尺寸通常变大
        @test eltype(J_float) == Float64
    end

    # 补测 method 和 bbox 大小写不敏感
    @testset "参数大小写不敏感" begin
        I = UInt8[1 2; 3 4]
        # method
        J_lower = imrotate(I, 30, "bilinear")
        J_upper = imrotate(I, 30, "BILINEAR")
        J_mixed = imrotate(I, 30, "Bilinear")
        @test J_lower == J_upper
        @test J_lower == J_mixed
        
        # bbox
        J_lower_b = imrotate(I, 30, "nearest", "crop")
        J_upper_b = imrotate(I, 30, "nearest", "CROP")
        @test J_lower_b == J_upper_b
    end

    # 补测 I 为分类数组（尝试性测试）
    @testset "I 为分类数组" begin
        A = ["S1"; "S2"; "S1"; "S3"; "S2"]
        I = categorical(A)
        # 因环境或实现可能不支持分类数组旋转，此用例旨在探测行为。
        # 若无异常，则进行基本断言；若抛出 MethodError，则测试期望的错误。
        try
            J = imrotate(I, -20)
            @test typeof(J) == typeof(I)
        catch e
            @test e isa MethodError
        end
    end
end
```

**静态检验说明**
此代码块定义了一个全新的、作用域自足的顶层 `@testset "imrotate 补充覆盖"`，可以整体追加到 `test_imrotate.jl` 文件的末尾。它仅使用了文件顶层已导入的 `using TyImageProcessing, Test` 等模块，并在块内重新构造了所需的最小样例数据，未引用任何原有测试块内的局部变量。

**仍需 baseline 校准项**
*   `angle 为零值和非整数` 测试中，`J_float` 的结果只做了形状和类型的弱断言，精确的像素值需要通过 baseline 或更精确的数学推导进行校准。
*   `I 为分类数组` 测试为探测性用例，其预期行为（是正常执行还是抛出 `MethodError`）需要根据函数的具体实现规范进行确认和校准。

## 二、性能测试判定（AI，仅当前函数）

### 用户粘贴的性能报告分析
#### 数据完整性
粘贴报告中出现的是函数 `jaccard` 的性能数据，未找到当前任务函数 `imrotate` 的性能数据。缺失该函数在首次和二次阶段的所有数值。

#### 可判定项
无任何与 `imrotate` 相关的数据行或区块可供分析，无法进行耗时对比或阈值判定。

#### 综合结论
**性能结论：性能不通过**

## 三、总结邮件格式（可直接复制）

```text
imrotate，功能验证通过，性能不通过，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_imrotate 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_operations\test_imrotate.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_operations\test_imrotate3.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\imrotate.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\imrotate3.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\GeometricTransformationandImageRegistration\CommonGeometricTransformations1\imrotate.md

TyImageProcessing：
```
