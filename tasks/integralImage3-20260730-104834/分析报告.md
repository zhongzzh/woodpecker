# integralImage3 提测分析报告

> 任务：integralImage3函数（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_integralImage3.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageFilteringandEnhancement/ImageFiltering/IntegralImageDomainFiltering/integralImage3.md`
> 单测：`test/im_math/test_integralImage3.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析）＋用户粘贴性能报告分析 ｜ 生成：2026-07-30 10:50 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 三维数值数组 | Float32、Float64、Int8、Int16、Int32、UInt8、UInt16、UInt32 | 灰度体积 |

### 文档自身缺陷审计

- 允许数据类型列表中未包含 `Int64`，但示例中构建的 `I` 为 `Array{Int64,3}`。
- 描述为“三维数值数组”，但实际函数可接受一维向量和二维矩阵并返回特定形状的积分体积，该行为未在文档中说明。
- 未说明空数组输入的行为（实现中会返回 `Float64[]`）。
- 对于非三维输入，输出尺寸并非简单的 `size(I) + 1`，文档中关于输出尺寸的描述仅适用于三维情形，缺少对低维输入的说明。

### 参数数据类型与结构覆盖

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Float32 | 三维数组 | test_integralImage3.jl → `@testset "J = integralImage3(I)"` 中 `rand(Float32,2,3,4)` 的构造、调用与断言 | ☑ |
| Float64 | 三维数组 | 同上，`rand(Float64,2,3,4)` | ☑ |
| Int8 | 三维数组 | 同上，`rand(Int8,2,3,4)` | ☑ |
| Int16 | 三维数组 | 同上，`rand(Int16,2,3,4)` | ☑ |
| Int32 | 三维数组 | 同上，`rand(Int32,2,3,4)` | ☑ |
| UInt8 | 三维数组 | 同上，`rand(UInt8,2,3,4)` | ☑ |
| UInt16 | 三维数组 | 同上，`rand(UInt16,2,3,4)` | ☑ |
| UInt32 | 三维数组 | 同上，`rand(UInt32,2,3,4)` | ☑ |
| 三维数组结构 | 输入必须是三维数组 | 上述所有测试及 `@testset "src"` 和 `@testset "numeric regression"` 中均构造了三维数组并断言 `size(J) == size(I) .+ 1` | ☑ |

说明：上述所有数据类型的覆盖证据均为“构造或赋值、调用、结果断言”齐全，但断言仅包含输出尺寸和元素类型，未对各数据类型单独验证积分结果的数值正确性（弱证据）。`numeric regression` 测试块仅针对 `Int64` 三维输入验证了数值真值，未覆盖文档声明的其余 8 种类型。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 三维数组 | 灰度体积，尺寸任意 | ☑ | 测试覆盖了多种尺寸与数据类型，但数值正确性只对 Int64 做过强验证 |

### 覆盖缺口与优先级

**高优先级**

无。文档所列的全部类型与结构均有测试用例，未出现完全缺失的情况。

**中优先级**

- 文档声明的 8 种数据类型仅有弱证据（尺寸与元素类型断言），缺少针对每种数据类型的积分数值正确性验证。建议补充使用累积和公式 `cumsum(cumsum(cumsum(Float64.(I); dims=1); dims=2); dims=3)` 填充生成的期望值进行精确比对。
- 文档缺失 `Int64` 类型说明，导致该常用类型的支持未在规格中体现，应更新文档。

**低优先级**

- 空三维数组（如 `zeros(0,0,0)`）的行为未在测试中覆盖，文档也未描述。实现中空数组返回 `Float64[]`，可补充边界测试以提升健壮性。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| I | 9（8 种数据类型 + 三维数组结构） | 9 | 完全覆盖（弱证据） |

总计：完全覆盖参数数 1，至少有证据参数数 1（所有文档要求项均有弱证据覆盖）。

### 缺失测试代码补全

```julia
@testset "integralImage3 补充覆盖" begin
    # 对文档声明的每种数据类型补充数值正确性验证
    # 使用累积和公式生成基准真值，并与积分图像输出进行逐元素比对
    for T in (Float32, Float64, Int8, Int16, Int32, UInt8, UInt16, UInt32)
        I = rand(T, 3, 4, 5)
        J = integralImage3(I)
        # 按文档描述的等价变换构建期望值
        expected = zeros(Float64, size(I) .+ 1)
        expected[2:end, 2:end, 2:end] = cumsum(
            cumsum(cumsum(Float64.(I); dims=1); dims=2); dims=3)
        # 整数型输入累加结果完全精确；浮点型输入可能累积误差，使用近似比较
        if T <: AbstractFloat
            @test J ≈ expected
        else
            @test J == expected
        end
    end

    # 空三维数组边界测试（输入为空，应返回 Float64[]）
    I_empty = zeros(Int, 0, 0, 0)
    J_empty = integralImage3(I_empty)
    @test J_empty == Float64[]
    @test eltype(J_empty) == Float64
end
```

静态检验说明：该代码块作用域自足，仅依赖顶层 `using Test, TyImageProcessing`；内部所有变量均独立构造，不引用任何原有测试块中的局部变量。整体追加到 `test_integralImage3.jl` 文件末尾即可，无需编辑原有代码。

仍需 baseline 校准项：浮点类型的数值比较使用了 `≈`（默认 `isapprox`），若累积求和顺序与实现内部循环顺序有差异，可能产生微小浮点舍入误差，建议在实际运行后确认容差是否合理；目前容差采用 `isapprox` 默认值，若出现失败可适当放宽或改用更精确的顺序匹配。

## 二、性能测试判定

### 用户粘贴的性能报告分析

> ⚠️ **性能测试 AI 分析失败**
>
> 已固定使用当前 AI 配置，调用失败时最多尝试三次，未切换其他通道或模型。性能报告原文已保存在材料快照中，覆盖分析结果不受影响。
>
> 失败原因：性能分析未返回规定的四态结论，无法写入总结邮件

## 三、总结邮件格式（可直接复制）

```text
integralImage3函数，功能验证通过，性能测试 AI 分析失败（原始报告已保留），请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_integralImage3 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_integralImage3.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\integralImage3.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ImageFiltering\IntegralImageDomainFiltering\integralImage3.md

TyImageProcessing：
```
