# jaccard 提测分析报告

> 任务：jaccard（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_jaccard.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/ImageSegmentation/EvaluateSegmentationAccuracy/jaccard.md`
> 单测：`test/im_math/test_jaccard.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析）＋用户粘贴性能报告分析 ｜ 生成：2026-07-31 10:06 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| BW1 | 逻辑数组（任意维度） | Bool | 第一个二值图像 |
| BW2 | 逻辑数组（与BW1大小相同） | Bool | 第二个二值图像 |
| L1 | 非负整数数组（任意维度） | Int64 | 第一个标签图像 |
| L2 | 非负整数数组（与L1大小相同） | Int64 | 第二个标签图像 |

### 文档自身缺陷审计

- 文档声明L1/L2数据类型为Int64，但实现代码也接受Float64（要求为非负整数值），文档未提及Float64支持。
- 文档未说明输入为空数组时的错误行为。
- 文档未说明输入数组大小不一致时的错误行为。
- 文档未说明L1/L2元素类型不一致时的错误行为。
- 文档未说明标签图像中存在缺失标签（如标签1和3但无标签2）时的返回值行为（返回NaN）。
- 文档示例中标签图像数据类型声明为Int64，但实际加载的`L_groundTruth`被转换为Float64，示例与声明不一致。

### 参数数据类型与结构覆盖

#### 参数 BW1

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Bool | 二值逻辑数组，二维 | test_jaccard.jl / "similarity = jaccard(BW1,BW2)" / `BW1 = rand(Bool, 100, 100)` | ☑ |
| Bool | 二值逻辑数组，二维（regression） | test_jaccard.jl / "numeric regression" / `BW1 = Bool[1 0; 1 0]` | ☑ |

说明：仅覆盖二维数组，文档声明“任意维度”的高维Bool数组无测试证据。

#### 参数 BW2

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Bool | 二值逻辑数组，与BW1大小相同 | test_jaccard.jl / "similarity = jaccard(BW1,BW2)" / `BW2 = rand(Bool, 100, 100)` | ☑ |
| Bool | 二值逻辑数组，与BW1大小相同（regression） | test_jaccard.jl / "numeric regression" / `BW2 = Bool[1 1; 0 0]` | ☑ |

说明：仅覆盖与BW1大小相同的二维数组，文档声明“任意维度”的高维Bool数组无测试证据。

#### 参数 L1

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Int64 | 非负整数标签，二维 | test_jaccard.jl / "similarity = jaccard(L1,L2)" / `L1 = Int64.(rand(UInt8, 100, 100))` | ☑ |
| Int64 | 非负整数标签，二维（regression） | test_jaccard.jl / "numeric regression" / `L1 = [1 1 2; 0 3 3]` | ☑ |
| Float64 | 非负整数值，二维 | test_jaccard.jl / "similarity = jaccard(L1,L2)" / `L1 = Float64.(rand(UInt8, 100, 100))` | ☑ |
| Float64 | 非负整数值，二维（regression） | test_jaccard.jl / "numeric regression" / `jaccard(Float64.(L1), Float64.(L2))` | ☑ |

说明：文档仅声明Int64，但实现接受Float64；仅覆盖二维数组，文档声明“任意维度”的高维数组无测试证据；缺少含缺失标签（产生NaN）的测试；缺少空数组、大小不一致、类型不一致的错误路径测试。

#### 参数 L2

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Int64 | 非负整数标签，与L1大小相同 | test_jaccard.jl / "similarity = jaccard(L1,L2)" / `L2 = Int64.(rand(UInt8, 100, 100))` | ☑ |
| Int64 | 非负整数标签，与L1大小相同（regression） | test_jaccard.jl / "numeric regression" / `L2 = [1 2 2; 0 0 3]` | ☑ |
| Float64 | 非负整数值，与L1大小相同 | test_jaccard.jl / "similarity = jaccard(L1,L2)" / `L2 = Float64.(rand(UInt8, 100, 100))` | ☑ |
| Float64 | 非负整数值，与L1大小相同（regression） | test_jaccard.jl / "numeric regression" / `jaccard(Float64.(L1), Float64.(L2))` | ☑ |

说明：与L1相同的覆盖缺口。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维Bool数组 | BW1/BW2输入 | ☑ | 有正常路径和回归断言 |
| 二维Int64数组 | L1/L2输入 | ☑ | 有正常路径和回归断言 |
| 二维Float64数组 | L1/L2输入（文档未声明） | ☑ | 文档应补充 |
| 高维数组（三维及以上） | 文档声明“任意维度” | 未覆盖 | 所有参数均缺 |
| 空数组 | 错误路径 | 未覆盖 | 实现抛出错误，测试缺 |
| 大小不一致数组 | 错误路径 | 未覆盖 | 实现抛出错误，测试缺 |
| 类型不一致数组 | 错误路径 | 未覆盖 | 实现抛出错误，测试缺 |
| 含缺失标签数组 | L1/L2边界行为 | ☑ | "numeric regression"有覆盖，但弱证据（仅isnan断言类型） |

### 覆盖缺口与优先级

**高优先级**

- 错误路径全覆盖缺失：空数组、大小不一致数组、类型不一致数组。实现中有明确的错误抛出逻辑，但无任何测试，属于高风险缺口。
- 标签图像含NaN/Inf/负数等非法值：Float64路径有验证但无测试。
- 标签图像含非整数值的Float64：有验证但无测试。

**中优先级**

- 高维数组（三维及以上）输入：文档声明“任意维度”，但无任何高维测试，无法证明高维输入正确性。
- 一维向量输入：同样属于“任意维度”声明的一部分，但未覆盖。

**低优先级**

- 文档补充Float64类型声明后，需在文档层面与实现对齐。
- 标签图像返回值中NaN的精确断言（当前仅有`isnan`，缺少对具体位置的断言）。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| BW1 | Bool二维数组 | Bool、任意维度 | 部分覆盖 |
| BW2 | Bool二维数组 | Bool、任意维度 | 部分覆盖 |
| L1 | Int64二维数组 | Int64、任意维度 | 部分覆盖 |
| L2 | Int64二维数组 | Int64、任意维度 | 部分覆盖 |

总计：完全覆盖参数数 0，至少有证据参数数 4（全部为部分覆盖）。

### 缺失测试代码补全

```julia
@testset "jaccard 补充覆盖" begin
    # 错误路径：空数组
    @testset "空数组错误" begin
        @test_throws ErrorException jaccard(Bool[], Bool[])
        @test_throws ErrorException jaccard(Int64[], Int64[])
        @test_throws ErrorException jaccard(Float64[], Float64[])
        @test_throws ErrorException jaccard(Bool[1 0; 1 0], Bool[])
        @test_throws ErrorException jaccard(Int64[1 0; 1 0], Int64[])
    end

    # 错误路径：大小不一致
    @testset "大小不一致错误" begin
        @test_throws ErrorException jaccard(Bool[1 0; 1 0], Bool[1 0 1])
        @test_throws ErrorException jaccard(Int64[1 0; 1 0], Int64[1 0 1])
        @test_throws ErrorException jaccard(Float64[1 0; 1 0], Float64[1 0 1])
    end

    # 错误路径：类型不一致
    @testset "类型不一致错误" begin
        @test_throws ErrorException jaccard(Bool[1 0; 1 0], Int64[1 0; 1 0])
        @test_throws ErrorException jaccard(Int64[1 0; 1 0], Float64[1 0; 1 0])
    end

    # 错误路径：标签图像含非法值
    @testset "标签非法值错误" begin
        @test_throws ErrorException jaccard(Int64[-1 0; 1 0], Int64[1 0; 1 0])
        @test_throws ErrorException jaccard(Int64[1 0; 1 0], Int64[-1 0; 1 0])
        @test_throws ErrorException jaccard(Float64[-1.0 0.0; 1.0 0.0], Float64[1.0 0.0; 1.0 0.0])
        @test_throws ErrorException jaccard(Float64[1.0 0.0; Inf 0.0], Float64[1.0 0.0; 1.0 0.0])
        @test_throws ErrorException jaccard(Float64[1.0 0.0; NaN 0.0], Float64[1.0 0.0; 1.0 0.0])
        @test_throws ErrorException jaccard(Float64[1.5 0.0; 1.0 0.0], Float64[1.0 0.0; 1.0 0.0])
    end

    # 高维数组
    @testset "高维数组输入" begin
        BW1_3d = rand(Bool, 10, 10, 5)
        BW2_3d = rand(Bool, 10, 10, 5)
        sim_bw = jaccard(BW1_3d, BW2_3d)
        @test sim_bw isa Float64

        L1_3d = Int64.(rand(0:5, 10, 10, 5))
        L2_3d = Int64.(rand(0:5, 10, 10, 5))
        sim_l = jaccard(L1_3d, L2_3d)
        @test sim_l isa AbstractVecOrMat{Float64}

        L1f_3d = Float64.(rand(0:5, 10, 10, 5))
        L2f_3d = Float64.(rand(0:5, 10, 10, 5))
        sim_lf = jaccard(L1f_3d, L2f_3d)
        @test sim_lf isa AbstractVecOrMat{Float64}
    end

    # 一维向量
    @testset "一维向量输入" begin
        BW1_vec = rand(Bool, 50)
        BW2_vec = rand(Bool, 50)
        sim_bw_vec = jaccard(BW1_vec, BW2_vec)
        @test sim_bw_vec isa Float64

        L1_vec = Int64.(rand(0:3, 50))
        L2_vec = Int64.(rand(0:3, 50))
        sim_l_vec = jaccard(L1_vec, L2_vec)
        @test sim_l_vec isa AbstractVecOrMat{Float64}
    end

    # 标签缺失产生NaN的精确断言
    @testset "缺失标签NaN位置断言" begin
        L1_skip = Int64[1 3; 3 1]
        L2_skip = Int64[1 3; 3 1]
        sim_skip = jaccard(L1_skip, L2_skip)
        @test length(sim_skip) == 3
        @test sim_skip[1] ≈ 1.0
        @test isnan(sim_skip[2])
        @test sim_skip[3] ≈ 1.0
    end
end
```

**静态检验说明**

- 本代码块整体追加到test_jaccard.jl文件物理末尾。
- 所有新增测试用例组织在独立的顶层@testset "jaccard 补充覆盖"中，不嵌套或修改原有测试块。
- 作用域自足：仅使用顶层导入的TyImageProcessing、Test等模块，未引用原有测试块内部局部变量；需重新构造的少量样例数据均在新增块内局部创建。
- 代码无占位符、省略号或插入指示，可原样粘贴。

**仍需 baseline 校准项**

- 高维和一维向量输入的正向用例仅断言返回类型（Float64标量或AbstractVecOrMat），未验证具体数值；需要baseline真值或独立计算确认返回值精确性。
- 标签缺失NaN位置断言用例仅验证NaN存在性和位置，数值精度已由原有regression测试覆盖，无需额外校准。

## 二、性能测试判定（AI，仅当前函数）

### 用户粘贴的性能报告分析

#### 数据完整性
报告原文仅末尾提供了一行以制表符分隔的数值：`5.48E-05	4.62E-05	0.026516	0.00061	2.07E-03	7.57E-02`。原文未给出任何列标题或字段映射说明，无法将该行数值与 Julia 首次、Julia 二次、MATLAB 首次、MATLAB 二次的耗时建立唯一对应关系。因此，当前函数 `jaccard` 在 Julia 首次、Julia 二次、MATLAB 首次、MATLAB 二次四个关键阶段的耗时均缺失。

#### 可判定项
由于无法从报告中解析出任何属于 `jaccard` 的有效耗时，不存在可计算的耗时比值，无符合筛选规则的判定项。

#### 综合结论
缺失 Julia 首次与二次的可判定数值，按规则以不通过归类。

**性能结论：性能不通过**

## 三、总结邮件格式（可直接复制）

```text
jaccard，功能验证通过，性能不通过，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_jaccard 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_jaccard.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\jaccard.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ImageSegmentation\EvaluateSegmentationAccuracy\jaccard.md

TyImageProcessing：
```
