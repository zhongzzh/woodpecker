# lillietest 提测分析报告

> 任务：TyStatisticsCore：lillietest 函数性能优化（TyStatisticsCore.jl 521，分支 lius/lillietest-optim）
> 任务类型：性能优化
> 文档：本地 syslab-docs-2.0（origin/develop）`syslabHelpSourceCode/projects/TyStatistics/Doc/TyStatistics/HypothesisTests/DistributionTests/lillietest.md`
> 单测：`test/HypothesisTests/DistributionTests/lillietest.jl`（伴随数据 2 个）
> benchmark：`benchmark/HypothesisTests/DistributionTests/lillietest`
> HTML 预览：未执行
> 分析规则：提示词 v2（D20）＋性能标准 D13/D15/D16/D23~D28 ｜ 生成：2026-08-26 11:44 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

我们需要回答用户。用户给了完整文档和测试文件，要求按既定规则输出分析报告。需要生成严谨审计报告，包括文档定义输入参数、文档缺陷、覆盖表、缺口、度量、补测代码。必须遵守硬约束。需要分析。

需要注意：用户消息包含“整理后的对话记录和工具进展”和“请基于以上整理后的对话记录和工具进展，继续当前任务，直接回答最新请求。” 这是要求我们输出报告，针对 lillietest.md 和 lillietest.jl。

我们需要仔细分析文档和测试。

先理解函数 lillietest(x, alpha=0.05, dist="normal", mctol=[]).

输入参数：x, alpha, dist, mctol。

文档定义：
- x: 向量。数据类型：Float32 | Float64 | Int8 | Int16 | Int32 | Int64 | Int128 | UInt8 | UInt16 | UInt32 | UInt64 | UInt128
- alpha: 范围 (0,1) 标量；默认0.05，不使用mctol alpha [0.001,0.50]；使用mctol alpha (0,1)。数据类型同上
- dist: "normal"（默认） | "exponential" | "ev" | "extreme value"
- mctol: 范围 (0,1) 标量；数据类型同上

测试文件 lillietest.jl 中内容。

需要分别核对结构形式和数据类型两个维度。注意文档未明确声明绑定组合，因此按独立维度统计：某数据类型只要在任一已测试结构中被明确构造/赋值并传入参数，就算覆盖；每种结构只需明确测试证据。

需要判断测试中的参数使用。测试文件有多个 @testset：
- lillitest_test1: lillietest(MPG) 默认 alpha, dist; 未显式传入 alpha, dist，只测试默认路径。x是MPG，数据类型？MPG 数据包含整数和浮点数（如17.5, 43.1等），还有 NaN。Julia 数组包含 NaN 和 Int 和 Float 时，整体类型是 Float64。文档说 NaN 被忽略。因此 x 是 Float64 向量。结构形式：向量。证据有调用和断言 h,p,kstat,critval。这是强覆盖（非默认 x）。
- lillitest_test2: lillietest(MPG; alpha=0.03, dist="ev") 显式 alpha=0.03 Float64? 0.03 是 Float64，dist="ev" 字符串。x MPG Float64 向量。断言 [1],[2],[4]，注释了 [3]。alpha 数据类型 Float64 覆盖；dist "ev" 覆盖；结构标量覆盖。alpha 未覆盖其他类型。
- lillitest_test3: lillietest(MPG; alpha=0.03, dist="exponential") alpha Float64，dist exponential。x Float64。断言覆盖。
- lillitest_test4: lillietest(rng, MPG; alpha=0.05, dist="normal", mctol=0.003) 注意多了一个位置参数 rng？这看起来是旧接口？文档语法中无 rng 参数，但测试中调用 lillietest(rng, MPG; ...)，可能原函数支持随机数生成器作为第一个可选位置参数？但文档未声明 rng 输入参数。我们需要审计文档公开声明为准，只分析用户可设置的输入参数。所以忽略 rng 这个位置参数？可能这属于未文档化参数，不是文档要求项。但测试中 x 是 MPG 作为第二个位置参数，实际上 lillietest(rng, MPG; ...) 可能位置参数顺序与文档不符？但测试能运行说明函数支持。审计文档定义的输入参数包括 x, alpha, dist, mctol，不包含 rng。所以我们关注 alpha=0.05 显式 Float64，dist="normal" 显式，mctol=0.003 Float64。断言 h,p。mctol 覆盖。
- lillitest_test5: lillietest(MPG; alpha=0.05, dist="ev", mctol=0.003) 覆盖。
- lillitest_test6: lillietest(MPG; alpha=0.05, dist="exponential", mctol=0.003) 覆盖。
- lillitest_test7: lillietest(x) 默认、lillietest(x; alpha=0.01)、lillietest(x; alpha=0.01, dist="exponential")、lillietest(x; alpha=0.01, dist="ev")。其中 x 来自 data2，是整数向量？lillietest_data2.jl 中 x = [65, 61,...] 全部整数，Julia 推断为 Vector{Int64}。因此 x 结构：向量，数据类型 Int64 覆盖。断言包括 h,p,kstat,critval 等。alpha=0.01 Float64，dist exponential/ev。无 mctol。
- lillitest_test8: x = [118 ... 125] 全部整数，Vector{Int64}，调用 lillietest(x) 默认。断言。
- lillietest preserves non-NaN input data: input = [3.0,1.0,4.0,2.0] Vector{Float64}；调用 lillietest(input) 默认，断言输入不变。这是弱证据? 但 x Float64 覆盖。
- lillietest rejects insufficient valid observations: inputs 包括 Any[], Int[], Float64[], [1], [1,2], [NaN], fill(NaN,4), [1.0, NaN, 2.0, NaN, 3.0]。这些是错误输入，验证抛错。注意文档说 x 样本数据向量，数据类型包括许多。这些错误测试中涉及 Int[]、Float64[] 空向量等，但不属于有效覆盖数据类型吗？需要判定：只有“构造或赋值、调用、结果断言”均有明确代码证据才算覆盖。错误测试调用并断言抛错，但并未成功检验，不属于覆盖有效数据类型。但数据类型 Int（如 [1]）和 Float64 出现在错误测试中，但没有结果断言（只有异常），不能算有效覆盖。所以忽略。

测试中 x 数据类型覆盖：Float64（MPG, input [3.0...]）和 Int64（data2 x, test8 x）。其他整数类型如 Int8/Int16/Int32/Int128/UInt8... 未覆盖。结构形式：向量覆盖（所有 x 都是向量）。

alpha 数据类型：测试中 alpha 显式传入 0.03,0.05,0.01 都是 Float64 字面量。未测试其他类型如 Float32, Int, UInt 等。结构形式：标量覆盖。默认 alpha 0.05 未显式传入，默认路径执行但不证明非默认值类型已覆盖。但文档默认 alpha=0.05 本身也是 Float64？默认参数未显式传入只证明默认路径被执行，不证明非默认值或全部类型已覆盖。根据规则3，默认参数未显式传入只能证明默认路径被执行，不能证明非默认值或全部类型已覆盖。alpha 的数据类型要求有 Float32, Float64, Int8, Int16, Int32, Int64, Int128, UInt8, UInt16, UInt32, UInt64, UInt128。目前只有 Float64 显式覆盖。Int64 可能通过默认？默认 0.05 是 Float64 不是 Int。所以 alpha 缺失很多数据类型。结构形式：标量已覆盖。

dist：文档要求 "normal"（默认）| "exponential" | "ev" | "extreme value"。测试中有 "normal" 显式传入在 test4/5/6? test4 传 dist="normal"，test5 ev，test6 exponential，test7 exponential/ev。默认 normal 在 test1/7/8 也执行但未显式。但根据规则，默认路径不能证明非默认值？但 dist 默认值 "normal" 的覆盖：文档要求四种取值。测试显式传了 "normal"（test4 mctol调用中 dist="normal"）覆盖；"exponential" 覆盖；"ev" 覆盖；"extreme value" 没有出现。需要检查测试中是否有 "extreme value"？没有。所以 dist 取值缺失 "extreme value"。结构形式：字符串标量，已覆盖。数据类型？dist 文档没有列出数据类型，只有取值。所以只审计取值。

mctol：文档允许范围 (0,1) 标量，数据类型同 alpha 那12种。测试中 mctol=0.003 显式传入，Float64。无其他类型。结构形式：标量已覆盖。默认 mctol=[] 表示不使用蒙特卡洛，不是允许值。需要覆盖 mctol 参数吗？文档将 mctol 作为输入参数，数据类型列表。测试有 mctol=0.003 Float64 显式，所以 Float64 覆盖。但其他数据类型缺失。结构形式：标量覆盖。默认 [] 只证明默认路径，不证明其他。mctol 非默认路径有覆盖。需要看 mctol 的默认值是 []，空数组，但文档说数据类型是那些标量。测试未测试 mctol=[] 显式？默认即为 []。但文档数据类型不包括 []。规则3默认参数未显式传入只证明默认路径，但 mctol 显式传入 0.003 覆盖 Float64。

另外注意测试中有调用 lillietest(rng, MPG; ...) 带 rng 位置参数，文档没有。这个不算输入参数。

文档自身缺陷审计：需要检查文档类型遗漏、前后矛盾、示例笔误、约束描述不完整。
可能问题：
- 语法中 `mctol=[]`，但输入参数 mctol 描述是范围 (0,1) 标量，数据类型数字。默认值 [] 与允许类型不符，可能表示无蒙特卡洛，但未说明 [] 是哨兵值。输出 p 范围描述说非 mctol 时 [0.001,0.50]，但示例1 p=0.001 并警告小于表最小值返回0.001。示例4 p=0.5 但说明 p 范围 [0.001,0.50] 包含端点？示例5 p=0.0 与输出参数 p 范围 (0,1) 矛盾，因为蒙特卡洛近似返回 p=0.0 不在 (0,1)。文档输出 p 说范围 (0,1) 但示例 p=0.0。这是缺陷。
- alpha 描述：如果不使用 mctol，Alpha 必须在 [0.001,0.50] 范围内；如果使用 mctol，Alpha 必须在 (0,1) 范围内。但输入参数摘要写“范围 (0,1) 中的标量值”，矛盾/不完整，但详细说明有区分。不算严重，但摘要未提及约束。
- mctol 描述说范围 (0,1) 中的标量值，但默认值是 []，与类型列表不一致。需要说明哨兵值。
- 语法 h,p,kstat,critval = lillietest(x,alpha=0.05,dist="normal",mctol=[]) 中参数用逗号而非分号，但 Julia 关键字参数应该用分号？文档可能为了显示。测试中用分号，这个示例笔误。文档语法线用了逗号分隔关键字参数，可能容易误导，但其他部分用 `alpha =` 等。可以指出。
- 函数库 TyStatistics，包名可能应为 TyStatistics，测试文件用 TyStatisticsCore 和 TyMathCore，但文档示例用 using TyMath, using TyStatistics。可能不一致，但不属于文档输入参数。
- 示例3 输出显示 `(true, 0.001, 0.39202806575992033, 0.054331381150922446)` 但代码 `h, = lillietest(...)` 只取第一个，输出却四元组，奇怪。示例3 中 h 应该 true，但显示四元组。可指出示例输出与赋值不匹配。
- 示例4 输出 h1 = false, p=0.5，但警告说 P 大于表中最大值返回 0.50。输出参数 p 范围 (0,1) 标量，0.5 ok。但非 mctol p 声称 [0.001,0.50]，范围闭合。
- 文档中 `lillietest(MPG,alpha = 0.05,dist = "normal",mctol=1e-4)` 示例5 返回 p=0.0，与输出 p 范围 (0,1) 矛盾。由于蒙特卡洛 p 值可能为 0，但文档写 (0,1) 不准确。可以写缺陷。

输入参数覆盖分析：
我们需要按文档参数顺序输出四级标题：x, alpha, dist, mctol。

#### x
表格列：数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态

x 文档要求：
- 结构形式：向量
- 数据类型：Float32, Float64, Int8, Int16, Int32, Int64, Int128, UInt8, UInt16, UInt32, UInt64, UInt128

在测试证据：
- 结构：向量：MPG 和 x 均为向量，多次调用。
- 数据类型：
  - Float64：MPG（lillietest_data1.jl）是 Float64 向量；input [3.0,1.0,4.0,2.0] 是 Float64 向量；均有调用和断言（input 测试只断言输入不变，属于弱证据？但 Float64 已被 MPG 强覆盖）。
  - Int64：lillietest_data2.jl x 向量（全部整数），test8 x 向量（全部整数），调用并有断言。
  - 其他类型未见。

所以 x 表：
| 向量 | 样本数据结构 | lillietest_test1/2/3/... 等 | ☑ |
| Float64 | MPG、[3.0,1.0,4.0,2.0] | lillietest_test1, preserves... | ☑ |
| Int64 | data2 x、test8 x | lillietest_test7/8 | ☑ |
| Float32 | 未发现 | — | 未覆盖 |
| Int8 | 未发现 | — | 未覆盖 |
... 其他类型未覆盖。

但根据“结构形式”和“数据类型”独立统计，表中可以将“向量”作为一行，数据类型分别多行。可以合并一些行？但“每个参数使用同一张表”可以将结构形式和数据类型作为独立行或独立条目统计，不生成组合行。所以可以列所有项。

x 覆盖状态：结构向量 ☑；Float64 ☑；Int64 ☑；其他 10 种类型未覆盖。需要列出来。可能写“Int8、Int16、Int32、Int128、UInt8、UInt16、UInt32、UInt64、UInt128”等未覆盖，为了可读性可以多行，但每个表格行短。可以每个类型一行，或合并多个短类型用顿号分隔，但覆盖状态不同：都是未覆盖，可以一行写“Int8、Int16、Int32、Int128、UInt8、UInt16、UInt32、UInt64、UInt128”未覆盖。但是否要求每个类型单独？规则没有要求逐项，可以用一行说明。但为了完整审计，最好每个类型单独一行；但表格会很长。用户要求表格单元格只放短句，多个类型或形式使用中文顿号分隔，不使用竖线。因此可以合并“其余整数类型”一行，说明涵盖。但覆盖状态只允许 ☑ 或 未覆盖。可以写“Int8、Int16、Int32、Int128、UInt8、UInt16、UInt32、UInt64、UInt128”作为条目，未覆盖。但 Float32 也是未覆盖，可以并入吗？可以，但 Float32 是浮点，最好单独。我们尽量简洁。

按照“参数数据类型与结构覆盖”要求，每个参数使用同一张表。我们可以这样：
| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| 向量 | 文档要求样本数据结构 | lillietest_test1、test7、test8 | ☑ |
| Float64 | MPG、input 向量 | lillietest_test1、preserves... | ☑ |
| Int64 | data2 x、test8 x | lillietest_test7、test8 | ☑ |
| Float32 | 未发现构造并传入 | — | 未覆盖 |
| Int8、Int16、Int32、Int128 | 未发现 | — | 未覆盖 |
| UInt8、UInt16、UInt32、UInt64、UInt128 | 未发现 | — | 未覆盖 |

但覆盖状态一行多个类型未覆盖可以接受吗？示例中允许多个类型或形式使用中文顿号分隔。可以。

#### alpha
文档要求：
- 结构形式：标量
- 数据类型：Float32, Float64, Int8, Int16, Int32, Int64, Int128, UInt8, UInt16, UInt32, UInt64, UInt128
- 约束：范围 (0,1)；不用 mctol [0.001,0.50]；用 mctol (0,1)。

测试证据：
- 标量：alpha=0.03,0.05,0.01 显式传入。
- Float64：0.03/0.05/0.01 为 Float64 字面量。
- 其他类型未发现。
- 默认 alpha=0.05 多次执行但未显式传入，不覆盖其他值。
所以表：
| 标量 | 显式 alpha=0.03/0.05/0.01 | lillietest_test2/3/4/7 | ☑ |
| Float64 | 字面量 0.03、0.05、0.01 | 同上 | ☑ |
| Float32 | 未发现 | — | 未覆盖 |
| Int8、Int16、Int32、Int64、Int128 | 未发现 | — | 未覆盖 |
| UInt8、UInt16、UInt32、UInt64、UInt128 | 未发现 | — | 未覆盖 |

需要说明：默认路径虽然多次执行，但不构成非默认值类型覆盖；alpha 边界行为 0.001/0.50/0.99 有错误测试？lillietest_error 测试了 alpha=0.99 抛错，但这是无效输入，不算边界有效覆盖；也测试了 alpha=2 抛错。可以提及弱证据？在参数表后说明。

#### dist
文档要求四种取值："normal"（默认）| "exponential" | "ev" | "extreme value"。
测试：
- "normal" 显式：lillietest(rng, MPG; alpha=0.05, dist="normal", mctol=0.003) 在 test4，以及默认路径 test1/7/8 但默认不算。
- "exponential" 显式：test3, test6, test7（lillietest(x; alpha=0.01, dist="exponential")）
- "ev" 显式：test2, test5, test7
- "extreme value" 未发现。
结构形式：字符串标量，有覆盖（所有 dist 值都是字符串标量）。
表：
| 字符串标量 | dist 为字符串取值 | test2-7 | ☑ |
| "normal" | 显式 dist="normal" | lillietest_test4 | ☑

## 二、性能测试判定（脚本演算）

### 性能优化衰退判定（标准 D13/D15/D16/D23~D28，参照系：分支/基准 Julia）

依据报告：julia分支性能对比测试报告 2026-08-20 09:52:48（同 MR 中当前函数报告时间最新的一条）

| 用法 | 检查项 | 基准 T(s) | 分支耗时(s) | 报告变化 | x(分支/基准) | 阈值 | 数据来源 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PerformanceTest2_0 | 首次 | 5.47248 | 2.87328 | 变快 47.5% | 0.525 | 1.2 | 摘要换算 | 未衰退 |
| PerformanceTest1_0 | 首次 | 8.13764 | 2.11046 | 变快 74.1% | 0.259 | 1.2 | 摘要换算 | 未衰退 |
| PerformanceTest3_0 | 首次 | 7.70969 | 3.36249 | 变快 56.4% | 0.436 | 1.2 | 摘要换算 | 未衰退 |
| PerformanceTest2_0 | 二次 | 1.39718 | 1.25986 | 变快 9.8% | 0.902 | 1.2 | 摘要换算 | 未衰退 |
| PerformanceTest1_0 | 二次 | 3.41028 | 0.340811 | 变快 90% | 0.1 | 1.2 | 摘要换算 | 未衰退 |
| PerformanceTest3_0 | 二次 | 2.79865 | 0.946541 | 变快 66.2% | 0.338 | 1.2 | 摘要换算 | 未衰退 |

**逐用法结论：**

- `PerformanceTest2_0`（指数分布检验）：【未衰退（性能提升/持平）】
- `PerformanceTest1_0`（检验正态分布）：【未衰退（性能提升/持平）】
- `PerformanceTest3_0`（威布尔分布检验）：【未衰退（性能提升/持平）】

**整体结论：✅ 通过：全部用法未衰退**

## 三、总结邮件格式（可直接复制）

```text
TyStatisticsCore：lillietest 函数性能优化，功能验证通过，性能验证通过（未衰退），请补充自动化脚本
函数代码未合并，请dev对应分支后验证，地址如下：

提测函数库分支：
https://git.tongyuan.cc/syslab/packages/math/TyStatisticsCore.jl/-/merge_requests/521

帮助文档：本地既有文档（syslab-docs-2.0 / origin/develop）

TyStatisticsCore：
```

## 四、上级汇报格式（可直接复制）

```text
TyStatisticsCore：lillietest 函数性能优化，功能单点验证通过，性能验证通过（未衰退）
测试环境：windows
测试项：lillietest函数性能优化
遗留缺陷：无
```
