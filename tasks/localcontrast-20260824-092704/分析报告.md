# localcontrast 提测分析报告

> 任务：localcontrast（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_localcontrast.jl）
> 任务类型：本地材料分析
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ContrastAdjustment\localcontrast.md`
> 单测：`test/im_filter/test_localcontrast.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ContrastAdjustment\localcontrast.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-24 09:36 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | 灰度图像 m×n、RGB 图像 m×n×3 | Float32、UInt8、UInt16、Int8、Int16 | 实数非稀疏矩阵 |
| edgeThreshold | 0.3（默认）、[0,1] 范围内数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 要保留不变的强边缘最小振幅 |
| amount | 0.25（默认）、[-1,1] 范围内数值标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 所需增强或平滑量 |

### 文档自身缺陷审计

- `edgeThreshold` 与 `amount` 声明支持多种整数类型，但 [0,1] 与 [-1,1] 连续范围对整数类型只有 0、1 或 -1、0、1 等离散值有意义，文档未说明整数取值的解释或约束。
- 文档对 `A` 仅写“非稀疏矩阵”，未明确稀疏容器如 `SparseMatrixCSC` 是否被拒绝，也未给出对应错误行为。
- `amount` 的 0、1、-1 行为说明为定性描述，未说明中间值的映射规则；结合无符号整数类型时，-1 不可表示，约束描述不完整。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 灰度图像 m×n | 8×8 与 96×96 Float32 灰度显式调用 | test_localcontrast.jl defaults and output shape、Float32 RGB and LUT paths 中 G 与 largeGray | ☑ |
| RGB 图像 m×n×3 | 8×8×3 UInt8 与 Float32 RGB 显式调用 | test_localcontrast.jl defaults、Float32 RGB and LUT paths 中 A 与 rgb32 | ☑ |
| 数据类型 Float32 | 灰度 G、largeGray 与 RGB rgb32 均构造并传入 | test_localcontrast.jl defaults and output shape、Float32 RGB and LUT paths | ☑ |
| 数据类型 UInt8 | RGB A 已构造并传入 | test_localcontrast.jl defaults 与 MATLAB reference outputs 中 A | ☑ |
| 数据类型 UInt16 | 未发现显式构造并传入 UInt16 图像 | 无 | 未覆盖 |
| 数据类型 Int8 | 未发现显式构造并传入 Int8 图像 | 无 | 未覆盖 |
| 数据类型 Int16 | 未发现显式构造并传入 Int16 图像 | 无 | 未覆盖 |

#### edgeThreshold

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 [0,1] 范围 | 显式 Float64 0.4 与 Int64 0 标量传入 | test_localcontrast.jl defaults、Float32 RGB and LUT paths、no-op cases | ☑ |
| 默认值 0.3 | 仅通过默认路径执行，未显式传入 0.3 | test_localcontrast.jl defaults and output shape 中 localcontrast(A) | 未覆盖 |
| 数据类型 Float32 | 未发现显式 Float32 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 Float64 | 0.4 多处显式传入 | test_localcontrast.jl defaults、Float32 RGB and LUT paths | ☑ |
| 数据类型 Int8 | 未发现显式 Int8 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 Int16 | 未发现显式 Int16 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 Int32 | 未发现显式 Int32 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 Int64 | 0 标量显式传入，但为 no-op 路径 | test_localcontrast.jl no-op cases 中 localcontrast(A, 0, 0.5) | ☑ |
| 数据类型 UInt8 | 未发现显式 UInt8 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 UInt16 | 未发现显式 UInt16 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 UInt32 | 未发现显式 UInt32 edgeThreshold 传入 | 无 | 未覆盖 |
| 数据类型 UInt64 | 未发现显式 UInt64 edgeThreshold 传入 | 无 | 未覆盖 |

说明：Int64 edgeThreshold=0 仅覆盖 no-op 返回路径，为弱证据；Float64 0.4 有非零显式调用，证据较强。默认值 0.3 未显式传入，按规则视为未覆盖。

#### amount

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 数值标量 [-1,1] 范围 | 显式 Float64 0.5、-0.5 与 Int64 0 标量传入 | test_localcontrast.jl MATLAB reference outputs、no-op cases | ☑ |
| 默认值 0.25 | 仅通过默认路径执行，未显式传入 0.25 | test_localcontrast.jl defaults and output shape 中 localcontrast(A) | 未覆盖 |
| 数据类型 Float32 | 未发现显式 Float32 amount 传入 | 无 | 未覆盖 |
| 数据类型 Float64 | 0.5、-0.5 显式传入 | test_localcontrast.jl MATLAB reference outputs | ☑ |
| 数据类型 Int8 | 未发现显式 Int8 amount 传入 | 无 | 未覆盖 |
| 数据类型 Int16 | 未发现显式 Int16 amount 传入 | 无 | 未覆盖 |
| 数据类型 Int32 | 未发现显式 Int32 amount 传入 | 无 | 未覆盖 |
| 数据类型 Int64 | 0 标量显式传入，但为 no-op 路径 | test_localcontrast.jl no-op cases 中 localcontrast(A, 0.4, 0) | ☑ |
| 数据类型 UInt8 | 未发现显式 UInt8 amount 传入 | 无 | 未覆盖 |
| 数据类型 UInt16 | 未发现显式 UInt16 amount 传入 | 无 | 未覆盖 |
| 数据类型 UInt32 | 未发现显式 UInt32 amount 传入 | 无 | 未覆盖 |
| 数据类型 UInt64 | 未发现显式 UInt64 amount 传入 | 无 | 未覆盖 |

说明：Int64 amount=0 仅覆盖 no-op 路径，为弱证据；Float64 0.5、-0.5 有非零显式调用，证据较强。默认值 0.25 未显式传入，按规则视为未覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 灰度图像 m×n | 使用 Float32 8×8、96×96 灰度矩阵 | ☑ | 强覆盖 |
| RGB 图像 m×n×3 | 使用 UInt8 与 Float32 8×8×3 RGB 矩阵 | ☑ | 强覆盖 |
| 数值标量结构 edgeThreshold 与 amount | 显式 Float64 与 Int64 标量传入 | ☑ | Int64 为 no-op 弱证据 |

### 覆盖缺口与优先级

**高优先级**

- A 缺少 UInt16、Int8、Int16 输入类型：现有测试仅有 Float32 与 UInt8，需构造这些类型的灰度或 RGB 输入并验证输出类型和形状。
- edgeThreshold 缺少 Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 显式类型：现有仅 Float64 与 Int64，且 Int64 为 no-op 弱证据，需补非零有效标量。
- amount 缺少 Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 显式类型：现有仅 Float64 与 Int64，且 Int64 为 no-op 弱证据，需补测。

**中优先级**

- edgeThreshold 默认值 0.3 与 amount 默认值 0.25 未显式传入，仅默认路径执行；需显式调用补测。
- 有效边界值 edgeThreshold=1、amount=1、amount=-1 未作为成功输入测试，仅有越界 -1.1、1.1 报错；建议补边界行为验证。

**低优先级**

- 非稀疏矩阵约束未显式验证，例如 `SparseMatrixCSC{Float32}` 是否应被拒绝。
- 灰度 UInt8、RGB UInt16、RGB Int8、RGB Int16 等结构与类型组合未出现；按独立维度规则不是必须补齐，补测时选择任一结构覆盖缺失类型即可。
- 1×1、1×N、N×1 等小尺寸或退化尺寸图像未覆盖，但文档未明确要求，优先级较低。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| A | 灰度结构、RGB 结构、Float32、UInt8 | 灰度结构、RGB 结构、Float32、UInt8、UInt16、Int8、Int16 | UInt16、Int8、Int16 | 部分覆盖 |
| edgeThreshold | 数值标量 0.4 与 0、Float64、Int64 | 0.3（默认）、[0,1] 数值标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 默认值 0.3（显式传入）、Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |
| amount | 数值标量 0.5、-0.5、0、Float64、Int64 | 0.25（默认）、[-1,1] 数值标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 默认值 0.25（显式传入）、Float32、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：完全覆盖参数数 0；至少有证据参数数 3。

### 缺失测试代码补全

```julia
@testset "localcontrast 补充" begin
    @testset "A 补充类型" begin
        gray_u16 = reshape(UInt16.(mod.(0:63, 256)), 8, 8)
        B_u16 = localcontrast(gray_u16)
        @test size(B_u16) == size(gray_u16)
        @test eltype(B_u16) == UInt16
        @test all(isfinite, B_u16)

        gray_i8 = reshape(Int8.(mod.(0:63, 256) .- 128), 8, 8)
        B_i8 = localcontrast(gray_i8)
        @test size(B_i8) == size(gray_i8)
        @test eltype(B_i8) == Int8
        @test all(isfinite, B_i8)

        gray_i16 = reshape(Int16.(mod.(0:63, 256) .- 32768), 8, 8)
        B_i16 = localcontrast(gray_i16)
        @test size(B_i16) == size(gray_i16)
        @test eltype(B_i16) == Int16
        @test all(isfinite, B_i16)
    end

    @testset "edgeThreshold 补充类型" begin
        G = reshape(Float32.(range(0, 1; length=64)), 8, 8)
        edge_types = (Float32, Int8, Int16, Int32, UInt8, UInt16, UInt32, UInt64)
        for T in edge_types
            threshold = T === Float32 ? Float32(0.4) : one(T)
            B = localcontrast(G, threshold, 0.5)
            @test size(B) == size(G)
            @test eltype(B) == Float32
            @test all(isfinite, B)
        end
    end

    @testset "amount 补充类型" begin
        G = reshape(Float32.(range(0, 1; length=64)), 8, 8)
        amount_types = (Float32, Int8, Int16, Int32, UInt8, UInt16, UInt32, UInt64)
        for T in amount_types
            amount = T === Float32 ? Float32(0.5) : one(T)
            B = localcontrast(G, 0.4, amount)
            @test size(B) == size(G)
            @test eltype(B) == Float32
            @test all(isfinite, B)
        end
    end

    @testset "显式默认值与有效边界" begin
        G = reshape(Float32.(range(0, 1; length=64)), 8, 8)

        B_default = localcontrast(G, 0.3, 0.25)
        @test size(B_default) == size(G)
        @test eltype(B_default) == Float32
        @test all(isfinite, B_default)

        B_edge1 = localcontrast(G, 1.0, 0.5)
        @test size(B_edge1) == size(G)
        @test eltype(B_edge1) == Float32
        @test all(isfinite, B_edge1)

        B_amt_pos1 = localcontrast(G, 0.4, 1.0)
        @test size(B_amt_pos1) == size(G)
        @test eltype(B_amt_pos1) == Float32
        @test all(isfinite, B_amt_pos1)

        B_amt_neg1 = localcontrast(G, 0.4, -1.0)
        @test size(B_amt_neg1) == size(G)
        @test eltype(B_amt_neg1) == Float32
        @test all(isfinite, B_amt_neg1)
    end
end
```

**静态检验说明**

以上新增代码块只包含一个独立的顶层 `@testset "localcontrast 补充"`，所有测试数据均在块内重新构造，未引用原测试块内部局部变量；仅复用原文件顶层的 `using TyImageProcessing` 和 `using Test`。该代码块可整体追加到 test_localcontrast.jl 文件末尾，不需要插入、替换或改写原文件任何已有代码。

**仍需 baseline 校准项**

新增的 UInt16、Int8、Int16 图像输入以及各类 edgeThreshold、amount 标量类型测试，仅断言了输出类型、形状和有限性，未提供与 MATLAB 或其他参考实现的精确数值比对。正式纳入测试集前，建议使用参考实现或已确认的数学真值，补充这些输入组合的输出 sum、平方和或抽样值断言。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
localcontrast，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自用户电脑本地文件：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_localcontrast.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\localcontrast.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ContrastAdjustment\localcontrast.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
localcontrast，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：localcontrast
遗留缺陷：无
```
