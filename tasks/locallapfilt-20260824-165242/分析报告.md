# locallapfilt 提测分析报告

> 任务：locallapfilt（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_locallapfilt.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（pyh/add_locallapfilt）`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageFilteringandEnhancement/ImageFiltering/Edge-PreservingFiltering/locallapfilt.md`
> 单测：`test/im_filter/test_locallapfilt.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ImageFiltering\Edge-PreservingFiltering\locallapfilt.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-24 17:03 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 二维灰度图像、二维 RGB 图像 | Float32、Int8、Int16、UInt8、UInt16 | 灰度图像为 m×n，RGB 图像为 m×n×3 |
| sigma | 非负数 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 边缘振幅阈值 |
| alpha | 正数 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 细节平滑或增强量 |
| beta | 非负数，默认值 1 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 动态范围控制 |
| ColorMode | "luminance"（默认）、"separate" | AbstractString、Symbol | 仅对 RGB 图像滤波方式有效 |
| NumIntensityLevels | "auto"（默认）、正整数 | AbstractString、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 强度采样数，浮点类型需为整数值 |

### 文档自身缺陷审计

- NumIntensityLevels 的数据类型列出 Float32、Float64 等浮点类型，但说明要求“正整数”，两者存在矛盾；文档未明确浮点值必须为整数值，例如 2.0 有效、2.5 无效。
- sigma 的取值范围描述不够严谨：文档同时写“非负数”和“对于取值范围为 [a,b] 的单精度图像，sigma 也应在 [a,b] 范围内”，未说明实现是否强制该范围，也未说明非 [0,1] 范围图像的边界处理。
- ColorMode 的 Symbol 类型支持前缀匹配，例如 `:sep` 可匹配 "separate"，但文档未说明这种缩写匹配行为。
- 文档未明确说明 Int8、Int16 图像输入时内部动态范围的缩放假设，对结果验证和测试断言可能造成歧义。

### 参数数据类型与结构覆盖

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 二维灰度图像 | m×n 灰度输入 | test_locallapfilt.jl / Float32 grayscale、constant images remain constant | ☑ |
| 二维 RGB 图像 | m×n×3 RGB 输入 | test_locallapfilt.jl / identity preserves type and values、RGB luminance and separate modes | ☑ |
| Float32 | 灰度 Float32 输入 | test_locallapfilt.jl / Float32 grayscale | ☑ |
| UInt8 | RGB UInt8 输入 | test_locallapfilt.jl / identity preserves type and values | ☑ |
| UInt16 | 灰度 UInt16 输入 | test_locallapfilt.jl / constant images remain constant | ☑ |
| Int8 | 未出现 | 无 | 未覆盖 |
| Int16 | 未出现 | 无 | 未覆盖 |

#### sigma

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 非负标量 | 0.4、0.2f0、0.25、0.1 等显式传入 | test_locallapfilt.jl / identity、Float32 grayscale、constant images、edge-aware denoising | ☑ |
| Float32 | 0.2f0 传入 | test_locallapfilt.jl / Float32 grayscale | ☑ |
| Float64 | 0.4、0.25、0.1 等传入 | test_locallapfilt.jl / identity、constant images、edge-aware denoising | ☑ |
| Int8 | 未出现 | 无 | 未覆盖 |
| Int16 | 未出现 | 无 | 未覆盖 |
| Int32 | 未出现 | 无 | 未覆盖 |
| Int64 | 未出现 | 无 | 未覆盖 |
| UInt8 | 未出现 | 无 | 未覆盖 |
| UInt16 | 未出现 | 无 | 未覆盖 |
| UInt32 | 未出现 | 无 | 未覆盖 |
| UInt64 | 未出现 | 无 | 未覆盖 |

#### alpha

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 正标量 | 1.0、0.6、0.7f0、0.5、4.0 等显式传入 | test_locallapfilt.jl / identity、RGB modes、Float32 grayscale、constant images、edge-aware denoising | ☑ |
| Float32 | 0.7f0 传入 | test_locallapfilt.jl / Float32 grayscale | ☑ |
| Float64 | 1.0、0.6、0.5、4.0 等传入 | test_locallapfilt.jl / identity、RGB modes、edge-aware denoising | ☑ |
| Int8 | 未出现 | 无 | 未覆盖 |
| Int16 | 未出现 | 无 | 未覆盖 |
| Int32 | 未出现 | 无 | 未覆盖 |
| Int64 | 未出现 | 无 | 未覆盖 |
| UInt8 | 未出现 | 无 | 未覆盖 |
| UInt16 | 未出现 | 无 | 未覆盖 |
| UInt32 | 未出现 | 无 | 未覆盖 |
| UInt64 | 未出现 | 无 | 未覆盖 |

#### beta

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 非负标量 | 无显式 beta 传入 | 无 | 未覆盖 |
| Float32 | 无显式 beta 传入 | 无 | 未覆盖 |
| Float64 | 无显式 beta 传入 | 无 | 未覆盖 |
| Int8 | 无显式 beta 传入 | 无 | 未覆盖 |
| Int16 | 无显式 beta 传入 | 无 | 未覆盖 |
| Int32 | 无显式 beta 传入 | 无 | 未覆盖 |
| Int64 | 无显式 beta 传入 | 无 | 未覆盖 |
| UInt8 | 无显式 beta 传入 | 无 | 未覆盖 |
| UInt16 | 无显式 beta 传入 | 无 | 未覆盖 |
| UInt32 | 无显式 beta 传入 | 无 | 未覆盖 |
| UInt64 | 无显式 beta 传入 | 无 | 未覆盖 |

说明：现有测试仅通过三参数调用走默认 beta=1 路径，未显式构造、赋值并传入 beta，因此不能视为覆盖。

#### ColorMode

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| "luminance" 取值 | 仅默认路径执行，未显式传入 | 无 | 未覆盖 |
| "separate" 取值 | :sep 显式传入并调用 | test_locallapfilt.jl / RGB luminance and separate modes | ☑ |
| AbstractString | 未出现 | 无 | 未覆盖 |
| Symbol | :sep 传入 | test_locallapfilt.jl / RGB luminance and separate modes | ☑ |

说明：默认 luminance 路径已被执行，但没有显式构造和传入 ColorMode="luminance" 的强覆盖证据。

#### NumIntensityLevels

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| "auto" 取值 | 仅默认路径执行，未显式传入 | 无 | 未覆盖 |
| 正整数取值 | 6、8、1 显式传入 | test_locallapfilt.jl / RGB modes、Float32 grayscale | ☑ |
| AbstractString | 未出现 | 无 | 未覆盖 |
| Float32 | 未出现 | 无 | 未覆盖 |
| Float64 | 未出现 | 无 | 未覆盖 |
| Int8 | 未出现 | 无 | 未覆盖 |
| Int16 | 未出现 | 无 | 未覆盖 |
| Int32 | 未出现 | 无 | 未覆盖 |
| Int64 | 6、8、1 显式传入 | test_locallapfilt.jl / RGB modes、Float32 grayscale | ☑ |
| UInt8 | 未出现 | 无 | 未覆盖 |
| UInt16 | 未出现 | 无 | 未覆盖 |
| UInt32 | 未出现 | 无 | 未覆盖 |
| UInt64 | 未出现 | 无 | 未覆盖 |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维灰度图像 | I 的灰度形式 | ☑ | 由 Float32、UInt16 测试覆盖 |
| 二维 RGB 图像 | I 的 RGB 形式 | ☑ | 由 UInt8 测试覆盖 |
| 非负标量 | sigma 形式 | ☑ | 多个 Float32、Float64 标量传入 |
| 正标量 | alpha 形式 | ☑ | 多个 Float32、Float64 标量传入 |
| 非负标量 | beta 形式 | 未覆盖 | 未显式传入 beta |
| "luminance" 值 | ColorMode 形式 | 未覆盖 | 默认路径执行但未显式传入 |
| "separate" 值 | ColorMode 形式 | ☑ | Symbol :sep 显式传入 |
| "auto" 值 | NumIntensityLevels 形式 | 未覆盖 | 默认路径执行但未显式传入 |
| 正整数 | NumIntensityLevels 形式 | ☑ | Int64 6、8、1 显式传入 |

### 覆盖缺口与优先级

**高优先级**

- beta 无任何显式测试：缺少非负标量、Float32、Float64 及所有整数类型。现有测试仅走默认 beta=1 路径，建议补四参数调用并断言输出类型和大小。
- I 缺少 Int8、Int16 图像类型：文档声明支持但测试未出现。建议补 Int8 和 Int16 灰度或 RGB 输入。
- ColorMode 缺少 AbstractString 类型和 "luminance" 显式值：现有仅 Symbol :sep，默认 luminance 未显式。建议补字符串显式调用。
- NumIntensityLevels 缺少 "auto" 显式值以及除 Int64 外的数据类型：现有仅正整数 Int64。建议补 Float32、Float64 整数值、各整数类型和 String "auto"。

**中优先级**

- sigma 缺少 Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 类型，现有仅 Float32、Float64。
- alpha 缺少同 sigma 的整数类型，现有仅 Float32、Float64。
- NumIntensityLevels 缺少 Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 及浮点整数值类型，现有 Int64 已覆盖正整数。

**低优先级**

- 现有 RGB separate 模式测试仅断言类型和大小，没有值域、PSNR 或颜色饱和性质断言，属于弱覆盖。
- 缺少 sigma=0、alpha=1、beta≠1 等组合下的输出性质断言；这些后续可基于 baseline 校准补强。
- 图像样本尺寸偏小，缺少更大或不同纵横比图像的滤波路径验证。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| I | 二维灰度、二维 RGB、Float32、UInt8、UInt16 | 二维灰度、二维 RGB、Float32、Int8、Int16、UInt8、UInt16 | Int8、Int16 | 部分覆盖 |
| sigma | 非负标量、Float32、Float64 | 非负标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |
| alpha | 正标量、Float32、Float64 | 正标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |
| beta | 无 | 非负标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 非负标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 未覆盖 |
| ColorMode | separate、Symbol | luminance、separate、AbstractString、Symbol | luminance、AbstractString | 部分覆盖 |
| NumIntensityLevels | 正整数、Int64 | auto、正整数、AbstractString、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | auto、AbstractString、Float32、Float64、Int8、Int16、Int32、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：完全覆盖参数数 0 个；至少有证据参数数 5 个；完全无证据参数数 1 个，即 beta。

### 缺失测试代码补全

```julia
@testset "locallapfilt 补充" begin
    @testset "Int8 与 Int16 图像" begin
        for T in (Int8, Int16)
            A = T[-3 4; 5 -6; 7 8]
            B = locallapfilt(A, 0.2f0, 0.8f0; NumIntensityLevels=4)
            @test typeof(B) == T
            @test size(B) == size(A)
            @test all(isfinite, Float32.(B))
        end
    end

    @testset "标量参数浮点与整数类型及 beta 显式传入" begin
        A = Float32[(row + col) / 8 for row in 1:6, col in 1:6]
        for T in (Float32, Float64, Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64)
            sigma = T(1)
            alpha = T(1)
            beta = T(1)
            B = locallapfilt(A, sigma, alpha, beta; NumIntensityLevels=2)
            @test typeof(B) == Float32
            @test size(B) == size(A)
        end
    end

    @testset "NumIntensityLevels 数据类型与 auto" begin
        A = Float32[(row + 2col) / 16 for row in 1:5, col in 1:7]
        for T in (Float32, Float64, Int8, Int16, Int32, UInt8, UInt16, UInt32, UInt64)
            n = T(2)
            B = locallapfilt(A, 0.2f0, 0.7f0; NumIntensityLevels=n)
            @test typeof(B) == Float32
            @test size(B) == size(A)
        end
        B_auto = locallapfilt(A, 0.2f0, 0.7f0; NumIntensityLevels="auto")
        @test typeof(B_auto) == Float32
        @test size(B_auto) == size(A)
    end

    @testset "ColorMode 显式字符串" begin
        A = reshape(UInt8.(0:47), 4, 4, 3)
        B_lum = locallapfilt(A, 0.35, 0.6; ColorMode="luminance", NumIntensityLevels=4)
        B_sep = locallapfilt(A, 0.35, 0.6; ColorMode="separate", NumIntensityLevels=4)
        @test typeof(B_lum) == UInt8
        @test typeof(B_sep) == UInt8
        @test size(B_lum) == size(A)
        @test size(B_sep) == size(A)
    end
end
```

静态检验说明：本新增代码块是一个独立的顶层 `@testset "locallapfilt 补充"`，作用域自足，未引用原测试块内部局部变量，可整体追加到 `test_locallapfilt.jl` 文件物理末尾，不需要编辑或插入到任何现有测试块中。该代码块仅使用原文件顶层已导入的 `TyImageProcessing` 和 `Test`。

仍需 baseline 校准项：新增用例仅断言类型、大小和有限性，未验证滤波数值与参考实现一致；`Int8`、`Int16` 图像以及不同 `NumIntensityLevels` 下的输出值需与 baseline 图像或数学真值校准。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
locallapfilt，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 pyh/add_locallapfilt 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_locallapfilt.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\locallapfilt.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageFilteringandEnhancement\ImageFiltering\Edge-PreservingFiltering\locallapfilt.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
locallapfilt，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：locallapfilt
遗留缺陷：无
```
