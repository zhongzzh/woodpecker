# localtonemap 提测分析报告

> 任务：localtonemap（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_localtonemap.jl）
> 任务类型：本地材料分析
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\HighDynamicRangeImages\localtonemap.md`
> 单测：`test/im_filter/test_localtonemap.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\dist\Help\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\HighDynamicRangeImages\localtonemap.html`
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-24 10:40 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

以下为审计结果。

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| HDR | m×n 矩阵、m×n×3 数组 | Float32 | 非负数 HDR 图像 |
| RangeCompression | 默认值 1、[0,1] 数字标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 动态范围压缩量 |
| EnhanceContrast | 默认值 0、[0,1] 数字标量 | Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 局部对比度增强量 |

### 文档自身缺陷审计

- HDR 约束描述不完整：文档仅说明“非负数”，未明确 Inf、NaN 等非有限值是否允许；当前实现未在验证阶段拒绝非有限值，测试中 Inf 输入返回 NaN，但文档未声明该行为。
- 其余未见明显类型矛盾、示例笔误或前后冲突。

### 参数数据类型与结构覆盖

#### HDR

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| m×n 矩阵 | 12×15 Float32 灰度矩阵显式构造并传入 | test_localtonemap.jl / grayscale and RGB output / grayOutput | ☑ |
| m×n×3 数组 | 12×15×3 Float32 RGB 数组显式构造并传入 | test_localtonemap.jl / grayscale and RGB output / rgbOutput | ☑ |
| Float32 | gray 与 rgb 均为 Float32，且断言输出类型与输入一致 | test_localtonemap.jl / grayscale and RGB output / typeof(...) | ☑ |

说明：HDR 的两类结构与数据类型均有构造、调用和结果断言，属于强覆盖。非负数约束由 validation 测试块中 `fill(-1.0f0, 4, 4)` 的异常断言覆盖；非有限值行为未在文档中声明，已列入文档缺陷审计。

#### RangeCompression

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 1 | 无关键字调用 constantHDR，使用默认压缩量 1 | test_localtonemap.jl / calling forms and special dynamic ranges / constantHDR | ☑ |
| [0,1] 数字标量（显式） | 0.2、0.1 显式传入 | test_localtonemap.jl / grayscale and RGB output；calling forms | ☑ |
| Float64 | 0.2、0.1 浮点字面量为 Float64 | test_localtonemap.jl / grayscale and RGB output；calling forms | ☑ |
| Float32 | 无显式 Float32 标量传入 | 无 | 未覆盖 |
| Int8 | 无 | 无 | 未覆盖 |
| Int16 | 无 | 无 | 未覆盖 |
| Int32 | 无 | 无 | 未覆盖 |
| Int64 | 无显式 Int64 标量传入；默认值 1 不能作为用户显式类型覆盖 | 无 | 未覆盖 |
| UInt8 | 无 | 无 | 未覆盖 |
| UInt16 | 无 | 无 | 未覆盖 |
| UInt32 | 无 | 无 | 未覆盖 |
| UInt64 | 无 | 无 | 未覆盖 |

说明：默认值 1 仅由默认路径执行证明，是弱证据；Float64 的显式传入为强证据。validation 测试块覆盖了越界、NaN、非标量等异常，不能作为上述正常数据类型的输入覆盖。

#### EnhanceContrast

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认值 0 | 无关键字调用 constantHDR，使用默认增强量 0 | test_localtonemap.jl / calling forms and special dynamic ranges / constantHDR | ☑ |
| [0,1] 数字标量（显式） | 0.5、0.4 显式传入 | test_localtonemap.jl / grayscale and RGB output；calling forms | ☑ |
| Float64 | 0.5、0.4 浮点字面量为 Float64 | test_localtonemap.jl / grayscale and RGB output；calling forms | ☑ |
| Float32 | 无显式 Float32 标量传入 | 无 | 未覆盖 |
| Int8 | 无 | 无 | 未覆盖 |
| Int16 | 无 | 无 | 未覆盖 |
| Int32 | 无 | 无 | 未覆盖 |
| Int64 | 无显式 Int64 标量传入 | 无 | 未覆盖 |
| UInt8 | 无 | 无 | 未覆盖 |
| UInt16 | 无 | 无 | 未覆盖 |
| UInt32 | 无 | 无 | 未覆盖 |
| UInt64 | 无 | 无 | 未覆盖 |

说明：默认值 0 仅由默认路径执行证明，是弱证据；Float64 的显式传入为强证据。validation 测试块中的异常断言不能视为这些正常数据类型的覆盖。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| m×n 矩阵 | HDR 灰度输入 | ☑ | Float32 强证据 |
| m×n×3 数组 | HDR RGB 输入 | ☑ | Float32 强证据 |
| 标量数值 | RangeCompression 与 EnhanceContrast 名称-值参数 | ☑ | Float64 显式覆盖，其余类型缺失 |

### 覆盖缺口与优先级

**高优先级**

- RangeCompression 缺少 Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64。现有证据只有 Float64 字面量显式传入；建议在补充测试中分别显式传入这些类型的 [0,1] 标量，并断言输出类型、大小、有限性与范围。
- EnhanceContrast 缺少相同的 9 种数据类型。现有证据只有 Float64 字面量显式传入；建议同上。

**中优先级**

- RangeCompression 显式边界值 0 和 1：默认值 1 仅默认路径弱覆盖，0 无显式覆盖；建议补显式边界值测试。EnhanceContrast 显式边界值 1 无覆盖，0 仅默认路径弱覆盖。
- HDR 非有限值行为：文档仅说明非负数，未说明 Inf、NaN；现有测试仅覆盖 Inf 输入返回 NaN，建议补 NaN 输入或补充文档说明。

**低优先级**

- 文档未声明位置名称-值对和缩写形式，但现有测试已覆盖 `:Range`、`"Enhance"`；如需文档与实现完全一致，可补充文档说明，不构成代码测试缺口。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| HDR | m×n 矩阵、m×n×3 数组、Float32 | m×n 矩阵、m×n×3 数组、Float32 | 无 | 完全覆盖 |
| RangeCompression | 默认值 1（弱证据）、[0,1] 数字标量、Float64 | 默认值 1、[0,1] 数字标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |
| EnhanceContrast | 默认值 0（弱证据）、[0,1] 数字标量、Float64 | 默认值 0、[0,1] 数字标量、Float32、Float64、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | Float32、Int8、Int16、Int32、Int64、UInt8、UInt16、UInt32、UInt64 | 部分覆盖 |

总计：完全覆盖参数数：1；至少有证据参数数：3。

### 缺失测试代码补全

```julia
@testset "localtonemap 补充" begin
    HDR = Float32[(row + 2col)^2 / 40 for row in 1:12, col in 1:15]
    RGB = cat(HDR, HDR .* 0.6f0, HDR .* 0.2f0; dims=3)

    @testset "RangeCompression 缺失数据类型" begin
        scalar_values = (
            0.5f0, Int8(1), Int16(1), Int32(1), Int64(1),
            UInt8(1), UInt16(1), UInt32(1), UInt64(1),
        )
        for value in scalar_values
            output = localtonemap(HDR; RangeCompression=value)
            @test typeof(output) == typeof(HDR)
            @test size(output) == size(HDR)
            @test all(isfinite, output)
            @test all(v -> 0 <= v <= 1, output)
        end
    end

    @testset "EnhanceContrast 缺失数据类型" begin
        scalar_values = (
            0.5f0, Int8(0), Int16(0), Int32(0), Int64(0),
            UInt8(0), UInt16(0), UInt32(0), UInt64(0),
        )
        for value in scalar_values
            output = localtonemap(RGB; EnhanceContrast=value)
            @test typeof(output) == typeof(RGB)
            @test size(output) == size(RGB)
            @test all(isfinite, output)
            @test all(v -> 0 <= v <= 1, output)
        end
    end
end
```

静态检验说明：上述 `@testset` 为独立顶层测试块，作用域自足，仅依赖文件顶部的 `using TyImageProcessing, Test`；未引用原测试块内部的局部变量。该代码块可整体追加到 `test_localtonemap.jl` 文件末尾，无需插入或修改现有代码。

仍需 baseline 校准项：无。本补测仅断言类型、大小、有限性和 [0,1] 范围，未引入精确数值 expected。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
localtonemap，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自用户电脑本地文件：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_localtonemap.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\localtonemap.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\Import,Export,andConversion\HighDynamicRangeImages\localtonemap.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
localtonemap，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：localtonemap
遗留缺陷：无
```
