# psnr 提测分析报告

> 任务：psnr（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_psnr.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/ImageQuality/FullReferenceQualityMetrics/psnr.md`
> 单测：`test/im_math/test_psnr.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-06 11:35 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | 任意维度数值数组、与 ref 大小相同 | UInt8、UInt16、Int16、Float32、Float64 | 待分析图像 |
| ref | 数值数组、与 A 大小和数据类型相同 | UInt8、UInt16、Int16、Float32、Float64 | 参考图像 |

### 文档自身缺陷审计

- `ref` 的摘要写作“数值矩阵”，正文却写作“数值数组”，二者对维度结构的描述不一致。
- 文档只声明了 A 和 ref 两个参数，但实现还公开支持可选参数 `peakval`、关键字参数 `nargout` 和 `DataFormat`，这些接口及其取值约束没有出现在文档中。
- 文档没有说明不同数据类型对应的默认峰值。实现对 UInt8、UInt16、Int16 和浮点类型使用了不同默认值，其中 Int16 使用 `65535.0`，该语义尤其需要文档明确说明。
- 文档没有说明 A 和 ref 必须具有相同大小和相同元素类型，也没有说明违反约束时的异常类型。
- 文档没有说明空数组、批次维度、输出数组形状或 `nargout=2` 时的 SNR 输出行为。
- 示例使用 `imread` 和 `imnoise`，但未说明输入图像的元素类型；示例中的精确 PSNR 数值也缺少可复现的随机种子或固定噪声数据，难以作为稳定测试真值。
- 语法和示例只展示二参数调用，未反映实现中 `peakval`、`nargout` 和 `DataFormat` 的实际调用形式。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 | 构造 UInt8 矩阵并传入 psnr，断言数值结果和输出类型 | test_psnr.jl：`default peak values and output types` | ☑ |
| UInt16 | 构造 UInt16 矩阵并传入 psnr，断言数值结果和输出类型 | test_psnr.jl：`default peak values and output types` | ☑ |
| Int16 | 构造 Int16 矩阵并传入 psnr，断言数值结果和输出类型 | test_psnr.jl：`default peak values and output types` | ☑ |
| Float32 | 构造 Float32 矩阵、向量和三维数组并传入 psnr，断言数值结果、输出类型和形状 | test_psnr.jl：`default peak values and output types`、`batch dimension`、`empty inputs` | ☑ |
| Float64 | 构造 Float64 矩阵、向量和二维数组并传入 psnr，断言数值结果、输出类型和异常行为 | test_psnr.jl：`default peak values and output types`、`custom peak and SNR output`、`invalid inputs` | ☑ |
| 一维数组 | 构造 Float64、UInt8、Float32 和空向量并调用，断言标量或空结果 | test_psnr

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
psnr，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_psnr1 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_psnr.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\psnr.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ImageQuality\FullReferenceQualityMetrics\psnr.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
psnr，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：psnr
遗留缺陷：无
```
