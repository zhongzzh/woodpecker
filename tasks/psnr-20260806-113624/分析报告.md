# psnr 提测分析报告

> 任务：psnr（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_psnr.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/ImageQuality/FullReferenceQualityMetrics/psnr.md`
> 单测：`test/im_math/test_psnr.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-06 11:37 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| A | 任意维度数值数组 | UInt8、UInt16、Int16、Float32、Float64 | 待分析图像 |
| ref | 与 A 尺寸相同的数值数组 | UInt8、UInt16、Int16、Float32、Float64 | 参考图像，文档要求与 A 具有相同尺寸和数据类型 |

### 文档自身缺陷审计

- 语法只声明了 `psnr(A, ref)`，未声明实现中公开可调用的可选位置参数 `peakval` 以及关键字参数 `nargout`、`DataFormat`。
- `ref` 的参数摘要写成“数值矩阵”，正文却写成“数值数组”，与“任意维度”约束不一致。
- 文档未说明数组是否允许为空、是否允许一维或三维以上结构，也未说明 `A` 与 `ref` 的尺寸和元素类型不匹配时的异常行为。
- 示例使用 `imnoise` 生成随机噪声，但没有设置随机种子；示例给出的精确 PSNR 值无法稳定复现，示例数值应改为性质断言或补充固定随机种子。
- 数据类型列表未说明整数类型的默认峰值取值规则，也未说明显式 `peakval` 的允许范围。

### 参数数据类型与结构覆盖

#### A

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 数值数组 | 构造二维 UInt8 矩阵并显式传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `UInt8` 用例 | ☑ |
| UInt16 数值数组 | 构造二维 UInt16 矩阵并显式传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `UInt16` 用例 | ☑ |
| Int16 数值数组 | 构造二维 Int16 矩阵并显式传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `Int16` 用例 | ☑ |
| Float32 数值数组 | 构造二维 Float32 矩阵并显式传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `Float32` 用例 | ☑ |
| Float64 数值数组 | 构造二维 Float64 矩阵并显式传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `Float64` 用例 | ☑ |
| 一维数组 | 构造 Float64 向量并传入 `psnr`，断言数值结果 | `test_psnr.jl`，`custom peak and SNR output` 中的 `source` | ☑ |
| 二维数组 | 构造各文档数据类型的二维矩阵并断言结果 | `test_psnr.jl`，`default peak values and output types` | ☑ |
| 三维数组 | 构造 Float32 三维数组并断言批量结果的尺寸、类型和数值 | `test_psnr.jl`，`batch dimension` | ☑ |
| 空数组 | 构造 UInt8、Float32、Float64 的空数组并断言结果 | `test_psnr.jl`，`empty inputs` | ☑ |

说明：文档中的“任意维度”无法通过有限样本穷举证明；现有测试覆盖了一维、二维、三维和空数组，属于对该结构要求的代表性覆盖，不代表所有更高维度均已验证。空数组结果断言较强，但它属于边界结构，不等同于非空数组的全面维度覆盖。

#### ref

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 数值数组 | 构造二维 UInt8 参考矩阵并传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `reference` | ☑ |
| UInt16 数值数组 | 构造二维 UInt16 参考矩阵并传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `reference` | ☑ |
| Int16 数值数组 | 构造二维 Int16 参考矩阵并传入 `psnr` | `test_psnr.jl`，`default peak values and output types` 中的 `reference` | ☑ |
| Float32 数值数组 | 构造二维、三维 Float32 参考数组并断言结果 | `test_psnr.jl`，`default peak values and output types`、`batch dimension` | ☑ |
| Float64 数值数组 | 构造二维、一维 Float64 参考数组并断言结果 | `test_psnr.jl`，`default peak values and output types`、`custom peak and SNR output` | ☑ |
| 一维数组 | 构造 Float64 参考向量并传入 `psnr` | `test_psnr.jl`，`custom peak and SNR output` | ☑ |
| 二维数组 | 构造各文档数据类型的二维参考矩阵并断言结果 | `test_psnr.jl`，`default peak values and output types` | ☑ |
| 三维数组 | 构造 Float32 三维参考数组并断言批量结果 | `test_psnr.jl`，`batch dimension` | ☑ |
| 与 A 尺寸相同 | 正常调用中使用同尺寸数组；另有尺寸不匹配异常断言 | `test_psnr.jl`，`default peak values and output types`、`invalid inputs` | ☑ |
| 与 A 元素类型相同 | 正常调用中使用同类型数组；另有 Float64 与 Float32 不匹配异常断言 | `test_psnr.jl`，`default peak values and output types`、`invalid inputs` | ☑ |
| 空数组 | 构造与 A 同类型、同尺寸的空参考数组并断言结果 | `test_psnr.jl`，`empty inputs` | ☑ |

说明：正常用例均对返回值进行数值、类型或形状断言；空数组用例也有明确结果断言。尺寸和元素类型不匹配用例属于异常路径证据，不能替代全部合法数组结构的验证。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 一维数组 | Float64 非空向量和多类型空向量 | ☑ | 有结果断言 |
| 二维数组 | 五种文档声明类型均有非空矩阵 | ☑ | 有精确 PSNR 或类型断言 |
| 三维数组 | Float32 三维数组，含批量维度 | ☑ | 有尺寸、元素类型和数值断言 |
| 空数组 | 一维、二维、三维空数组 | ☑ | 属于边界结构 |
| 任意维度数组 | 文档泛化声明，测试仅覆盖一维至三维 | ☑ | 代表性覆盖，未穷举四维及更高维 |
| A 与 ref 同尺寸 | 正常路径和尺寸不匹配异常路径均有测试 | ☑ | 合法路径与错误路径均有证据 |
| A 与 ref 同元素类型 | 五种合法类型均在正常路径配对使用 | ☑ | 另有类型不匹配异常测试 |

### 覆盖缺口与优先级

#### 高优先级

- 文档声明“任意维度”，现有测试只覆盖一维、二维、三维；虽然已有代表性证据，但没有四维或更高维的合法非空数组调用。建议补测四维 Float64 或 Float32 数组，并断言返回值类型和有限性。
- 文档未公开 `peakval`、`nargout`、`DataFormat`，但实现和现有测试已将它们作为可调用参数使用。建议明确这些参数是否属于公开 API，并补充其类型、取值范围和维度语义文档。

#### 中优先级

- 文档没有描述尺寸不匹配、元素类型不匹配和不支持元素类型的异常行为。现有测试包含这些行为，但文档

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
psnr，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_psnr1 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_math\test_psnr.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\psnr.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ImageQuality\FullReferenceQualityMetrics\psnr.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
psnr，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：psnr
遗留缺陷：无
```
