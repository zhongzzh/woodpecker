# radon 提测分析报告

> 任务：radon（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_radon.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/ImageSegmentationandAnalysis/ObjectAnalysis/DetectLines/radon.md`
> 单测：`test/im_filter/test_radon.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-06 09:35 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| I | 二维数值矩阵 | UInt8、UInt16、Int16、Float32、Float64 | 待执行拉东变换的二维灰度图像 |
| theta | 省略时使用 0:179、显式数值向量 | Int64 | 以度为单位指定投影角度 |

### 文档自身缺陷审计

- 文档将 `I` 的类型限定为 UInt8、UInt16、Int16、Float32、Float64，但实现和测试还明确支持 Bool；公开类型列表存在遗漏。
- 文档将 `theta` 限定为 Int64 数值向量，但实现和测试明确接受 Real 标量、Float64 向量及二维数组；数据类型和结构描述均不完整。
- 示例使用 `theta = 0:180`，会产生 181 个角度，而默认范围是 `0:179`。文档未说明示例是否有意额外包含 180 度，容易被误认为默认范围示例的端点笔误。
- 文档未说明 `theta` 是否允许任意范围、重复值、空向量及非有限值，也未说明非有限角度会产生包含 NaN 的特殊结果。
- 文档只称 `theta` 为数值向量，没有明确示例中的整数范围对象也属于允许结构。
- 文档未说明输入图像是否允许空矩阵、单行矩阵或单列矩阵，图像尺寸约束不完整。
- 语法中的 `R, = radon(...)` 不直观，而实现始终返回两个值；文档应明确第二个返回值可以被忽略，而不是让读者误认为存在单返回值方法。

### 参数数据类型与结构覆盖

#### I

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| UInt8 二维矩阵 | 构造 1×1 矩阵，调用后精确断言 R、xp 及 R 的元素类型 | test_radon.jl + single pixel has a hand-checkable projection | ☑ |
| UInt16 二维矩阵 | 构造 1×1 矩阵，调用后精确断言 R、xp 及 R 的元素类型 | test_radon.jl + single pixel has a hand-checkable projection | ☑ |
| Int16 二维矩阵 | 构造 1×1 矩阵，调用后精确断言 R、xp 及 R 的元素类型 | test_radon.jl + single pixel has a hand-checkable projection | ☑ |
| Float32 二维矩阵 | 构造 1×1 矩阵，调用后精确断言 R、xp 及 R 的元素类型 | test_radon.jl + single pixel has a hand-checkable projection | ☑ |
| Float64 二维矩阵 | 同时覆盖 1×1 和含正负值的 2×2 矩阵，并断言精确值、形状及投影和性质 | test_radon.jl + single pixel has a hand-checkable projection、angle forms and projection invariants | ☑ |
| Bool 二维矩阵 | 实现支持但文档未声明，覆盖 1×1 和 2×2 矩阵 | test_radon.jl + single pixel has a hand-checkable projection、default and non-finite angles | ☑ |

说明：UInt8、UInt16、Int16、Float32 只有单点常量图像证据。虽然具备构造、调用和结果断言，可以记为已有证据，但按照单点输入属于弱证据的规则，不能据此认定这些类型已获得完整的非平凡二维图像覆盖。Bool 属于文档外扩展，不计入公开输入覆盖率分母。

#### theta

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 省略参数的默认 0:179 | 未显式传入 theta，断言 R 的 180 列、投影和及 xp | test_radon.jl + default and non-finite angles | ☑ |
| Int64 数值向量 | 没有显式构造并传入 Int64 向量或整数范围的测试 | 无 | 未覆盖 |
| Int64 标量 | 有标量 0 和 90 的调用及结果断言，但标量不属于文档声明的数值向量 | test_radon.jl + single pixel has a hand-checkable projection、angle forms and projection invariants | ☑ |
| Float64 向量 | 覆盖空向量和包含 Inf、NaN 的向量，但类型不符合文档声明的 Int64 | test_radon.jl + angle forms and projection invariants、default and non-finite angles | ☑ |
| Float64 二维数组 | 调用时被实现展平为四个角度并断言形状及投影性质，但二维数组不属于文档声明的向量 | test_radon.jl + angle forms and projection invariants | ☑ |

说明：默认调用只能证明默认路径被执行，不能替代显式 Int64 数值向量覆盖。现有标量、Float64 向量和 Float64 二维数组测试都是文档外形式，不能用于认定文档声明的 Int64 向量已经覆盖。Float64 空向量还是空对象弱证据，但同一类型另有包含非有限值的非空向量测试。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| I 的二维数值矩阵 | 五种文档类型均有构造、调用和断言 | ☑ | UInt8、UInt16、Int16、Float32 仅有 1×1 弱证据 |
| I 的非平凡二维矩阵 | 文档类型中只有 Float64 具有非平凡 2×2 输入 | 未覆盖 | 其余四种公开类型需要补测 |
| theta 的默认省略形式 | 验证默认结果具有 180 个投影角度 | ☑ | 仅证明默认路径 |
| theta 的 Int64 数值向量 | 未显式传入 Vector{Int64} 或整数范围 | 未覆盖 | 文档核心显式输入形式缺失 |
| theta 的标量形式 | Int64 标量已有结果断言 | ☑ | 文档外结构，不计入公开覆盖分母 |
| theta 的二维数组形式 | Float64 二维数组已有结果断言 | ☑ | 文档外结构，不计入公开覆盖分母 |
| theta 的空向量形式 | Float64 空向量已有形状和 xp 断言 | ☑ | 文档未说明，且为空对象弱证据 |

### 覆盖缺口与优先级

**高优先级**

- 缺少显式 Int64 数值向量覆盖。现有默认调用仅执行内部默认范围，标量和 Float64 数组又不符合文档声明的类型与结构；建议显式构造 `Vector{Int64}`，验证输出形状、元素类型、径向坐标及投影和不变量。
- 文档示例中的 Int64 范围 `0:180` 没有测试。默认测试不能证明显式范围参数被正确接收；建议按示例显式传入范围并验证产生 181 列。

**中优先级**

- UInt8、UInt16、Int16、Float32 都只有 1×1 单点矩阵。精确断言虽然可证明最小路径可用，但不能覆盖非零像素位于不同坐标时的几何累加；建议使用统一的非平凡 2×2 图像验证形状、有限性和投影和守恒性质。
- 文档与实现对 `theta` 的类型和结构描述不一致。现有测试证明标量、Float64 向量和二维数组可运行，但文档没有公开这些形式；建议决定这些形式是正式 API 还是实现细节，并相应修正文档或收紧接口。

**低优先级**

- 空角度向量只有形状断言，属于空对象弱证据；若将空向量纳入公开接口，应补充其语义和稳定性约束。
- 非有限角度已有实现特定断言，但文档没有定义该行为；建议明确是接受并返回特殊矩阵，还是应拒绝此类输入。
- 文档没有定义空图像、单行图像和单列图像的合法性。现有 1×1 测试只能证明单点图像可调用，不能回答空尺寸约束；建议先补充公开约束，再决定正常结果或异常测试。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| I | 1 | 5 | 部分覆盖 |
| theta | 1 | 2 | 部分覆盖 |

总计：文档声明的 2 个参数中，完全覆盖参数数为 0，至少有明确证据的参数数为 2。按细分要求项计，7 项中有 2 项达到完整覆盖；其余项目包含 4 项单点弱覆盖和 1 项明确未覆盖，不将部分覆盖折算为精确覆盖百分比。文档外的 Bool、标量 theta、Float64 theta 和二维 theta 数组不计入分母。

### 缺失测试代码补全

```julia
@testset "radon 补充覆盖" begin
    @testset "documented image types with an Int64 angle vector" begin
        theta = Int64[0, 90, 179]

        for T in (UInt8, UInt16, Int16, Float32, Float64)
            I = T[1 0; 2 3]
            R, xp = radon(I, theta)

            @test size(R) == (7, length(theta))
            @test size(xp) == (7, 1)
            @test eltype(R) == Float64
            @test eltype(xp) == Float64
            @test vec(xp) == collect(-3.0:3.0)
            @test all(isfinite, R)
            @test vec(sum(R; dims=1)) ≈ fill(Float64(sum(I)), length(theta))
        end
    end

    @testset "documented explicit integer range from the example" begin
        I = Float64[1 0; 2 3]
        theta = Int64(0):Int64(180)
        R, xp = radon(I, theta)

        @test size(R) == (7, 181)
        @test size(xp) == (7, 1)
        @test eltype(R) == Float64
        @test eltype(xp) == Float64
        @test vec(xp) == collect(-3.0:3.0)
        @test all(isfinite, R)
        @test vec(sum(R; dims=1)) ≈ fill(sum(I), 181)
    end
end
```

静态检验说明：该代码仅包含一个新的独立顶层 `@testset`，内部重新构造了图像和角度数据，不引用原有测试块的局部变量。它可原样整体追加到文件末尾，并复用目标测试文件顶层已有的 `TyImageProcessing` 和 `Test` 导入；以上仅为静态审查结论，未声称已经运行新增测试。

仍需 baseline 校准项：新增断言验证类型、形状、有限性、径向坐标和投影和守恒性质，没有为非平凡图像编造逐元素精确结果。若要验证各角度投影值的数值正确性，仍需使用独立数学计算或受信任实现生成 baseline，再补充带适当容差的逐元素比较。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
radon，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lzq/add_radon 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_filter\test_radon.jl
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\src\radon.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\ImageSegmentationandAnalysis\ObjectAnalysis\DetectLines\radon.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
radon，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：radon
遗留缺陷：无
```
