# rmnode 提测分析报告

> 任务：函数优化rmnode（TyGraphTheory.jl 195，分支 tz/rmnode）
> 任务类型：性能优化
> 文档：本地 syslab-docs-2.0（origin/develop）`syslabHelpSourceCode/projects/TyMath/Doc/TyMath/TyGraphTheory/Modify/rmnode.md`
> 单测：`test/GraphTheory/rmnode.jl`
> benchmark：`benchmark/Modify/rmnode`
> HTML 预览：未执行
> 分析规则：提示词 v2（D20）＋性能标准 D13/D15/D16 ｜ 生成：2026-08-26 10:08 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| G | Graph 对象、DiGraph 对象 | 图对象 | 输入图，无向 Graph 或有向 DiGraph |
| nodeID | 标量、向量 | 整数索引、字符串名称 | 一个或多个节点索引或节点名称 |

### 文档自身缺陷审计

- 未说明 `nodeID` 为越界索引、非正索引、重复 ID 时的行为；实际实现为越界忽略、非正报错、重复按集合语义删除一次，但文档未声明。
- 未说明是否允许同时混用节点索引和节点名称（例如 `[1, "A"]`）。
- `rmnode!` 的原地修改语义和返回值未在输出参数或说明中明确。
- 示例 `DiGraph(s, t, Float64[], names)` 的构造参数含义未解释，可能误导用户。

### 参数数据类型与结构覆盖

#### 参数 G

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Graph 对象 | 无向图 | rmnode.jl rmnode-test 中 `Graph(s,t)` 及多个 `rmnode` 调用 | ☑ |
| DiGraph 对象 | 有向图 | rmnode.jl rmnode-test 中 `DiGraph(s,t)` 及 `rmnode(Gd,[7,5])` 等 | ☑ |

说明：Graph 和 DiGraph 均有构造、调用及 `isEqualG` 或节点/边数量断言，属强覆盖。

#### 参数 nodeID

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量 | 单一节点输入 | rmnode.jl rmnode-test `rmnode(G3,2)`、`rmnode(G3,"d")` 等 | ☑ |
| 向量 | 多个节点输入 | rmnode.jl rmnode-test `rmnode(Gd,[7,5])`、`rmnode(G3,["b","e"])` 等 | ☑ |
| 整数索引 | 数值索引输入 | rmnode.jl rmnode-test `rmnode(G3,2)`、`rmnode(Gd,[7,5])` 等 | ☑ |
| 字符串名称 | 名称输入 | rmnode.jl rmnode-test `rmnode(G3,"d")`、`rmnode(G3,["b","e"])` 等 | ☑ |

说明：标量、向量、整数、字符串均有正常删除路径的构造、调用和结果断言；异常路径（如无名称图传字符串）也验证了错误处理，但正常覆盖不依赖异常用例。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| Graph 对象 | 无向图输入 | ☑ | rmnode-test 与 rmnode-copy-contract 中多处使用 |
| DiGraph 对象 | 有向图输入 | ☑ | rmnode-test 与 rmnode-matlab-compare 中多处使用 |
| 标量 | nodeID 单一节点输入 | ☑ | 整数标量和字符串标量均已测试 |
| 向量 | nodeID 多个节点输入 | ☑ | 整数向量和字符串数组均已测试 |

### 覆盖缺口与优先级

- 高优先级：无。
- 中优先级：无。文档声明项均有测试证据，不存在缺失独立维度。
- 低优先级：文档未公开声明的边界行为（越界、非正、重复、混合索引/名称）未在文档中说明，但现有测试已覆盖越界、非正、重复 ID 等实现行为；这些不属于文档公开声明项的覆盖率缺口。若需固化实现语义，建议补充文档说明或增加混合类型输入测试，但并非必需。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| G | Graph 对象、DiGraph 对象 | Graph 对象、DiGraph 对象 | 无 | 完全覆盖 |
| nodeID | 标量、向量、整数索引、字符串名称 | 标量、向量、整数索引、字符串名称 | 无 | 完全覆盖 |

总计：完全覆盖参数数 2 个；至少有证据参数数 2 个。

### 缺失测试代码补全

无缺失项，无需补充测试代码，因此不提供新增 `@testset` 代码块。

## 二、性能测试判定（脚本演算）

### 性能判定（标准 D13/D15/D16，参照系 MATLAB）

依据报告：julia分支性能对比测试报告 2026-08-18 16:54:01（同 MR 中当前函数的最新一条）

| 用法 | 检查项 | T=MATLAB耗时(s) | 档位阈值 | x(j/m) | 结论 |
| --- | --- | --- | --- | --- | --- |
| PerformanceTest3_0 | 首次 | 1.6368 | 1.2 | 0.3041 | 通过 |
| PerformanceTest2_0 | 首次 | 0.60108 | 1.25 | 0.4659 | 通过 |
| PerformanceTest1_0 | 首次 | 0.75996 | 1.25 | 0.3218 | 通过 |
| PerformanceTest3_0 | 二次 | 1.5492 | 1.2 | 0.2156 | 通过 |
| PerformanceTest2_0 | 二次 | 0.45834 | 1.25 | 0.119 | 通过 |
| PerformanceTest1_0 | 二次 | 0.66291 | 1.25 | 0.1527 | 通过 |

**整体结论：✅ 通过**（任一用法任一项超标即不通过）

## 三、总结邮件格式（可直接复制）

```text
函数优化rmnode，功能验证通过，性能验证通过，请补充自动化脚本
函数代码未合并，请dev对应分支后验证，地址如下：

提测函数库分支：
https://git.tongyuan.cc/syslab/packages/math/TyGraphTheory.jl/-/merge_requests/195

帮助文档：本地既有文档（syslab-docs-2.0 / origin/develop）

TyGraphTheory：
```

## 四、上级汇报格式（可直接复制）

```text
函数优化rmnode，功能单点验证通过，性能验证通过
测试环境：windows
测试项：rmnode函数性能优化
遗留缺陷：无
```
