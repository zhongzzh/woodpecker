# wcOptions 提测分析报告

> 任务：wcOptions（本地材料：C:\Users\TR\Desktop\doc\TyRobustControl.jl\test\Uncertain System Analysis\Robustness and Worst-Case Analysis\test_wcOptions.jl）
> 任务类型：本地材料分析
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyRobustControl\Doc\TyRobustControl\UncertainSystemAnalysis\RobustnessAndWorst-CaseAnalysis\wcOptions.md`
> 单测：`test/Uncertain System Analysis/Robustness and Worst-Case Analysis/test_wcOptions.jl`（伴随数据 1 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-31 11:25 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| ULevel | 标量名称-值对 | 正实数（Real） | 默认值 1，缩放归一化不确定性。 |
| Display | 标量名称-值对 | Bool | 默认值 false，控制是否显示 mussv 进度。 |
| Sensitivity | 标量名称-值对 | Bool | 默认值 false，控制是否计算逐元素灵敏度。 |
| VaryFrequency | 标量名称-值对 | Bool | 默认值 false，控制是否返回频率曲线。 |
| SensitivityPercent | 标量名称-值对 | 正实数（Real） | 默认值 25，灵敏度有限差分百分比增量。 |
| VaryUncertainty | 标量名称-值对 | Bool | 默认值 false，兼容性布尔字段。 |
| MussvOptions | 标量名称-值对 | String | 默认值 ""，底层 mussv 选项字符串。 |

### 文档自身缺陷审计

- “正实数”未指定具体 Julia 类型；实现字段类型为 `Real`，接受整数和浮点数，但 `wcOptions` 构造函数本身不检查正值，实际正数约束由 `wcgain` 在使用时检查，文档未说明错误发生位置。
- 示例中展示了点符号赋值设置选项，但输入参数表仅声明名称-值参数，点符号赋值未作为正式输入形式列出，覆盖范围存在轻微歧义。
- `Display` 说明中“是否显示 mussv 计算进度的字符串”表述不通顺，可能应为“进度信息”或“进度字符串”；其与 `MussvOptions` 中 `"s"` 静默选项的交互描述较模糊。
- `SensitivityPercent` 未像 `ULevel` 那样给出非默认正实数示例或边界说明，约束描述可进一步完整。

### 参数数据类型与结构覆盖

#### ULevel

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; ULevel=0.75, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| 正实数（Real） | 显式传入 0.75 浮点值并断言 `custom_options.ULevel == 0.75` | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |

#### Display

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; Display=true, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| Bool | 显式传入 true 并断言；默认 false 在 default_options 中断言 | test_wcOptions.jl、test_wcgain 中 custom_options 与 default_options | ☑ |

#### Sensitivity

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; Sensitivity=true, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| Bool | 显式传入 true 并断言；默认 false 在 default_options 中断言 | test_wcOptions.jl、test_wcgain 中 custom_options 与 default_options | ☑ |

#### VaryFrequency

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; VaryFrequency=true, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| Bool | 显式传入 true 并断言；curve_options 再次传入 true 并影响 wcgain 频率曲线 | test_wcOptions.jl、test_wcgain 中 custom_options 与 curve_options | ☑ |

#### SensitivityPercent

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; SensitivityPercent=10, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| 正实数（Real） | 显式传入 Int 10，属于 Real 且为正数，断言 `custom_options.SensitivityPercent == 10` | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |

说明：当前仅以 Int 10 覆盖 SensitivityPercent 的“正实数”数据类型；文档未要求区分整数与浮点具体类型，因此不构成缺失，但如需更完整可补 Float64 样本。

#### VaryUncertainty

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; VaryUncertainty=true, ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| Bool | 显式传入 true 并断言；默认 false 在 default_options 中断言 | test_wcOptions.jl、test_wcgain 中 custom_options 与 default_options | ☑ |

#### MussvOptions

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 标量名称-值对 | `wcOptions(; MussvOptions="f", ...)` 显式传入并断言字段 | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |
| String | 显式传入字符串 "f" 并断言 `custom_options.MussvOptions == "f"` | test_wcOptions.jl、test_wcgain 中 custom_options | ☑ |

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 标量名称-值对 | 所有 7 个参数通过 `wcOptions(; ...)` 一次显式传入并断言 | ☑ | custom_options 覆盖 |

### 覆盖缺口与优先级

- 高优先级：无。
- 中优先级：无。
- 低优先级：无。

说明：文档要求的全部参数结构形式和数据类型均已找到明确构造、调用和结果断言证据，没有完全缺失的独立维度。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| ULevel | 标量名称-值对、正实数 | 标量名称-值对、正实数 | 无 | 完全覆盖 |
| Display | 标量名称-值对、Bool | 标量名称-值对、Bool | 无 | 完全覆盖 |
| Sensitivity | 标量名称-值对、Bool | 标量名称-值对、Bool | 无 | 完全覆盖 |
| VaryFrequency | 标量名称-值对、Bool | 标量名称-值对、Bool | 无 | 完全覆盖 |
| SensitivityPercent | 标量名称-值对、正实数 | 标量名称-值对、正实数 | 无 | 完全覆盖 |
| VaryUncertainty | 标量名称-值对、Bool | 标量名称-值对、Bool | 无 | 完全覆盖 |
| MussvOptions | 标量名称-值对、String | 标量名称-值对、String | 无 | 完全覆盖 |

总计：完全覆盖参数数 7，至少有证据参数数 7。

### 缺失测试代码补全

未发现文档要求项存在覆盖缺失，因此不追加补测代码。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
wcOptions，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自用户电脑本地文件：

代码/单测：
C:\Users\TR\Desktop\doc\TyRobustControl.jl\test\Uncertain System Analysis\Robustness and Worst-Case Analysis\test_wcOptions.jl
C:\Users\TR\Desktop\doc\TyRobustControl.jl\src\Uncertain System Analysis\Robustness and Worst-Case Analysis\wcOptions.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyRobustControl\Doc\TyRobustControl\UncertainSystemAnalysis\RobustnessAndWorst-CaseAnalysis\wcOptions.md

TyRobustControl：
```

## 四、上级汇报格式（可直接复制）

```text
wcOptions，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：wcOptions
遗留缺陷：无
```
