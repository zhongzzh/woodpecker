# writeVideo 提测分析报告

> 任务：writeVideo（本地材料：C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_video\test_writeVideo.jl）
> 任务类型：本地材料分析
> 文档：本地 syslab-docs-2.0（当前工作区（未切换））`syslabHelpSourceCode/projects/TyImageProcessing/Doc/TyImageProcessing/DataImportandAnalysis/DataImportandExport/StandardFileFormats/AudioandVideoData/ReadorWriteVideo/writeVideo.md`
> 单测：`test/im_video/test_writeVideo.jl`
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-11 10:01 ｜ 模型：deepseek-v4-pro

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| v | VideoWriter 对象 | VideoWriter | 已调用 open 的 VideoWriter 对象 |
| img | 二维数组 | Float32、Float64、UInt8 | 灰度或单色图像，高×宽；AVI 文件 Float32/Float64 须在 [0,1] 内 |
| img | 三维数组 | Float32、Float64、UInt8 | RGB 真彩色图像，高×宽×3；AVI 文件 Float32/Float64 须在 [0,1] 内 |
| img | 四维数组 | Float32、Float64、UInt8 | 灰度序列(高×宽×1×帧数)或 RGB 序列(高×宽×3×帧数) |
| img (MJ2) | 二维数组 | UInt8、Int8、UInt16、Int16 | Motion JPEG 2000 文件单色图像，高×宽 |
| img (MJ2) | 三维数组 | UInt8、Int8、UInt16、Int16 | Motion JPEG 2000 文件 RGB 图像，高×宽×3 |
| img (MJ2) | 四维数组 | UInt8、Int8、UInt16、Int16 | Motion JPEG 2000 文件单色或 RGB 序列 |

### 文档自身缺陷审计

*   三维、四维数组对 MJ2 文件的支持未明确声明，仅从二维数组的 MJ2 部分和 img 总述中推断。
*   AVI 文件下，三维和四维数组未像二维数组那样显式列出 Float32/Float64 和 UInt8，存在遗漏。
*   MJ2 文件下，三维和四维数组的数据类型要求未在数据类型行中体现，数据类型行只列出了 UInt8、Int8、UInt16、Int16。
*   文档示例 `writeVideo(v, A)` 中 `A` 为 `imread` 结果，`imread` 返回类型未知，示例未显式验证 img 数据类型。

### 参数数据类型与结构覆盖

#### v

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| VideoWriter 对象 | 已调用 open 的 VideoWriter | test_writeVideo.jl + "src" 子测试集内多处 | ☑ |

#### img

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 二维 Float64 (AVI) | 灰度图像 | test_writeVideo.jl + "writeVideo补充" > "二维 Float64" | ☑ |
| 二维 Int16 (MJ2) | 单色图像 | test_writeVideo.jl + "writeVideo补充" > "二维 Int16 MJ2" | ☑ |
| 二维 UInt16 (MJ2) | 单色图像 | test_writeVideo.jl + "writeVideo补充" > "二维 UInt16 MJ2" | ☑ |
| 三维 Float32 (AVI RGB) | RGB 图像 | test_writeVideo.jl + "writeVideo补充" > "三维 Float32 (AVI RGB)" | ☑ |
| 四维 UInt8 (AVI 灰度序列) | 高×宽×1×帧数 | test_writeVideo.jl + "writeVideo补充" > "四维灰度序列 UInt8" | ☑ |
| 二维 Float32 (AVI) | 灰度图像 | test_writeVideo.jl + "img" 子测试集，`writeVideo(v, A)` 调用 | ☑ |
| 二维 UInt8 (AVI) | 灰度或 RGB (imread 结果) | test_writeVideo.jl + "src" 子测试集，`writeVideo(v, A)` 调用 | ☑ |
| 二维 Float32 (MJ2) | 该组合未被文档显式支持或禁止，但存在调用证据 | test_writeVideo.jl + "performance" 子测试集，写入 `.mj2` 后缀推断 | ☑ |
| 二维 UInt8 (MJ2) | 文档声明的 MJ2 支持类型 | 未找到对应测试用例 | 未覆盖 |
| 二维 Int8 (MJ2) | 文档声明的 MJ2 支持类型 | 未找到对应测试用例 | 未覆盖 |
| 三维 UInt8 (AVI RGB) | RGB 图像 | 未找到对应测试用例 | 未覆盖 |
| 三维 Float64 (AVI RGB) | RGB 图像 | 未找到对应测试用例 | 未覆盖 |
| 三维 UInt8 (MJ2 RGB) | 文档未明确但由二维推断支持 | 未找到对应测试用例 | 未覆盖 |
| 三维 Int8 (MJ2 RGB) | 文档未明确但由二维推断支持 | 未找到对应测试用例 | 未覆盖 |
| 三维 Int16 (MJ2 RGB) | 文档未明确但由二维推断支持 | 未找到对应测试用例 | 未覆盖 |
| 三维 UInt16 (MJ2 RGB) | 文档未明确但由二维推断支持 | 未找到对应测试用例 | 未覆盖 |
| 四维 Float32 (AVI) | 灰度或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |
| 四维 Float64 (AVI) | 灰度或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |
| 四维 UInt8 (MJ2) | 单色或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |
| 四维 Int8 (MJ2) | 单色或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |
| 四维 Int16 (MJ2) | 单色或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |
| 四维 UInt16 (MJ2) | 单色或 RGB 序列 | 未找到对应测试用例 | 未覆盖 |

说明：
*   `performance` 测试集使用 `Float32` 数组并写入推断的 `.mj2` 文件，但未对文件类型和写入数据做断言，属于弱证据。
*   `img` 测试集对 `Float32` 的调用无后续断言，属于弱证据。
*   `src` 测试集依赖 `imread` 返回的 `UInt8` 或 `Float32` 数组，未做类型断言，属于弱证据。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| 二维数组 | 高×宽 | ☑ | 覆盖了部分类型，部分依赖弱证据 |
| 三维数组 | 高×宽×3 | 未覆盖 | 仅 Float32 有覆盖，其余类型均缺失 |
| 四维数组 | 高×宽×1×帧数 或 高×宽×3×帧数 | 未覆盖 | 仅 UInt8 灰度序列有覆盖 |

### 覆盖缺口与优先级

**高优先级**

*   三维 UInt8 数组 (AVI RGB)：文档明确支持，但无测试用例。需构造 RGB UInt8 图像并写入 AVI。
*   四维 Float32 数组 (AVI 序列)：文档明确支持，但无测试用例。需构造 Float32 序列并写入 AVI。
*   二维 UInt8 数组 (MJ2)：文档明确支持，但无测试用例。需构造 UInt8 图像并写入 MJ2。
*   二维 Int8 数组 (MJ2)：文档明确支持，但无测试用例。需构造 Int8 图像并写入 MJ2。

**中优先级**

*   三维 Float64 数组 (AVI RGB)、四维 Float64 数组 (AVI 序列)：文档对 AVI 文件明确支持 Float64，但无三维和四维测试。
*   四维 UInt8 数组 (MJ2 序列)：文档在 MJ2 部分支持 UInt8，但无四维序列测试。

**低优先级**

*   三维、四维数组与 MJ2 文件的各种 Int8、Int16、UInt16 组合：文档未明确声明这些组合，但由其数据类型声明可推导，当前均无测试。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 缺失项 | 参数结论 |
| --- | --- | --- | --- | --- |
| v | VideoWriter 对象 | VideoWriter 对象 | 无 | 完全覆盖 |
| img | 二维 Float64 (AVI)、二维 Int16 (MJ2)、二维 UInt16 (MJ2)、三维 Float32 (AVI RGB)、四维 UInt8 (AVI 灰度序列)、二维 Float32 (AVI)、二维 UInt8 (AVI) | 二维: Float32/Float64/UInt8( AVI)/UInt8/Int8/UInt16/Int16(MJ2); 三维: Float32/Float64/UInt8( AVI); 四维: Float32/Float64/UInt8( AVI) | 二维 Int8(MJ2)、二维 UInt8(MJ2)、三维 UInt8(AVI RGB)、三维 Float64(AVI RGB)、四维 Float32(AVI)、四维 Float64(AVI) | 部分覆盖 |

总计：`v` 完全覆盖，`img` 部分覆盖。完全覆盖参数数：1。至少有证据参数数：2。

### 缺失测试代码补全

```julia
@testset "writeVideo 补充" begin
    @testset "二维 UInt8 MJ2" begin
        output = tempname() * ".mj2"
        try
            img = rand(UInt8, 120, 90)
            v = VideoWriter(output, [90, 120])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 1
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end

    @testset "二维 Int8 MJ2" begin
        output = tempname() * ".mj2"
        try
            img = Int8.(rand(-128:127, 100, 80))
            v = VideoWriter(output, [80, 100])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 1
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end

    @testset "三维 UInt8 AVI RGB" begin
        output = tempname() * ".avi"
        try
            img = rand(UInt8, 60, 80, 3)
            v = VideoWriter(output, [80, 60])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 1
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end

    @testset "三维 Float64 AVI RGB" begin
        output = tempname() * ".avi"
        try
            img = Float64.(rand(70, 90, 3))
            v = VideoWriter(output, [90, 70])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 1
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end

    @testset "四维 Float32 AVI 灰度序列" begin
        output = tempname() * ".avi"
        try
            img = Float32.(rand(40, 50, 1, 4))
            v = VideoWriter(output, [50, 40])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 4
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end

    @testset "四维 Float64 AVI 灰度序列" begin
        output = tempname() * ".avi"
        try
            img = Float64.(rand(55, 65, 1, 3))
            v = VideoWriter(output, [65, 55])
            open(v)
            writeVideo(v, img)
            close(v)
            @test v.FrameCount == 3
            @test isfile(output)
        finally
            rm(output; force=true)
        end
    end
end
```

静态检验说明：此新增代码块是一个独立的顶层 `@testset`，作用域自足，复用了原文件的 `using` 导入和 `tempname`、`VideoWriter` 等函数。可整体追加到 `test_writeVideo.jl` 文件末尾。

仍需 baseline 校准项：所有新增测试仅校验 `FrameCount` 和文件存在性，未校验写入视频的像素真值。需要 baseline 视频或已知像素值进行像素级回归验证。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
writeVideo，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自本地仓库分支 lxf/update_VideoWriter 自动定位：

代码/单测：
C:\Users\TR\Desktop\doc\TyImageProcessing.jl\test\im_video\test_writeVideo.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TyImageProcessing\Doc\TyImageProcessing\DataImportandAnalysis\DataImportandExport\StandardFileFormats\AudioandVideoData\ReadorWriteVideo\writeVideo.md

TyImageProcessing：
```

## 四、上级汇报格式（可直接复制）

```text
writeVideo，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：writeVideo
遗留缺陷：无
```
