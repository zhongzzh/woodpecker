# ztrans 提测分析报告

> 任务：ztrans（本地材料：C:\Users\TR\Desktop\doc\TySymbolicMath.jl\test\Symbolic Math\Calculus\Transforms\ztrans.jl）
> 任务类型：本地材料分析
> 文档：用户本地文件 `C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TySymbolicMath\Doc\TySymbolicMath\SymbolicMath\Calculus\Transforms\ztrans.md`、`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\MultiLanguage\Doc\MultiLanguage\TyMLang\Functions\SymbolicMathToolbox\Mathematics\iztrans.md`、`C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TySymbolicMath\Doc\TySymbolicMath\SymbolicMath\Calculus\Transforms\iztrans.md`
> 单测：`test/Symbolic Math/Calculus/Transforms/ztrans.jl`（伴随数据 2 个）
> benchmark：未定位到
> HTML 预览：未执行
> 分析规则：提示词 v2（本地文档/代码覆盖分析） ｜ 生成：2026-08-05 09:39 ｜ 模型：gpt-5.6-sol

## 一、单元测试覆盖分析（AI）

### 文档定义的输入参数

#### ztrans

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| f | 符号表达式、函数、向量、矩阵 | Num | 输入序列或符号函数；数组输入按元素处理 |
| var | 标量、向量、矩阵 | 符号变量 | 自变量；默认值为 n |
| transVar | 标量、向量、矩阵 | 符号变量、符号表达式、Num | 变换变量；默认值为 z |

#### iztrans

| 参数 | 允许形式 | 允许数据类型 | 说明 |
| --- | --- | --- | --- |
| F | 符号表达式、函数、向量、矩阵 | Num | Z 域输入；数组输入按元素处理 |
| var | 标量、向量、矩阵 | 符号变量、符号表达式、Num | Z 域自变量；文档版本之间默认值描述不一致 |
| transVar | 标量、向量、矩阵 | 符号变量、符号表达式、Num | 逆变换后的变量；默认值描述不一致 |

### 文档自身缺陷审计

- `ztrans.md` 的 `transVar` 参数摘要写为 `z（默认） | Num`，正文又允许符号变量、表达式、向量或矩阵，类型和结构描述不一致。
- `ztrans.md` 将 `f` 的类型摘要写为 `Num`，正文另外声明允许函数、向量和矩阵，但没有清晰区分符号函数对象、符号函数调用表达式以及普通 Julia 函数。
- `ztrans.md` 的示例 `@variables m ,n` 含有不规范的逗号空格，属于示例书写问题，但不影响语义。
- `ztrans.md` 示例中将 `ztrans(f, m, y)` 说明为“自变量为 m、变换变量为 y”，但 `f = exp(m+n)` 中 m 同时也是表达式中的符号变量，文档没有明确说明这种替换是否按元素变量绑定还是仅改变求和索引。
- `iztrans` 文档出现两套内容。前半部分的参数摘要将 `var` 默认值写为 `x`，将 `transVar` 默认值写为 `x` 或允许值 `t`；后半部分及说明则写为默认自变量 `z`、默认变换变量 `n`。实现中的 `_iztrans_get_independent_variable` 也使用 `z` 和 `n`，文档应统一。
- `iztrans` 两套文档对 `F` 的类型描述不同：一处摘要写为“符号表达式、符号向量、符号矩阵”，另一处写为 `Num` 且正文声称允许函数、向量或矩阵。
- `iztrans` 文档允许 `var` 和 `transVar` 使用符号表达式、向量或矩阵，但没有说明非标量参数之间必须尺寸一致，也没有明确说明标量扩展规则适用于哪些参数组合。
- `iztrans` 文档中的 MATLAB 示例和 Julia 示例混杂，且被注释掉的符号函数示例没有结果内容，无法作为稳定的公开行为说明。
- 给出的 `ztrans.jl` 内容包含测试文件和 `iztrans` 实现，未提供 `ztrans` 的实现代码，因此本次只能依据文档和测试证据审计 `ztrans`，不能核对其实现签名与分派范围。

### 参数数据类型与结构覆盖

#### f（ztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Num 标量符号表达式 | 使用 `sin`、`cos`、幂函数和多项式序列 | `ztrans_test1`、`ztrans_test2`、`ztrans evaluates geometric sequences` | ☑ |
| 符号函数或符号函数调用 | 文档声明允许函数，现有用例只测试 `tan(n)` 等普通符号表达式 | 无明确函数对象构造证据 | 未覆盖 |
| Num 向量 | 文档提示数组按元素处理，现有测试没有一维向量输入 | 无明确向量构造和结果断言 | 未覆盖 |
| Num 矩阵 | 构造了含 `exp`、常数、`sin` 和复数项的 2×2 矩阵，并断言尺寸和逐元素结果 | `ztrans supports complex matrices` | ☑ |

#### var（ztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认自变量 n | 未显式传入时使用默认路径 | `ztrans_test1`、`ztrans evaluates geometric sequences` | ☑ |
| 标量符号变量 | 显式传入 `n`、`m` 等符号变量并断言结果 | `ztrans_test2`、`ztrans follows documented variable selection` | ☑ |
| 符号表达式 | 文档未明确给出表达式变量的独立测试 | 无明确构造、调用和结果断言 | 未覆盖 |
| 符号向量 | 文档允许数组形式，但没有向量作为 `var` 的测试 | 无明确构造、调用和结果断言 | 未覆盖 |
| 符号矩阵 | `vars = [w x; y z]` 作为自变量矩阵传入，并断言矩阵结果 | `ztrans supports complex matrices` | ☑ |

#### transVar（ztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认变换变量 z | 未显式传入时执行默认变换变量路径 | `ztrans_test1`、`ztrans_test2` | ☑ |
| 标量符号变量 | 显式使用 `w`、`y`、`z` 等变换变量并断言结果 | `ztrans uses its independent variable as summation index`、`ztrans follows documented variable selection` | ☑ |
| 符号表达式或 Num 表达式 | 文档摘要允许 Num，但没有独立的表达式变换变量测试 | 无明确构造、调用和结果断言 | 未覆盖 |
| 符号向量 | 文档声明数组形式，但没有一维向量变换变量测试 | 无明确构造、调用和结果断言 | 未覆盖 |
| 符号矩阵 | `transvars = [a b; c d]` 传入并断言逐元素结果 | `ztrans supports complex matrices` | ☑ |

#### F（iztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| Num 标量符号表达式 | 覆盖有理函数、幂函数和未计算的 Z 变换节点 | `iztrans(F)_test1`、`iztrans(F)_test2`、`iztrans inverts unevaluated ztrans nodes` | ☑ |
| 符号函数或符号函数调用 | 文档的一处版本允许函数，相关示例被注释且测试文件没有对应输入 | 无明确函数输入构造及结果断言 | 未覆盖 |
| Num 向量 | 构造 `[1 / z, 1 / z^2]` 并断言输出尺寸及两个元素 | `iztrans handles documented scalar and array forms` | ☑ |
| Num 矩阵 | 构造 2×2 矩阵并使用逐元素变量矩阵和变换变量矩阵 | `iztrans(F::Matrix,var::Matrix,transvar::Matrix)_test2` | ☑ |

#### var（iztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认自变量 z | 调用 `iztrans(F)`，并断言默认逆变换结果 | `iztrans(F)_test1`、`iztrans(F)_test2` | ☑ |
| 标量符号变量 | 显式传入 `z`、`s`、`a` 等变量并断言结果 | `iztrans(F,var,transvar)_test1`、`iztrans_bug_1` | ☑ |
| 符号表达式或 Num 表达式 | 文档允许表达式，但现有用例中的 `var` 均为单个符号变量 | 无明确表达式变量测试 | 未覆盖 |
| 符号向量 | 文档允许向量，但现有向量用例仅将向量作为 `transVar` 传入 | 无明确向量 `var` 测试 | 未覆盖 |
| 符号矩阵 | 使用 `vars = [w x; y z]` 作为自变量矩阵并断言矩阵结果 | `iztrans(F::Matrix,var::Matrix,transvar::Matrix)_test2` | ☑ |

#### transVar（iztrans）

| 数据类型或结构 | 说明 | 对应测试用例位置 | 覆盖状态 |
| --- | --- | --- | --- |
| 默认变换变量 n | 默认调用结果中使用 `n` 并进行结果断言 | `iztrans(F)_test1`、`iztrans(F)_test2` | ☑ |
| 标量符号变量 | 显式传入 `m`、`n` 等变换变量并断言结果 | `iztrans(F,transvar)_test1`、`iztrans(F,var,transvar)_test1` | ☑ |
| 符号表达式或 Num 表达式 | 文档允许表达式，但没有表达式形式的变换变量用例 | 无明确构造、调用和结果断言 | 未覆盖 |
| 符号向量 | 使用 `[n, m]` 作为变换变量并断言逐元素结果 | `iztrans handles documented scalar and array forms` | ☑ |
| 符号矩阵 | 使用 `transVars = [a b; c d]` 并断言矩阵结果 | `iztrans(F::Matrix,var::Matrix,transvar::Matrix)_test2` | ☑ |

说明：`ztrans` 的矩阵用例对 `f`、`var` 和 `transVar` 同时提供了构造、调用及结果断言证据，因此这些矩阵项可计为覆盖。`iztrans` 的矩阵用例使用 `string(...)` 比较结果，仍然属于明确结果断言，但比直接的结构化符号等价断言弱。

#### 数据结构覆盖总结

| 数据结构 | 说明 | 覆盖状态 | 备注 |
| --- | --- | --- | --- |
| ztrans 标量输入 | `f`、`var`、`transVar` 的标量符号形式均有结果断言 | ☑ | 未覆盖 ztrans 的函数对象和表达式变量形式 |
| ztrans 向量输入 | 文档声明数组按元素处理，但现有测试没有一维向量 | 未覆盖 | 建议增加向量 `f` 及向量变量参数测试 |
| ztrans 矩阵输入 | `f`、`var`、`transVar` 均使用 2×2 矩阵并逐元素断言 | ☑ | 已覆盖复数矩阵输入 |
| iztrans 标量输入 | 标量 `F`、`var`、`transVar` 均有明确结果断言 | ☑ | 函数输入仍未覆盖 |
| iztrans 向量输入 | `F` 向量和 `transVar` 向量已覆盖 | ☑ | `var` 向量现有测试缺失 |
| iztrans 矩阵输入 | `F`、`var`、`transVar` 均有 2×2 矩阵用例 | ☑ | 结果通过字符串化矩阵比较 |
| 符号函数输入 | 两个文档均提及或暗示符号函数，但测试中没有对应输入 | 未覆盖 | 还需明确符号函数对象的 API 语义和 expected |

### 覆盖缺口与优先级

#### 高优先级

- `ztrans` 的 Num 向量输入未覆盖。现有测试只证明标量和 2×2 矩阵，无法证明一维数组按元素处理；建议增加向量输入并断言输出尺寸及每个元素的变换结果。
- `ztrans` 的 `var` 向量和 `transVar` 向量未覆盖。矩阵用例不能替代一维向量结构；建议使用与向量 `f` 等长的变量向量进行逐元素断言。
- `iztrans` 的 `var` 向量未覆盖。已有向量用例只把向量放在 `F` 或 `transVar` 位置；建议增加向量自变量并验证逐元素输出。
- 文档对 `iztrans` 默认自变量和变换变量的描述互相矛盾。应先统一为实现和现有测试实际支持的 `z`、`n`，再将默认路径作为稳定覆盖目标。

#### 中优先级

- `ztrans` 和 `iztrans` 的符号表达式形式 `var`、`transVar` 均缺少独立测试。现有证据仅覆盖标量符号变量和数组变量；建议使用简单可计算表达式，并优先断言返回类型、形状和变量绑定性质。
- 文档声明的函数输入没有有效测试证据。建议先确定 API 接受的是符号函数调用表达式还是 Julia 可调用对象，再增加对应测试；不能用普通 `Num` 表达式代替函数对象覆盖。
- `iztrans` 的标量扩展规则缺少系统覆盖。现有 `transVar` 向量用例证明了一种标量到向量扩展，但没有同时覆盖矩阵扩展及不同非标量参数尺寸不一致时的异常行为。

#### 低优先级

- `ztrans` 文档中 `f` 的 `Num` 摘要与“函数、向量、矩阵”正文应统一为明确的类型和结构列表。
- `iztrans` 两份文档的参数摘要、示例语言和默认值应合并，避免 MATLAB 与 Julia 示例混用造成调用约定歧义。
- 可增加对不支持输入或尺寸不一致输入的异常断言，以补足文档中“所有非标量参数必须具有相同大小”等约束的可验证性。
- `iztrans` 的矩阵测试目前使用字符串比较，建议增加尺寸、元素类型和逐元素结构断言，减少字符串格式变化带来的脆弱性。

### 差异化度量总结

| 参数 | 完全覆盖项 | 文档要求项 | 参数结论 |
| --- | --- | --- | --- |
| ztrans.f | Num 标量、Num 矩阵 | 符号表达式、函数、向量、矩阵 | 部分覆盖 |
| ztrans.var | 默认标量、标量符号变量、符号矩阵 | 标量、向量、矩阵及默认路径 | 部分覆盖 |
| ztrans.transVar | 默认标量、标量符号变量、符号矩阵 | 标量、表达式、向量、矩阵及默认路径 | 部分覆盖 |
| iztrans.F | Num 标量、Num 向量、Num 矩阵 | 符号表达式、函数、向量、矩阵 | 部分覆盖 |
| iztrans.var | 默认标量、标量符号变量、符号矩阵 | 标量、表达式、向量、矩阵及默认路径 | 部分覆盖 |
| iztrans.transVar | 默认标量、标量符号变量、符号向量、符号矩阵 | 标量、表达式、向量、矩阵及默认路径 | 部分覆盖 |

总计：

- 完全覆盖参数数：0。
- 至少有明确证据的参数数：6。
- 由于每个参数仍缺少至少一种文档声明的类型或结构，因此没有参数可判定为“完全覆盖”。
- 本报告不将部分覆盖折算为精确百分比；补测代码将补齐可确定数学真值的向量结构缺口，但函数对象和表达式变量仍需 API 语义确认。

### 缺失测试代码补全

```julia
@testset "ztrans 与 iztrans 补充覆盖" begin
    @testset "ztrans 向量输入" begin
        @variables n z
        f = [(-1)^n, n]
        result = ztrans(f, n, z)

        @test size(result) == (2,)
        @test isequal(result[1], z / (z + 1))
        @test isequal(sym_simplify(result[2] - z / (z - 1)^2), 0)
    end

    @testset "ztrans 向量自变量和变换变量" begin
        @variables n1 n2 z1 z2
        f = [(-1)^n1, (-1)^n2]
        vars = [n1, n2]
        transvars = [z1, z2]
        result = ztrans(f, vars, transvars)

        @test size(result) == (2,)
        @test isequal(result[1], z1 / (z1 + 1))
        @test isequal(result[2], z2 / (z2 + 1))
    end

    @testset "iztrans 向量自变量" begin
        @variables z n1 n2
        F = [1 / z, 1 / z^2]
        vars = [z, z]
        transvars = [n1, n2]
        result = iztrans(F, vars, transvars)

        @test size(result) == (2,)
        @test isequal(result[1], kroneckerDelta(n1 - 1, 0))
        @test isequal(result[2], kroneckerDelta(n2 - 2, 0))
    end
end
```

#### 静态检验说明

- 新增代码只有一个独立的顶层 `@testset "ztrans 与 iztrans 补充覆盖"`，内部场景均为子测试块。
- 每个新增场景都在自身作用域内重新构造符号变量、输入数据、调用结果和断言，不引用原有测试块中的局部变量。
- 新增代码可原样整体追加到文件末尾，不需要插入、替换、删除或改写任何现有代码。
- 新增断言覆盖了 `ztrans` 的一维向量输入、向量自变量、向量变换变量，以及 `iztrans` 的向量自变量。
- 代码中的 expected 来自几何序列和序列 `n`、`1/z`、`1/z^2` 的明确变换关系，不依赖未校准的复杂 baseline。
- 以上属于静态审计结论，未声称已经运行这些新增测试。

#### 仍需 baseline 校准项

- `ztrans` 的符号函数对象输入仍需确认实际 API 形式，例如符号函数调用表达式与 Julia 可调用对象是否分别受支持；确认后再补充结果断言。
- `ztrans` 和 `iztrans` 的符号表达式形式 `var`、`transVar` 需要先确认实现是否接受非变量 `Num` 表达式，以及表达式在内部是否被转换为合法的求和或逆变换变量。
- `ztrans` 向量变量和 `iztrans` 向量自变量的新增精确结果需要在目标 Julia 环境中运行校准；如果库的数组广播或符号变量绑定规则不同，应改为断言返回形状、元素类型和逐元素变量绑定性质。
- `iztrans` 文档声明的尺寸不一致异常、标量扩展到矩阵的行为，以及未能计算时返回未求值调用表达式，仍缺少可直接从现有材料确定的 baseline。

## 二、性能测试判定

### 性能分析

> 本次使用本地材料，用户未提供性能报告；按本地模式约定，本次不进行性能测试分析。

## 三、总结邮件格式（可直接复制）

```text
ztrans，功能验证通过，本次未进行性能分析，请补充自动化脚本
分析材料来自用户电脑本地文件：

代码/单测：
C:\Users\TR\Desktop\doc\TySymbolicMath.jl\test\Symbolic Math\Calculus\Transforms\ztrans.jl
C:\Users\TR\Desktop\doc\TySymbolicMath.jl\test\Symbolic Math\Calculus\Transforms\iztrans.jl
C:\Users\TR\Desktop\doc\TySymbolicMath.jl\src\Symbolic Math\Calculus\Transforms\iztrans.jl

帮助文档：
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TySymbolicMath\Doc\TySymbolicMath\SymbolicMath\Calculus\Transforms\ztrans.md
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\MultiLanguage\Doc\MultiLanguage\TyMLang\Functions\SymbolicMathToolbox\Mathematics\iztrans.md
C:\Users\TR\Desktop\doc\syslab-docs-2.0\syslabHelpSourceCode\projects\TySymbolicMath\Doc\TySymbolicMath\SymbolicMath\Calculus\Transforms\iztrans.md

TySymbolicMath：
```

## 四、上级汇报格式（可直接复制）

```text
ztrans，功能单点验证通过，本次未进行性能分析
测试环境：windows
测试项：ztrans
遗留缺陷：无
```
